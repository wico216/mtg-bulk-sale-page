---
phase: 16-schema-migration
plan: 01
type: execute
wave: 1
depends_on: ["15-02"]
files_modified:
  - scripts/migrate-v1.3-binder.ts
  - scripts/__tests__/migrate-v1.3-binder.test.ts
  - src/db/schema.ts
  - src/db/__tests__/schema.test.ts
  - package.json
autonomous: true
requirements:
  - BIND-01
  - BIND-02
  - BIND-03
  - BIND-04
  - FIN-01
must_haves:
  truths:
    - "Migration is a custom Drizzle SQL file applied as a one-shot atomic batch with the exact ordering documented in 16-CONTEXT.md <specifics> (D-01, D-02)"
    - "Three pre-flight idempotency assertions run BEFORE any DML and exit non-zero with zero changes if any fails (D-04)"
    - "Every existing cards row carries binder = 'unsorted' and a 5-segment composite id ending in -unsorted after migration (BIND-01, BIND-02, D-05, D-06)"
    - "cards.finish is a Postgres enum ('normal' / 'foil' / 'etched') backfilled from the legacy foil boolean with zero data loss; the foil column is dropped (FIN-01, D-07)"
    - "cards.quantity carries CHECK (quantity >= 0); a manual attempt to drive a row negative raises a constraint-violation error (BIND-04, D-08)"
    - "order_items has a binder text NOT NULL DEFAULT 'unsorted' column populated for both historical rows (default) and any new row written after migration (BIND-03, D-09)"
    - "src/db/schema.ts is updated to match the post-migration shape so future drizzle-kit push runs converge"
    - "Operator runs the migration MANUALLY via npm run migrate:v1.3 (with --dry-run gate against a Neon branch first); no Vercel build hook, no Neon-console paste (D-11, D-12, D-13)"
    - "The terminal summary printed at the end of the script is the SOLE verification surface (D-14, D-15) and includes pre-flight outcomes, schema deltas, row-count diffs, finish backfill counts, constraint presence, and 5 sample post-migration ids"
    - "No down migration; the migration script header comment documents the Neon point-in-time-restore rollback recipe (D-16, D-17)"
  artifacts:
    - path: "scripts/migrate-v1.3-binder.ts"
      provides: "One-shot custom Drizzle migration for the v1.3 binder + finish + CHECK schema flip"
      exports: ["main", "runPreflights", "buildBatchStatements", "printSummary"]
      contains: "runPreflights, db.batch, --dry-run"
    - path: "scripts/__tests__/migrate-v1.3-binder.test.ts"
      provides: "Pre-flight + statement-builder coverage (no live DB writes)"
      contains: "preflight, second-run rejection, batch ordering"
    - path: "src/db/schema.ts"
      provides: "Post-migration Drizzle schema (binder, finish enum, CHECK, order_items.binder; foil dropped)"
      contains: "pgEnum('finish'), binder, quantity check"
    - path: "src/db/__tests__/schema.test.ts"
      provides: "Schema invariant pins for the new shape"
      contains: "finish enum, binder column, order_items.binder, no foil column"
    - path: "package.json"
      provides: "migrate:v1.3 + migrate:v1.3:dry-run scripts"
      contains: "migrate:v1.3"
---

<objective>
Land the v1.3 schema floor: a custom, atomic, idempotent Drizzle migration that adds the `binder` dimension to `cards` and `order_items`, replaces `foil: boolean` with a 3-value `finish` enum, rewrites every `cards.id` to the 5-segment composite, and adds `CHECK (quantity >= 0)` — applied manually by the operator via `npm run migrate:v1.3`, gated by a Neon-branch dry run, and verified through a structured terminal summary.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-schema-migration/16-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/research/SUMMARY.md
@.planning/phases/15-production-hardening/15-02-SUMMARY.md
@.planning/phases/14-inventory-audit-trail/14-01-SUMMARY.md
</execution_context>

<context>
@src/db/schema.ts
@src/db/client.ts
@src/db/queries.ts
@src/db/__tests__/schema.test.ts
@package.json
@drizzle.config.ts
@scripts/smoke-production.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: db.batch typing spike (Stage A — 30 min, blocks Task 3 shape)</name>
  <files>
    scripts/migrate-v1.3-binder.ts
  </files>
  <read_first>
    src/db/queries.ts,
    src/db/client.ts,
    node_modules/drizzle-orm/neon-http/session.d.ts,
    node_modules/drizzle-orm/neon-http/driver.d.ts,
    .planning/research/ARCHITECTURE.md
  </read_first>
  <behavior>
    - Confirm whether `db.batch([db.execute(sql\`ALTER TABLE ...\`), db.execute(sql\`UPDATE ...\`), ...])` typechecks against the installed `drizzle-orm@0.45.2 / @neondatabase/serverless@1.0.2` pair.
    - Pick the apply path before writing the migration script body (D-03).
  </behavior>
  <action>
    Create a `scripts/migrate-v1.3-binder.ts` skeleton (file header + imports + a tiny no-op `BATCH_PROBE` constant array typed via `Parameters&lt;typeof db.batch&gt;[0]`) just to exercise the types under `npx tsc --noEmit`. Time-box to 30 minutes.

    Decide one of:
      - **Path A (preferred):** `db.batch([db.execute(sql\`...\`), ...])` typechecks → use the per-statement batch shape from CONTEXT.md D-02 verbatim.
      - **Path B (fallback):** typings reject raw `sql` calls inside the batch → switch to a single multi-statement raw call: `await db.execute(sql\`BEGIN; ALTER ...; UPDATE ...; ALTER ...; COMMIT;\`)`. Neon's HTTP transaction endpoint supports the multi-statement shape (per ARCHITECTURE.md NOTE).

    Document the chosen path + reason in the script header and in 16-01-SUMMARY.md.

    No live DB connection during the spike. Verification is purely TypeScript-level.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The chosen apply path is recorded in the script header with a one-paragraph justification.
    - `npx tsc --noEmit` is green with the probe in place.
    - If Path B is chosen, no claim about per-statement atomicity is made beyond what `BEGIN ... COMMIT;` in a single Neon HTTP request guarantees.
  </acceptance_criteria>
  <done>Apply path is locked; the rest of the plan can target it without re-litigating typings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pre-flight + statement-builder unit tests (red)</name>
  <files>
    scripts/__tests__/migrate-v1.3-binder.test.ts,
    scripts/migrate-v1.3-binder.ts
  </files>
  <read_first>
    src/db/__tests__/schema.test.ts,
    src/db/__tests__/replace-all-cards.test.ts,
    .planning/research/PITFALLS.md,
    .planning/phases/16-schema-migration/16-CONTEXT.md
  </read_first>
  <behavior>
    - Tests fail because the helpers below don't exist yet.
    - Tests cover `runPreflights({ db })`, `buildBatchStatements()`, and `formatSummary(snapshot)` with the DB layer mocked at the `db.execute` boundary (vitest `vi.fn()` doubles returning canned `{ rows: [...] }` shapes).
    - No live DB connection in the test file.
    - Pre-flights tested (D-04, Pitfall 4):
      a. `runPreflights` throws/exits non-zero when any `cards.id` already ends in `-unsorted` (lists offending ids in the message).
      b. `runPreflights` throws/exits non-zero when `information_schema.columns` already shows `cards.binder`.
      c. `runPreflights` returns a `snapshot` containing `orderItemsCardIdDistinctCount` (used by the post-DML summary diff).
      d. `runPreflights` returns the same `snapshot` shape on a clean DB (all preflights green).
    - `buildBatchStatements()` returns the statements in the exact order documented in 16-CONTEXT.md `<specifics>`:
      1. `ALTER TABLE cards ADD COLUMN binder text NOT NULL DEFAULT 'unsorted'`
      2. Create `pgEnum('finish', ['normal','foil','etched'])` (`CREATE TYPE finish AS ENUM (...)`)
      3. `ALTER TABLE cards ADD COLUMN finish finish` (nullable until backfill)
      4. Backfill `UPDATE cards SET finish = CASE WHEN foil THEN 'foil'::finish ELSE 'normal'::finish END`
      5. `ALTER TABLE cards ALTER COLUMN finish SET NOT NULL`
      6. `ALTER TABLE cards DROP COLUMN foil`
      7. `ALTER TABLE cards DROP CONSTRAINT cards_pkey`
      8. `UPDATE cards SET id = set_code || '-' || collector_number || '-' || finish::text || '-' || condition || '-' || binder` (5-segment composite, D-05)
      9. `ALTER TABLE cards ADD CONSTRAINT cards_pkey PRIMARY KEY (id)`
      10. `ALTER TABLE cards ADD CONSTRAINT cards_quantity_check CHECK (quantity >= 0)` (D-08, BIND-04)
      11. `ALTER TABLE order_items ADD COLUMN binder text NOT NULL DEFAULT 'unsorted'` (D-09, BIND-03)
    - `formatSummary(snapshot)` produces text matching the D-14 template (asserted by snapshot regex / line presence: header, schema-changes block, data-migration block, constraints block, sample-ids block, pre-flights block, "Next: deploy v1.3 application code to Vercel.").
  </behavior>
  <action>
    Write the failing test file first. Stub the public surface in `scripts/migrate-v1.3-binder.ts` (function signatures with `throw new Error('not implemented')` bodies) only enough to make the test imports resolve and the assertions reach `expect(...).toThrow()`.

    Mock `db.execute` and `db.batch` via vitest doubles; do NOT import the real `@/db/client` (the script's `main()` will, but the helpers under test must accept a passed-in `db` so they're testable in isolation).
  </action>
  <verify>
    <automated>npx vitest run scripts/__tests__/migrate-v1.3-binder.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All new tests fail with the expected "not implemented" / missing-export errors before Task 3.
    - Tests cover at least the four pre-flight branches, the 11-statement ordering, and the summary template structure.
  </acceptance_criteria>
  <done>Red tests define the migration's pre-flight + statement-ordering + summary contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement migration script + drive tests green</name>
  <files>
    scripts/migrate-v1.3-binder.ts,
    scripts/__tests__/migrate-v1.3-binder.test.ts,
    package.json
  </files>
  <read_first>
    src/db/queries.ts,
    src/db/client.ts,
    src/db/schema.ts,
    .planning/phases/16-schema-migration/16-CONTEXT.md,
    .planning/research/ARCHITECTURE.md
  </read_first>
  <behavior>
    - `scripts/migrate-v1.3-binder.ts` is a `tsx`-runnable, ESM-friendly script (mirrors `scripts/smoke-production.ts` shape).
    - Top-of-file header comment documents (in this order):
      1. Purpose ("v1.3 Schema & Migration — binder + finish enum + CHECK + 5-segment id").
      2. Manual-only execution model (D-11, D-12); links 16-CONTEXT.md.
      3. The exact 11-step batch ordering from 16-CONTEXT.md `<specifics>`.
      4. Three pre-flight assertions (D-04, Pitfall 4) and exit-non-zero contract.
      5. Neon point-in-time-restore rollback recipe (D-17): branch from prod at timestamp before run → verify → swap branch into the prod compute (or restore in place via `pg_restore` from PITR snapshot, per Neon docs). Link the Neon PITR doc.
      6. Apply-path note (recorded by Task 1: per-statement `db.batch` vs single multi-statement `db.execute`).
    - `--dry-run` flag (D-13): runs the pre-flights, builds the statements, prints a "DRY RUN — no DML executed" banner + the exact statement list it WOULD send + the summary template populated with the SELECT-only counts. Hits the DB ONLY for read-side pre-flights / snapshot queries. Never executes DDL or DML in dry-run mode.
    - Live mode (no flag): pre-flights → apply the batch → post-DML measurement queries (rowcount-after, finish backfill counts, constraint presence via `pg_constraint`, 5 random sample ids via `SELECT id FROM cards ORDER BY random() LIMIT 5`) → print the structured summary (D-14) → exit 0.
    - On ANY pre-flight failure: print the offending data, print "FAIL — zero changes applied", exit non-zero. No partial state left behind (pre-flights are read-only by definition).
    - On apply failure mid-batch: Neon's HTTP transaction endpoint rolls the batch back; the script catches, prints the error + a pointer to the Neon PITR rollback recipe in the header, exits non-zero.
    - Help (`--help` / `-h`): prints usage including the dry-run gate, the manual workflow (D-11), and a one-line warning that this script writes to `DATABASE_URL`. No secret values are printed.
    - `package.json` gains:
      - `"migrate:v1.3": "tsx scripts/migrate-v1.3-binder.ts"`
      - `"migrate:v1.3:dry-run": "tsx scripts/migrate-v1.3-binder.ts --dry-run"`
    - Tests from Task 2 turn green.
  </behavior>
  <action>
    Implement `runPreflights`, `buildBatchStatements`, the apply-path code (per Task 1's choice), the post-DML measurement queries, `formatSummary`, the `--dry-run` and `--help` flag handling, and `main()` orchestration. Wire `npm run migrate:v1.3` and `npm run migrate:v1.3:dry-run` into `package.json` scripts.

    Document the choice from Task 1 inline in the header comment ("Apply path: A (per-statement db.batch)" or "Apply path: B (multi-statement BEGIN; ...; COMMIT;)").

    Do NOT add a down-migration helper. Per D-16/D-17, rollback is Neon PITR. The header comment is the contract; a stub `down()` would be misleading because the data rewrite (foil drop, id rewrite) is not symmetrically reversible.
  </action>
  <verify>
    <automated>npx vitest run scripts/__tests__/migrate-v1.3-binder.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All Task 2 tests pass.
    - `npx tsc --noEmit` is green.
    - `package.json` has both new scripts.
    - The script header comment includes the rollback recipe and the apply-path decision.
  </acceptance_criteria>
  <done>Migration script + dry-run + scripts wired; unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Update src/db/schema.ts + extend schema invariant tests</name>
  <files>
    src/db/schema.ts,
    src/db/__tests__/schema.test.ts
  </files>
  <read_first>
    src/db/schema.ts,
    src/db/__tests__/schema.test.ts,
    .planning/phases/16-schema-migration/16-CONTEXT.md
  </read_first>
  <behavior>
    - `src/db/schema.ts` is rewritten so that `drizzle-kit push` against the post-migration database is a no-op (i.e., the Drizzle schema matches the SQL state the script lands).
    - Concrete edits:
      - Add `export const finishEnum = pgEnum("finish", ["normal", "foil", "etched"])` adjacent to `orderStatusEnum` (mirroring the precedent at `src/db/schema.ts:15`, D-07).
      - On the `cards` table:
        - Add `binder: text("binder").notNull().default("unsorted")` (BIND-01, D-06).
        - Add `finish: finishEnum("finish").notNull()` (FIN-01).
        - Remove the `foil: boolean(...)` column (D-07; foil is dropped after backfill).
        - Add a third entry to the table-builder array for the CHECK constraint, using Drizzle's `check()` helper or `sql\`CHECK (quantity >= 0)\`` block-level constraint syntax appropriate to drizzle-orm@0.45.2 (verify the helper name during implementation; PITFALLS Pitfall 1 + BIND-04, D-08).
      - On the `order_items` table:
        - Add `binder: text("binder").notNull().default("unsorted")` (BIND-03, D-09).
      - Update the column-count assertion comment in any places that hard-code "16 required card columns" — the new count is 17 (binder added; foil removed; finish added; net +1).
    - `src/db/__tests__/schema.test.ts` extensions (mechanical pins, D-15-equivalent at the schema-shape layer):
      - `cards.binder` column exists with `notNull = true`.
      - `cards.finish` column exists with `notNull = true`; `finishEnum.enumValues` deep-equals `["normal", "foil", "etched"]`.
      - `cards.foil` column is undefined (removed).
      - The cards "required columns" list updates to include `binder`, `finish` and excludes `foil`; the column-count assertion updates to 17.
      - `orderItems.binder` column exists with `notNull = true`.
      - The CHECK constraint pin: assert the exported table-builder entries include the `cards_quantity_check` definition (or the equivalent symbolic form Drizzle exposes; if 0.45.2 doesn't surface a structural API for raw CHECKs, document the gap in the SUMMARY and rely on the Task 5 in-DB verification instead).
  </behavior>
  <action>
    Update `src/db/schema.ts` to mirror the post-migration shape. Extend the existing `schema.test.ts` describe blocks; do not duplicate test files. If Drizzle's `pgTable` second-arg builder in 0.45.2 doesn't expose `check()` as a first-class helper, use `sql\`CHECK (quantity >= 0)\`` inside the array per the documented escape-hatch pattern; if even that is not surfaced, prefer correctness in the SQL migration (Task 3) and pin the constraint via the Task 5 live-DB query verification.

    Keep the `replaceAllCards` `db.batch` shape and other queries unchanged in this phase — Phase 19 will rename it to `replaceCardsForBinders`. This phase only ensures the schema compiles + the field references work. If `cardToRow` in `src/db/seed.ts` references `foil` directly, update it minimally (`foil` → `finish`) so `tsc --noEmit` stays green; do NOT redesign the parser's finish handling here (that lives in Phase 17).
  </action>
  <verify>
    <automated>npx vitest run src/db/__tests__/schema.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Schema invariant tests pass.
    - `npx tsc --noEmit` is green across the whole repo.
    - The `cards` column count assertion (if present) is updated to the new total.
    - The CHECK constraint is either pinned by a schema-shape test OR explicitly deferred to Task 5's live-DB verification (with a one-line note in 16-01-SUMMARY.md).
  </acceptance_criteria>
  <done>Drizzle schema matches the migrated database shape; tests pin the new invariants.</done>
</task>

<task type="auto">
  <name>Task 5: Full repo gate + dry-run rehearsal on a Neon branch</name>
  <files>
    scripts/migrate-v1.3-binder.ts
  </files>
  <read_first>
    .planning/phases/16-schema-migration/16-CONTEXT.md,
    .planning/research/PITFALLS.md
  </read_first>
  <behavior>
    - Whole-repo verification is green.
    - Operator (Claude executing the phase) walks through the dry-run flow against a Neon branch and captures the terminal output for inclusion in 16-01-SUMMARY.md.
    - Live production migration is NOT run in this phase — that step belongs to the operator's manual cutover (D-11 step 4) gated by the operator confirming the dry-run output (D-11 step 3, step 6).
  </behavior>
  <action>
    1. Run the full repo verification suite (typecheck, vitest, build).
    2. Create or reuse a Neon branch (operator action; if the executor has the Neon CLI / dashboard available, document the branch name; otherwise, confirm with the operator before proceeding and capture the branch identifier in 16-01-SUMMARY.md).
    3. Run `DATABASE_URL=&lt;branch-url&gt; npm run migrate:v1.3:dry-run` and capture the printed pre-flight outcomes + the would-be-executed statement list + the dry-run summary into the SUMMARY.
    4. (Optional, gated on operator confirmation) Run `DATABASE_URL=&lt;branch-url&gt; npm run migrate:v1.3` against the BRANCH (not prod). Capture the live summary template output + verify:
       - `cards` rowcount before == after.
       - Every sample id has 5 segments and ends in `-unsorted`.
       - finish counts: `(normal + foil + etched) == cards.count`; `etched` should be 0 on the v1.2 baseline (the latent etched bug means v1.2 has zero etched rows; Phase 17 starts emitting them).
       - `cards_quantity_check` constraint is `PRESENT` per `pg_constraint`.
       - `order_items` rowcount unchanged; every row has `binder = 'unsorted'`.
       - `SELECT COUNT(*) FROM cards WHERE id NOT LIKE '%-unsorted'` returns 0.
       - `SELECT COUNT(*) FROM order_items oi LEFT JOIN cards c ON oi.card_id = c.id WHERE c.id IS NULL` count matches the pre-migration count (zero new mismatches; per Pitfall 4 detection criterion).
    5. Re-run `npm run migrate:v1.3` against the SAME branch a second time and confirm the script exits non-zero with the "binder column already exists" / "rows already have -unsorted suffix" pre-flight rejection (idempotency proof).
    6. Discard the branch when finished (operator action).

    If a Neon branch isn't available to the executor, stop at step 1, document the gating in 16-01-SUMMARY.md under "Manual Step Required Before Production Cutover", and hand the rehearsal back to the operator with a runbook.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Full repo verification (tsc + vitest + build) is green.
    - Either the Neon-branch dry-run terminal output is captured in 16-01-SUMMARY.md, OR the SUMMARY explicitly hands the rehearsal back to the operator with a runbook (acceptable when the executor lacks Neon CLI access).
    - The idempotency proof (second-run rejection) is captured in the SUMMARY if step 4 was performed.
  </acceptance_criteria>
  <done>Repo is green; the migration is rehearsed (or runbook handed off); production cutover is left to the operator's explicit step (D-11).</done>
</task>

<task type="checkpoint:verify" gate="blocking">
  <name>Task 6: Verify pre-flight, idempotency, and summary contract</name>
  <files>scripts/migrate-v1.3-binder.ts</files>
  <action>
    Verification must include (where the Neon branch is reachable):
    1. Pre-flight rejection: against an already-migrated DB (the branch from Task 5), `npm run migrate:v1.3` exits non-zero before any DML.
    2. Pre-flight rejection #2: against a DB seeded with a row whose id already ends in `-unsorted`, the script exits non-zero with that id in the message.
    3. Dry-run: against a clean (pre-v1.3) Neon branch, `npm run migrate:v1.3:dry-run` prints the would-be statements + a populated dry-run summary and writes nothing.
    4. Live-on-branch: against a fresh clean branch, `npm run migrate:v1.3` produces the structured summary matching the D-14 template, and the post-state spot checks (sample ids 5-segment + ending in `-unsorted`, finish counts sum to total, CHECK present, order_items.binder populated, zero new order_items.cardId mismatches vs pre-migration) all pass.
    5. The rollback recipe in the header comment is verified to be syntactically actionable (operator can read it and execute it without follow-up clarification).
    6. No secrets in captured output: the SUMMARY redacts the branch DATABASE_URL and any token in the Neon CLI invocation.

    If the Neon branch is not reachable from the executor's environment: the executor MUST stop and explicitly hand off to the operator with the runbook (steps 1-5 above) embedded in 16-01-SUMMARY.md. Do not invent fake captured output.
  </action>
  <done>The migration's pre-flights, dry-run, idempotency, and summary contract are verified — either by the executor against a Neon branch, or by an explicit handoff runbook for the operator.</done>
</task>

</tasks>

<verification>
- `git diff --check`
- `npx vitest run scripts/__tests__/migrate-v1.3-binder.test.ts`
- `npx vitest run src/db/__tests__/schema.test.ts`
- `npx tsc --noEmit`
- `npm test`
- `npm run build`
- Neon-branch rehearsal: `DATABASE_URL=<branch-url> npm run migrate:v1.3:dry-run` (terminal output captured in 16-01-SUMMARY.md), and (where possible) live-on-branch run + idempotency rerun
- Production cutover (D-11 step 4) is the operator's explicit follow-up; this phase ships the script + the verified rehearsal, not the prod migration itself
</verification>

<success_criteria>
- [ ] BIND-01: cards composite ID includes a binder dimension; every existing row has a 5-segment id ending in `-unsorted` after migration
- [ ] BIND-02: every existing cards row carries `binder = 'unsorted'`; storefront, cart, and checkout still load and operate against the migrated rows
- [ ] BIND-03: `order_items.binder text NOT NULL DEFAULT 'unsorted'` exists and is populated for both historical rows (default) and any new row written after migration
- [ ] BIND-04: `cards.quantity >= 0` CHECK constraint is present in the database; manual attempt to drive a row negative rejects with a constraint-violation error
- [ ] FIN-01: `cards.finish` is the 3-value enum (`normal` / `foil` / `etched`) backfilled from the legacy `foil` boolean (`true → foil`, `false → normal`) with zero data loss; the legacy `foil` column is dropped
- [ ] The migration script refuses to run a second time against an already-migrated database (idempotency pre-flights catch repeat runs before any DML)
- [ ] The Neon-branch dry-run produces zero new `order_items.cardId` mismatches relative to the pre-migration count
- [ ] No down migration is added; the rollback recipe is documented in the script header (Neon PITR)
- [ ] The terminal summary printed at the end of the script matches the D-14 template
</success_criteria>

<output>
After completion, create `.planning/phases/16-schema-migration/16-01-SUMMARY.md` with:
- The Task 1 apply-path decision (Path A per-statement `db.batch` vs Path B multi-statement raw `db.execute`) and the typing evidence that drove it
- The final 11-statement batch ordering as actually shipped, with any divergence from the 16-CONTEXT.md `<specifics>` ordering called out + justified
- The pre-flight assertion details (exact SQL, exit conditions, error message wording)
- The verification evidence: tsc/vitest/build green, Neon-branch dry-run terminal output (or the operator handoff runbook if the branch wasn't reachable from the executor), idempotency rerun output (if performed)
- The Neon point-in-time-restore rollback recipe (final wording as embedded in the script header)
- Known limitations / deferred items (e.g., schema-version indicator on /admin/health is deferred to v1.4 per CONTEXT D-15; smoke-script schema check is deferred to Phase 22 per CONTEXT D-15)
- The cutover handoff note: "Production cutover (D-11 step 4) is the operator's explicit step; run `npm run migrate:v1.3:dry-run` against a fresh Neon branch first, eyeball the summary, then run `npm run migrate:v1.3` against the production DATABASE_URL, eyeball the live summary, then deploy v1.3 application code to Vercel."
</output>
