---
phase: 8
slug: authentication
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-12
---

# Phase 8 — UI Design Contract

> Visual and interaction contract for the authentication phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (hand-built Tailwind components) |
| Icon library | none (inline SVGs) |
| Font | Geist (sans) + Geist Mono, loaded via `next/font/google` |

Source: Codebase scan -- no `components.json`, no shadcn. All existing components use Tailwind utility classes directly.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, badge vertical padding |
| sm | 8px | Compact element spacing, input padding-y, icon-to-text gaps |
| sm+ | 12px | Compact bar padding-y (header `py-3`), error alert inner padding |
| md | 16px | Default element spacing, container px, button-to-link gaps |
| md+ | 20px | Button horizontal padding (`px-5`), matching existing CTA pattern |
| lg | 24px | Section padding, form group gaps, content area top padding |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level vertical centering offsets |

Exceptions: none. The `sm+` (12px) and `md+` (20px) tokens are non-standard 8-point values but are multiples of 4 and match established codebase patterns (`py-3` on headers, `px-5` on buttons throughout checkout and confirmation pages).

Source: Derived from existing codebase patterns (`px-4` = 16px, `py-3` = 12px used for compact bars, `py-2` = 8px for inputs, `mb-6` = 24px for section gaps, `px-5` on all CTA buttons).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Caption | 12px (`text-xs`) | 400 (regular) | 1.5 |
| Body | 14px (`text-sm`) | 400 (regular) | 1.5 |
| Heading | 20px (`text-xl`) | 700 (bold) | 1.2 |

Font weights used: 400 (regular) and 700 (bold). Only two weights.

Role usage:
- **Caption** (12px regular): Admin badge text, storefront footer "Admin" link
- **Body** (14px regular): Paragraph text, access notice, error messages, link text, form labels. Labels are distinguished by context (above inputs) not by weight.
- **Heading** (20px bold): Page headings ("Access Denied", "Welcome, {name}"), store name display ("Viki MTG Bulk Store" on login page, "Viki" in admin header)

Note: The existing storefront header uses `text-lg font-bold` (18px) for the store name and `text-xl font-semibold` (20px) for page headings. This phase unifies both under Heading (20px bold) for the admin-specific pages. The storefront header itself is not modified, so the existing 18px store name remains untouched.

Source: Existing storefront uses `text-sm` for body/labels, `text-xl font-semibold` for page headings. Collapsed to 2 weights per design contract rules.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#ffffff` light / `#0a0a0a` dark | Page background, login page background, admin content area |
| Secondary (30%) | `zinc-50` (`#fafafa`) light / `zinc-900` (`#18181b`) dark | Admin header background, card surfaces, Access Denied panel |
| Accent (10%) | `#4f46e5` (indigo-600) | "Sign in with Google" button, "Admin" badge background, primary action links |
| Accent hover | `#4338ca` (indigo-700) | Hover state for accent elements |
| Accent light | `#eef2ff` (indigo-50) | Light accent surface (badge background alternative) |
| Destructive | `#dc2626` (red-600) | Error alert text, error border; no destructive actions exist in this phase |

Accent reserved for: "Sign in with Google" button, "Admin" badge pill in admin header, "Back to store" link text on login page, "Try again" link on error state.

Source: `globals.css` defines `--color-accent: #4f46e5`, `--color-accent-hover: #4338ca`, `--color-accent-light: #eef2ff`. Error colors from existing checkout error pattern (`red-50`/`red-600`/`red-200` borders).

---

## Copywriting Contract

### Login Page (`/admin/login`)

| Element | Copy |
|---------|------|
| Page title (browser tab) | `Admin Login -- Viki MTG Bulk Store` |
| Store name display | `Viki` (bold accent) + `MTG Bulk Store` (light zinc-500) |
| Primary CTA | `Sign in with Google` |
| Access notice | `Only authorized admins can access this area.` |
| Back link | `Back to store` |
| OAuth error (D-15) | `Sign-in failed. Try again.` |

### Access Denied Page

| Element | Copy |
|---------|------|
| Heading | `Access Denied` |
| Body | `{email} does not have admin access to this store.` |
| Sign out action | `Sign out` |
| Back link | `Back to store` |

### Admin Placeholder (`/admin`)

| Element | Copy |
|---------|------|
| Page title (browser tab) | `Admin -- Viki MTG Bulk Store` |
| Welcome heading | `Welcome, {firstName}` |
| Placeholder body | `Inventory management coming soon.` |

### Admin Header

| Element | Copy |
|---------|------|
| Store name | `Viki` (bold accent) + `Admin` (badge) |
| Sign out button | `Sign out` |

### Storefront Footer Link (D-11)

| Element | Copy |
|---------|------|
| Link text | `Admin` |

Source: CONTEXT.md decisions D-01, D-02, D-06, D-09, D-11, D-12, D-15.

---

## Page Layouts

### Login Page (`/admin/login`)

Structure: Vertically centered card on full-page background. No header, no footer -- standalone branded page.

```
+------------------------------------------+
|                                          |
|          [Viki MTG Bulk Store]           |  <- store name, text-xl font-bold
|                                          |
|     +----------------------------+       |
|     |                            |       |
|     |  [Google icon] Sign in     |       |  <- accent bg, white text, rounded-md
|     |       with Google          |       |
|     |                            |       |
|     +----------------------------+       |
|                                          |
|     Only authorized admins can           |  <- text-sm, zinc-500
|     access this area.                    |
|                                          |
|     Back to store                        |  <- text-sm, accent text, underline
|                                          |
|     {error toast if OAuth failed}        |  <- red-50 bg, red-600 text, same
|                                          |     pattern as checkout error
+------------------------------------------+
```

- Container: `max-w-sm mx-auto`, centered vertically with `min-h-screen flex items-center justify-center`
- Card area: no visible card border -- just centered content block with `text-center`
- Google button: `w-full px-5 py-3 text-sm font-bold rounded-md bg-accent text-white hover:bg-accent-hover transition-colors`
- Google icon: inline SVG (24x24 Google "G" logo) placed left of button text, 8px gap

### Access Denied Page

Structure: Same centered layout as login page. No header.

```
+------------------------------------------+
|                                          |
|          [Viki MTG Bulk Store]           |
|                                          |
|          Access Denied                   |  <- text-xl font-bold
|                                          |
|     user@example.com does not have       |  <- text-sm, zinc-500
|     admin access to this store.          |     email in font-bold
|                                          |
|     [Sign out]    Back to store          |  <- Sign out: accent bg button
|                                          |     Back to store: text link
+------------------------------------------+
```

- Same container as login: `max-w-sm mx-auto`, vertically centered
- "Sign out" button: `px-5 py-2 text-sm font-bold rounded-md bg-accent text-white`
- "Back to store" link: `text-sm text-accent hover:underline`, 16px gap from button

### Admin Layout (header + content)

Structure: Full-width header bar, content area below. Matches storefront header pattern.

```
+------------------------------------------+
| Viki  [Admin]              [Sign out]    |  <- border-b, same height as storefront
+------------------------------------------+
|                                          |
|  {child route content}                   |  <- max-w-7xl mx-auto px-4 pt-6
|                                          |
+------------------------------------------+
```

- Header: `border-b border-zinc-200 dark:border-zinc-800`, inner `max-w-7xl mx-auto px-4 py-3 flex items-center justify-between`
- "Viki" text: `text-xl font-bold text-accent` (admin header uses Heading size for store name)
- "Admin" badge: `ml-2 text-xs font-bold px-2 py-1 rounded-full bg-accent-light text-accent` (indigo-50 bg, indigo-600 text)
- "Sign out" button: `text-sm text-zinc-500 hover:text-zinc-700 dark:hover:text-zinc-300` -- text-only button, no background
- Content area: `max-w-7xl mx-auto px-4 pt-6`

### Admin Placeholder (`/admin`)

Structure: Simple centered content within admin layout.

```
+------------------------------------------+
| [Admin Header]                           |
+------------------------------------------+
|                                          |
|  Welcome, Wiko                           |  <- text-xl font-bold
|                                          |
|  Inventory management coming soon.       |  <- text-sm, zinc-500
|                                          |
+------------------------------------------+
```

- Heading: `text-xl font-bold` (matches Heading role)
- Body: `text-sm text-zinc-500 dark:text-zinc-400 mt-2`

### Storefront Footer Addition

No footer component exists yet. Add a minimal footer to the storefront.

```
+------------------------------------------+
| [existing storefront content]            |
+------------------------------------------+
|                                   Admin  |  <- right-aligned, very subtle
+------------------------------------------+
```

- Footer: `border-t border-zinc-200 dark:border-zinc-800 mt-auto`
- Inner: `max-w-7xl mx-auto px-4 py-4 flex justify-end`
- "Admin" link: `text-xs text-zinc-400 dark:text-zinc-600 hover:text-zinc-600 dark:hover:text-zinc-400` -- deliberately subtle per D-11

---

## Interaction States

### "Sign in with Google" Button

| State | Visual |
|-------|--------|
| Default | `bg-accent text-white` |
| Hover | `bg-accent-hover text-white` |
| Active/Pressed | `bg-accent-hover opacity-90` |
| Loading (after click) | Button text changes to `Signing in...`, `opacity-70 cursor-not-allowed`, disabled |

### "Sign out" Button (Admin Header)

| State | Visual |
|-------|--------|
| Default | `text-zinc-500` text button |
| Hover | `text-zinc-700 dark:text-zinc-300` |

No confirmation dialog for sign-out -- immediate action (single admin, low risk).

### OAuth Error State

Displayed on the login page when redirected back with an error parameter.

| State | Visual |
|-------|--------|
| Error visible | `bg-red-50 dark:bg-red-950/20 border border-red-200 dark:border-red-800 rounded-md p-3 text-sm text-red-600 dark:text-red-400 mt-4` |
| Error copy | `Sign-in failed. Try again.` |

Source: Matches the existing checkout error pattern in `checkout-client.tsx`.

### Page Load / Redirect States

| Scenario | Behavior |
|----------|----------|
| Unauthenticated visits `/admin` | Server-side redirect to `/admin/login` |
| Unauthenticated visits `/admin/login` | Render login page |
| Authenticated non-admin visits `/admin` | Render Access Denied page |
| Authenticated admin visits `/admin/login` | Redirect to `/admin` |
| Authenticated admin visits `/admin` | Render admin placeholder |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| Third-party | none | not applicable |

No component library or registry is used in this project. All components are hand-built with Tailwind CSS utility classes. This phase introduces no new registry dependencies.

---

## Accessibility Notes

- "Sign in with Google" button must have visible focus ring: `focus:ring-2 focus:ring-accent/50 focus:ring-offset-2` (matches existing input focus pattern)
- "Back to store" links must be proper `<a>` / Next.js `<Link>` elements, not buttons styled as links
- Access Denied page must show the email as text (not just in an error message) so screen readers announce which account was used
- Admin header "Sign out" must be a `<button>` element (triggers an action, not navigation)
- OAuth error message must use `role="alert"` for live-region announcement (matches checkout error pattern)
- Login page title must include "Login" for screen reader context when landing on the page

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
