---
phase: 11-checkout-upgrade-order-history
plan: 02
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - src/db/orders.ts
  - src/db/__tests__/orders-admin.test.ts
  - src/app/api/admin/orders/route.ts
  - src/app/api/admin/orders/[id]/route.ts
  - src/app/api/admin/orders/__tests__/route.test.ts
  - src/app/admin/layout.tsx
  - src/app/admin/orders/page.tsx
  - src/app/admin/orders/[id]/page.tsx
  - src/app/admin/orders/_components/orders-table.tsx
  - src/app/admin/orders/_components/order-detail.tsx
autonomous: true
requirements:
  - ORD-02
  - ORD-03
must_haves:
  truths:
    - "Admin can view a paginated table of past orders sorted newest first"
    - "Order table shows order ref, buyer name/email, date, item count, total, and status"
    - "Admin can click an order row/link to view full order detail"
    - "Order detail shows buyer contact, optional message, line item snapshots, quantities, prices, and total"
    - "Order history survives inventory re-import because detail reads order_items snapshots, not current cards"
    - "Admin order APIs/pages require admin auth and return/redirect consistently with existing admin conventions"
  artifacts:
    - path: "src/db/orders.ts"
      provides: "Admin order listing and detail query helpers"
      exports: ["getAdminOrders", "getOrderById"]
    - path: "src/app/api/admin/orders/route.ts"
      provides: "Authenticated paginated order list endpoint"
      contains: "requireAdmin"
    - path: "src/app/api/admin/orders/[id]/route.ts"
      provides: "Authenticated order detail endpoint"
      contains: "requireAdmin"
    - path: "src/app/admin/orders/page.tsx"
      provides: "Admin order history page"
      contains: "Orders"
    - path: "src/app/admin/orders/[id]/page.tsx"
      provides: "Admin order detail page"
      contains: "Order"
    - path: "src/app/admin/layout.tsx"
      provides: "Admin navigation links for Inventory and Orders"
      contains: "Orders"
---

<objective>
Build the seller-facing order history surfaces: list all past orders and inspect any order's line-item snapshot. This completes ORD-02 and ORD-03 on top of the transactional checkout foundation from 11-01.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-checkout-upgrade-order-history/11-CONTEXT.md
@.planning/phases/11-checkout-upgrade-order-history/11-01-SUMMARY.md
</execution_context>

<context>
@src/db/orders.ts
@src/db/schema.ts
@src/lib/types.ts
@src/lib/auth/admin-check.ts
@src/lib/auth/helpers.ts
@src/app/admin/layout.tsx
@src/app/admin/page.tsx
@src/app/admin/_components/pagination.tsx
@src/app/admin/_components/toast.tsx
@src/app/api/admin/cards/route.ts
@src/app/api/admin/cards/[id]/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add admin order query and API tests</name>
  <files>
    src/db/__tests__/orders-admin.test.ts,
    src/app/api/admin/orders/__tests__/route.test.ts
  </files>
  <read_first>
    src/db/orders.ts,
    src/db/schema.ts,
    src/app/api/admin/cards/__tests__/route.test.ts,
    src/lib/auth/admin-check.ts
  </read_first>
  <behavior>
    - `getAdminOrders({ page, limit })` returns orders newest-first with total/page/limit/totalPages.
    - `getAdminOrders` caps limit at 100 and defaults to 25.
    - `getOrderById(id)` returns an order plus item snapshots ordered by insertion/id.
    - `GET /api/admin/orders` returns 401/403/200 consistently with existing admin routes.
    - `GET /api/admin/orders?limit=500` caps to 100.
    - `GET /api/admin/orders/[id]` returns 404 for missing order and 200 for found order.
  </behavior>
  <action>
    Write failing tests first. Mock DB calls for query helper tests and mock `requireAdmin` + order helpers for route tests. Do not hit a real database.
  </action>
  <verify>
    <automated>npx vitest run src/db/__tests__/orders-admin.test.ts src/app/api/admin/orders/__tests__/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests exist and fail before implementation.
    - Tests cover auth, pagination cap, missing order 404, and detail shape.
  </acceptance_criteria>
  <done>Red tests define admin order history contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement admin order query helpers and API routes</name>
  <files>
    src/db/orders.ts,
    src/app/api/admin/orders/route.ts,
    src/app/api/admin/orders/[id]/route.ts
  </files>
  <read_first>
    src/db/orders.ts,
    src/db/schema.ts,
    src/db/queries.ts,
    src/lib/auth/admin-check.ts,
    src/app/api/admin/cards/route.ts,
    src/app/api/admin/cards/[id]/route.ts,
    src/db/__tests__/orders-admin.test.ts,
    src/app/api/admin/orders/__tests__/route.test.ts
  </read_first>
  <behavior>
    - Query helpers return prices in dollars for API/UI consumers, preserving DB cents internally.
    - Order detail maps `order_items.imageUrl` into `OrderData.items[].imageUrl`.
    - Admin endpoints use `requireAdmin()` and existing 401/403 JSON conventions.
    - List endpoint is paginated and sorted newest first.
  </behavior>
  <action>
    Extend `src/db/orders.ts` with:
    ```typescript
    export interface AdminOrdersParams { page?: number; limit?: number; }
    export type OrderStatus = "pending" | "confirmed" | "completed";
    export interface AdminOrderSummary {
      id: string;
      buyerName: string;
      buyerEmail: string;
      totalItems: number;
      totalPrice: number;
      status: OrderStatus;
      createdAt: string;
    }
    export interface AdminOrdersResult { orders: AdminOrderSummary[]; total: number; page: number; limit: number; totalPages: number; }
    export async function getAdminOrders(params?: AdminOrdersParams): Promise<AdminOrdersResult>;
    export async function getOrderById(id: string): Promise<(OrderData & { status: OrderStatus }) | null>;
    ```

    Add API routes:
    - `GET /api/admin/orders`
    - `GET /api/admin/orders/[id]`
  </action>
  <verify>
    <automated>npx vitest run src/db/__tests__/orders-admin.test.ts src/app/api/admin/orders/__tests__/route.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/orders.ts` exports `getAdminOrders` and `getOrderById`.
    - `src/app/api/admin/orders/route.ts` imports `requireAdmin`.
    - `src/app/api/admin/orders/[id]/route.ts` returns 404 for missing order.
    - Focused tests and TypeScript pass.
  </acceptance_criteria>
  <done>Admin order APIs are implemented and covered.</done>
</task>

<task type="auto">
  <name>Task 3: Build admin orders list/detail UI and navigation</name>
  <files>
    src/app/admin/layout.tsx,
    src/app/admin/orders/page.tsx,
    src/app/admin/orders/[id]/page.tsx,
    src/app/admin/orders/_components/orders-table.tsx,
    src/app/admin/orders/_components/order-detail.tsx
  </files>
  <read_first>
    src/app/admin/layout.tsx,
    src/app/admin/page.tsx,
    src/app/admin/_components/pagination.tsx,
    src/app/admin/_components/inventory-table.tsx,
    src/db/orders.ts,
    src/lib/types.ts
  </read_first>
  <behavior>
    - Admin header shows Inventory and Orders nav links plus View store and Sign out.
    - `/admin/orders` renders order table with empty and error states as appropriate.
    - Each order row has a semantic link to `/admin/orders/[id]`.
    - `/admin/orders/[id]` renders order detail from snapshots, not current card data.
    - Detail page has a back link to Orders.
    - Prices and dates are formatted consistently and accessibly.
  </behavior>
  <action>
    Prefer server components for pages and small presentational components for table/detail. Since order history is read-only in Phase 11, avoid unnecessary client state. Use the page `searchParams` for pagination instead of client-side fetching unless execution proves that pattern awkward.

    Each admin orders page must perform the same defense-in-depth server check as `src/app/admin/page.tsx`: call `auth()`, redirect unauthenticated users to `/admin/login`, and redirect non-admin users to `/admin/access-denied`. Do not rely only on `proxy.ts` or the admin layout.

    Suggested UI:
    - Orders page heading: `Orders`
    - Table columns: Order, Buyer, Date, Items, Total, Status
    - Empty state: `No orders yet`
    - Detail sections: Buyer, Message, Items, Total
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/admin/layout.tsx` contains links to `/admin` and `/admin/orders`.
    - `src/app/admin/orders/page.tsx` exists and performs a direct server-side admin auth check with `auth()` and `isAdminEmail()` before rendering orders.
    - `src/app/admin/orders/[id]/page.tsx` exists, performs the same direct admin auth check, and renders not-found/404 behavior for missing order.
    - UI uses order item snapshots from `getOrderById`, not `getCardById` or current cards.
    - TypeScript, tests, and build pass.
  </acceptance_criteria>
  <done>Admin can browse order history and inspect order details.</done>
</task>

<task type="checkpoint:verify" gate="blocking">
  <name>Task 4: Browser verify order history</name>
  <files>src/app/admin/orders/page.tsx</files>
  <action>
    Verification must include:
    1. Place a checkout order in local/dev using the Phase 11 checkout path.
    2. Sign into admin locally.
    3. Visit `/admin/orders` and confirm the new order appears with buyer/date/total.
    4. Open the order detail and confirm item names, set codes, quantities, prices, buyer email, and optional message match checkout.
    5. Confirm `/admin` inventory quantity reflects the stock decrement.
    6. Confirm browser console and network logs have no unexpected errors.
  </action>
  <done>Order history is verified end-to-end in the browser.</done>
</task>

</tasks>

<verification>
- `git diff --check`
- `npx tsc --noEmit`
- `npm test`
- `npm run build`
- Browser verification of checkout -> admin orders list -> detail -> inventory quantity decrement
</verification>

<success_criteria>
- [ ] ORD-02: Admin order table shows past orders with buyer/date/total
- [ ] ORD-03: Admin order detail shows full line items and buyer info
- [ ] Order history reads snapshots and survives inventory changes
- [ ] Admin auth gates order APIs/pages
- [ ] Navigation exposes Orders from admin shell
</success_criteria>

<output>
After completion, create `.planning/phases/11-checkout-upgrade-order-history/11-02-SUMMARY.md` with:
- final endpoint/query contracts
- UI routes/screenshots or browser verification notes
- verification commands
- known limitations/deferred order status work
</output>
