---
phase: 14-inventory-audit-trail
plan: 01
type: execute
wave: 1
depends_on: ["13-02"]
files_modified:
  - src/db/schema.ts
  - src/db/queries.ts
  - src/db/orders.ts
  - src/db/__tests__/admin-audit.test.ts
  - src/app/api/admin/cards/route.ts
  - src/app/api/admin/cards/[id]/route.ts
  - src/app/api/admin/cards/bulk-delete/route.ts
  - src/app/api/admin/import/commit/route.ts
  - src/app/api/admin/orders/[id]/route.ts
  - src/app/api/admin/orders/[id]/cancel/route.ts
autonomous: true
requirements:
  - AUD-01
  - AUD-04
must_haves:
  truths:
    - "Successful high-impact admin mutations create audit records"
    - "Failed mutations do not create misleading success audit records"
    - "Audit entries include actor email, action, target type, target id/count, timestamp, and safe metadata"
    - "Audit metadata never stores secrets or raw CSV file contents"
    - "Inventory mutation audit entries are written through centralized helpers where practical"
    - "Order status/cancel/restore mutations are auditable after Phase 13"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Admin audit log table"
      contains: "admin_audit_log"
    - path: "src/db/queries.ts"
      provides: "Inventory mutations with audit integration"
      exports: ["createAdminAuditEntry", "getAdminAuditEntries"]
    - path: "src/db/orders.ts"
      provides: "Order workflow audit integration"
      contains: "createAdminAuditEntry"
    - path: "src/db/__tests__/admin-audit.test.ts"
      provides: "Audit helper and mutation coverage"
      contains: "inventory.delete_many"
---

<objective>
Add the durable audit foundation and wire it into high-impact admin mutations so destructive actions are explainable later.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-inventory-audit-trail/14-CONTEXT.md
@.planning/phases/13-admin-order-workflow/13-02-SUMMARY.md
@.planning/phases/12-bulk-operations-dashboard/12-02-SUMMARY.md
</execution_context>

<context>
@src/db/schema.ts
@src/db/queries.ts
@src/db/orders.ts
@src/app/api/admin/cards/route.ts
@src/app/api/admin/cards/[id]/route.ts
@src/app/api/admin/cards/bulk-delete/route.ts
@src/app/api/admin/import/commit/route.ts
@src/app/api/admin/orders/[id]/route.ts
@src/app/api/admin/orders/[id]/cancel/route.ts
@src/lib/auth/admin-check.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add audit schema/helper tests</name>
  <files>
    src/db/__tests__/admin-audit.test.ts
  </files>
  <read_first>
    src/db/schema.ts,
    src/db/queries.ts,
    src/db/orders.ts,
    src/db/__tests__/bulk-delete-cards.test.ts,
    src/db/__tests__/orders-admin.test.ts
  </read_first>
  <behavior>
    - `createAdminAuditEntry` stores safe metadata with action/actor/target fields.
    - `getAdminAuditEntries({ page, limit, action, targetType })` returns newest-first paginated entries.
    - Inventory edit/delete/delete-all/bulk delete helpers can accept audit context without breaking existing call sites.
    - Order workflow/cancel helpers can accept audit context without exposing notes/secrets incorrectly.
  </behavior>
  <action>
    Write failing tests first. Mock DB calls at the query-helper layer. Include metadata sanitization coverage: no secrets, no raw CSV content, no unbounded payloads.
  </action>
  <verify>
    <automated>npx vitest run src/db/__tests__/admin-audit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because audit helpers/schema are missing.
    - Tests cover create/list and at least one inventory mutation audit path.
  </acceptance_criteria>
  <done>Red tests define audit log contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement audit table and helpers</name>
  <files>
    src/db/schema.ts,
    src/db/queries.ts,
    src/db/__tests__/admin-audit.test.ts
  </files>
  <read_first>
    src/db/schema.ts,
    src/db/queries.ts,
    src/db/__tests__/admin-audit.test.ts,
    drizzle.config.ts
  </read_first>
  <behavior>
    - Audit entries persist in the database.
    - List helper supports pagination and lightweight filters.
    - Metadata is JSON-compatible and bounded.
    - Helper APIs are easy for mutation routes/helpers to call with actor context from `requireAdmin`.
  </behavior>
  <action>
    Add an audit table with fields for action, actor email, target type, target id, target count, metadata JSON, and created timestamp.

    Add audit helper types and functions. Keep the helper generic but intentionally narrow the action union to known admin actions so typos fail at compile time.
  </action>
  <verify>
    <automated>npx vitest run src/db/__tests__/admin-audit.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Audit helper tests pass.
    - TypeScript passes.
    - Schema update path is documented in summary.
  </acceptance_criteria>
  <done>Audit table and helpers are implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Wire audit into admin mutations</name>
  <files>
    src/db/queries.ts,
    src/db/orders.ts,
    src/app/api/admin/cards/route.ts,
    src/app/api/admin/cards/[id]/route.ts,
    src/app/api/admin/cards/bulk-delete/route.ts,
    src/app/api/admin/import/commit/route.ts,
    src/app/api/admin/orders/[id]/route.ts,
    src/app/api/admin/orders/[id]/cancel/route.ts
  </files>
  <read_first>
    src/db/queries.ts,
    src/db/orders.ts,
    src/app/api/admin/cards/route.ts,
    src/app/api/admin/cards/[id]/route.ts,
    src/app/api/admin/cards/bulk-delete/route.ts,
    src/app/api/admin/import/commit/route.ts,
    src/app/api/admin/orders/[id]/route.ts,
    src/app/api/admin/orders/[id]/cancel/route.ts
  </read_first>
  <behavior>
    - Inline inventory edit writes `inventory.update` audit metadata with changed fields.
    - Single delete writes `inventory.delete_one` with deleted card id/name where available.
    - Bulk delete writes `inventory.delete_many` with actual deleted IDs/count.
    - Delete-all writes `inventory.delete_all` with deleted count.
    - Import commit writes `inventory.import_commit` summary metadata.
    - Order status update writes `order.status_update`.
    - Order cancellation writes `order.cancel`, plus `order.restore_inventory` metadata when restore is requested.
  </behavior>
  <action>
    Pass actor email from `requireAdmin` or server auth context into mutation helpers/routes. Prefer central helper integration where the write already occurs; route-level audit is acceptable only when helper-level integration would force a risky refactor.

    Preserve existing response contracts unless Phase 14 explicitly needs to add audit IDs for debugging.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Existing mutation tests still pass.
    - New audit tests prove at least edit, bulk delete, import commit, status update, and cancel/restore write audit entries.
    - Full tests and build pass.
  </acceptance_criteria>
  <done>High-impact admin mutations write audit records.</done>
</task>

<task type="checkpoint:verify" gate="blocking">
  <name>Task 4: DB verify audit coverage</name>
  <files>src/db/queries.ts</files>
  <action>
    Verification must include disposable DB operations:
    1. Insert sentinel inventory.
    2. Perform an inline edit through the admin API/UI.
    3. Perform selected bulk delete.
    4. Perform an import commit or isolated import-helper equivalent with safe metadata.
    5. Perform order status update and cancel/restore if Phase 13 is present.
    6. Query audit log and confirm expected actions exist with safe metadata.
    7. Clean up sentinel rows and audit entries if they should not remain in the shared DB.
  </action>
  <done>Audit entries are verified against real mutations.</done>
</task>

</tasks>

<verification>
- `git diff --check`
- `npx vitest run src/db/__tests__/admin-audit.test.ts`
- `npx tsc --noEmit`
- `npm test`
- `npm run build`
- DB verification that real admin mutations create safe audit entries
</verification>

<success_criteria>
- [ ] AUD-01: High-impact admin mutations create audit log entries
- [ ] AUD-04: Destructive inventory actions remain understandable after the fact
- [ ] Audit metadata is safe and bounded
- [ ] Existing admin mutation behavior does not regress
</success_criteria>

<output>
After completion, create `.planning/phases/14-inventory-audit-trail/14-01-SUMMARY.md` with:
- final audit schema/helper contract
- mutation coverage map
- schema migration notes
- DB verification evidence
</output>
