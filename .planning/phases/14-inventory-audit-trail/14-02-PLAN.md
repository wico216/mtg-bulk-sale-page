---
phase: 14-inventory-audit-trail
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/db/schema.ts
  - src/db/queries.ts
  - src/db/__tests__/admin-audit.test.ts
  - src/app/admin/layout.tsx
  - src/app/admin/audit/page.tsx
  - src/app/admin/audit/_components/audit-table.tsx
  - src/app/admin/import/_components/import-client.tsx
  - src/app/api/admin/import/commit/route.ts
  - src/app/admin/_components/delete-confirmation.tsx
autonomous: true
requirements:
  - AUD-02
  - AUD-03
  - AUD-04
must_haves:
  truths:
    - "Admin can view recent audit entries from the admin shell"
    - "Admin can view import history with file counts, filenames, parsed/skipped counts, inserted count, actor, and timestamp"
    - "Import commit stores a durable import history record"
    - "Destructive inventory actions continue to recommend export/backup before mutation"
    - "Audit/history pages are admin-auth gated with direct server checks"
  artifacts:
    - path: "src/app/admin/audit/page.tsx"
      provides: "Admin audit/history page"
      contains: "Audit"
    - path: "src/app/admin/audit/_components/audit-table.tsx"
      provides: "Paginated audit table"
      contains: "action"
    - path: "src/db/queries.ts"
      provides: "Import history helpers"
      exports: ["getImportHistory"]
    - path: "src/app/api/admin/import/commit/route.ts"
      provides: "Import commit with history metadata"
      contains: "fileNames"
---

<objective>
Expose the audit trail and import history to the seller, and make irreversible inventory actions visibly safer without building full rollback.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-inventory-audit-trail/14-CONTEXT.md
@.planning/phases/14-inventory-audit-trail/14-01-SUMMARY.md
@.planning/phases/10.1-multi-csv-delete-inventory/10.1-01-SUMMARY.md
</execution_context>

<context>
@src/db/schema.ts
@src/db/queries.ts
@src/app/admin/layout.tsx
@src/app/admin/orders/page.tsx
@src/app/admin/_components/pagination.tsx
@src/app/admin/import/_components/import-client.tsx
@src/app/api/admin/import/commit/route.ts
@src/app/admin/_components/delete-confirmation.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add import history tests</name>
  <files>
    src/db/__tests__/admin-audit.test.ts
  </files>
  <read_first>
    src/db/queries.ts,
    src/db/schema.ts,
    src/app/api/admin/import/commit/route.ts,
    src/app/api/admin/import/__tests__/commit.test.ts
  </read_first>
  <behavior>
    - Import commit can persist file names, file count, parsed/skipped row counts, inserted card count, actor email, and timestamp.
    - `getImportHistory({ page, limit })` returns newest-first paginated import history.
    - Import history metadata excludes raw CSV body and card payload contents.
  </behavior>
  <action>
    Write failing tests first. If the import commit request contract needs to include metadata from preview, tests should define that request shape explicitly.
  </action>
  <verify>
    <automated>npx vitest run src/db/__tests__/admin-audit.test.ts src/app/api/admin/import/__tests__/commit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because import history is missing.
    - Tests cover single-file and multi-file metadata.
  </acceptance_criteria>
  <done>Red tests define import history contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement import history persistence</name>
  <files>
    src/db/schema.ts,
    src/db/queries.ts,
    src/app/api/admin/import/commit/route.ts,
    src/app/admin/import/_components/import-client.tsx
  </files>
  <read_first>
    src/db/schema.ts,
    src/db/queries.ts,
    src/app/api/admin/import/preview/route.ts,
    src/app/api/admin/import/commit/route.ts,
    src/app/admin/import/_components/import-client.tsx,
    src/lib/import-contract.ts
  </read_first>
  <behavior>
    - Import preview/commit handoff preserves safe file-level summary metadata.
    - Commit stores import history and audit entry together with the successful inventory replace where practical.
    - Failed import commit does not create a successful import history entry.
  </behavior>
  <action>
    Add import history schema/helper if it was not folded into audit in 14-01.

    Extend import preview/commit contract with safe metadata only. The client should not send raw CSV contents to commit; it already sends the approved enriched cards payload.
  </action>
  <verify>
    <automated>npx vitest run src/db/__tests__/admin-audit.test.ts src/app/api/admin/import/__tests__/commit.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Import history helper exists and is covered.
    - Commit route records import history on success.
    - Focused tests and TypeScript pass.
  </acceptance_criteria>
  <done>Successful imports leave durable import history.</done>
</task>

<task type="auto">
  <name>Task 3: Add audit/history admin page and backup affordances</name>
  <files>
    src/app/admin/layout.tsx,
    src/app/admin/audit/page.tsx,
    src/app/admin/audit/_components/audit-table.tsx,
    src/app/admin/_components/delete-confirmation.tsx,
    src/app/admin/_components/action-bar.tsx,
    src/app/admin/import/_components/import-client.tsx
  </files>
  <read_first>
    src/app/admin/layout.tsx,
    src/app/admin/orders/page.tsx,
    src/app/admin/_components/pagination.tsx,
    src/app/admin/_components/delete-confirmation.tsx,
    src/app/admin/_components/action-bar.tsx,
    src/app/admin/import/_components/import-client.tsx
  </read_first>
  <behavior>
    - Admin nav exposes an Audit/History page.
    - `/admin/audit` directly checks admin auth server-side.
    - Page shows recent audit entries and import history sections.
    - Empty states are clear.
    - Delete-all, bulk delete, and import commit confirmations include export/backup guidance and keep existing destructive confirmation behavior.
  </behavior>
  <action>
    Build the page as server-rendered where possible. Use pagination search params if the table grows.

    Keep UI copy operational: what changed, who did it, when, and enough metadata to understand scope.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `/admin/audit` renders audit entries and import history.
    - Admin nav includes the page.
    - Existing destructive confirmations still work and include backup/export guidance.
    - Full tests and build pass.
  </acceptance_criteria>
  <done>Seller can inspect audit and import history from admin UI.</done>
</task>

<task type="checkpoint:verify" gate="blocking">
  <name>Task 4: Browser verify audit/history page</name>
  <files>src/app/admin/audit/page.tsx</files>
  <action>
    Browser verification must include:
    1. Sign into admin locally.
    2. Perform a disposable inventory mutation that creates an audit entry.
    3. Perform a disposable import or mock-safe import path if a real import would be too destructive.
    4. Visit `/admin/audit` and confirm entries render with action, actor, timestamp, and safe metadata.
    5. Confirm destructive confirmations still include export/backup guidance.
    6. Clean up disposable data if needed.
    7. Confirm browser console and network logs have no unexpected errors.
  </action>
  <done>Audit/history UI is verified in browser.</done>
</task>

</tasks>

<verification>
- `git diff --check`
- `npx vitest run src/db/__tests__/admin-audit.test.ts src/app/api/admin/import/__tests__/commit.test.ts`
- `npx tsc --noEmit`
- `npm test`
- `npm run build`
- Browser verification of `/admin/audit` and destructive copy
</verification>

<success_criteria>
- [ ] AUD-02: Import commits create durable import history
- [ ] AUD-03: Admin can view audit/import history
- [ ] AUD-04: Destructive flows clearly preserve export/backup guidance
- [ ] Audit/history pages are admin-auth gated
</success_criteria>

<output>
After completion, create `.planning/phases/14-inventory-audit-trail/14-02-SUMMARY.md` with:
- final import history contract
- audit/history UI notes
- browser verification evidence
- deferred rollback/restore ideas
</output>
