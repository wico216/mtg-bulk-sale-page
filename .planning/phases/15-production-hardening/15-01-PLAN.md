---
phase: 15-production-hardening
plan: 01
type: execute
wave: 1
depends_on: ["14-02"]
files_modified:
  - src/lib/rate-limit.ts
  - src/lib/logger.ts
  - src/app/api/checkout/route.ts
  - src/app/api/admin/cards/route.ts
  - src/app/api/admin/cards/[id]/route.ts
  - src/app/api/admin/cards/bulk-delete/route.ts
  - src/app/api/admin/import/commit/route.ts
  - src/app/api/admin/orders/[id]/route.ts
  - src/app/api/admin/orders/[id]/cancel/route.ts
  - src/app/api/admin/health/route.ts
autonomous: true
requirements:
  - OPS-01
  - OPS-02
must_haves:
  truths:
    - "Checkout and admin mutation APIs have production-compatible rate limiting"
    - "Rate limit responses are explicit and do not mutate state"
    - "High-impact workflows emit structured logs for success/failure state transitions"
    - "Structured logs never include secrets, auth cookies, raw CSV bodies, or full environment values"
    - "Notification failures remain non-blocking but visible in logs/health data"
  artifacts:
    - path: "src/lib/rate-limit.ts"
      provides: "Serverless-compatible rate limit helper"
      exports: ["checkRateLimit"]
    - path: "src/lib/logger.ts"
      provides: "Structured logging helper"
      exports: ["logEvent", "logError"]
    - path: "src/app/api/checkout/route.ts"
      provides: "Rate-limited checkout with structured logs"
      contains: "checkout"
---

<objective>
Add guardrails and durable diagnostic signals to the write surfaces most likely to cause production damage or confusion: checkout and admin mutations.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-production-hardening/15-CONTEXT.md
@.planning/phases/14-inventory-audit-trail/14-02-SUMMARY.md
@.planning/phases/11-checkout-upgrade-order-history/11-01-SUMMARY.md
</execution_context>

<context>
@src/app/api/checkout/route.ts
@src/app/api/admin/cards/route.ts
@src/app/api/admin/cards/[id]/route.ts
@src/app/api/admin/cards/bulk-delete/route.ts
@src/app/api/admin/import/commit/route.ts
@src/app/api/admin/orders/[id]/route.ts
@src/app/api/admin/orders/[id]/cancel/route.ts
@src/lib/notifications.ts
@src/lib/auth/admin-check.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Choose and test rate-limit/logging primitives</name>
  <files>
    src/lib/rate-limit.ts,
    src/lib/logger.ts,
    src/lib/__tests__/rate-limit.test.ts,
    src/lib/__tests__/logger.test.ts
  </files>
  <read_first>
    package.json,
    src/app/api/checkout/route.ts,
    src/lib/notifications.ts,
    src/app/api/admin/health/route.ts
  </read_first>
  <behavior>
    - Rate limit helper works in serverless environments and can be configured per surface.
    - Tests can deterministically simulate allowed and blocked requests.
    - Logger emits JSON-serializable records with level, event, timestamp, route/helper, safe metadata, and error summary.
    - Logger redacts or rejects unsafe fields.
  </behavior>
  <action>
    Research the lightest production-compatible rate limit storage already available to this app. Prefer reusing Neon/Postgres if acceptable for this small store; avoid adding a new vendor unless necessary.

    Write tests for the chosen helper before wiring it into routes.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/rate-limit.test.ts src/lib/__tests__/logger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests define allowed/blocked behavior and safe logging output.
    - The chosen rate-limit strategy is documented in the summary with tradeoffs.
  </acceptance_criteria>
  <done>Rate-limit and structured logging primitives are selected and covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply rate limits to write APIs</name>
  <files>
    src/app/api/checkout/route.ts,
    src/app/api/admin/cards/route.ts,
    src/app/api/admin/cards/[id]/route.ts,
    src/app/api/admin/cards/bulk-delete/route.ts,
    src/app/api/admin/import/commit/route.ts,
    src/app/api/admin/orders/[id]/route.ts,
    src/app/api/admin/orders/[id]/cancel/route.ts
  </files>
  <read_first>
    src/app/api/checkout/route.ts,
    src/app/api/admin/cards/route.ts,
    src/app/api/admin/cards/[id]/route.ts,
    src/app/api/admin/cards/bulk-delete/route.ts,
    src/app/api/admin/import/commit/route.ts,
    src/app/api/admin/orders/[id]/route.ts,
    src/app/api/admin/orders/[id]/cancel/route.ts,
    src/lib/rate-limit.ts
  </read_first>
  <behavior>
    - Checkout rate limit blocks abusive repeat submissions before DB mutation.
    - Admin mutation rate limits run after auth where actor identity is available when practical.
    - Blocked requests return 429 with clear JSON and optional retry metadata.
    - Existing auth failures still return 401/403 rather than being hidden by admin rate limits.
  </behavior>
  <action>
    Add route-level rate-limit checks with different buckets for public checkout and admin mutations. Keep thresholds conservative but not annoying for normal manual use.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Route tests cover 429 behavior on checkout and at least two admin mutation routes.
    - Blocked requests do not call mutation helpers.
    - TypeScript and tests pass.
  </acceptance_criteria>
  <done>Write APIs are rate-limited.</done>
</task>

<task type="auto">
  <name>Task 3: Add structured logs to critical workflows</name>
  <files>
    src/app/api/checkout/route.ts,
    src/app/api/admin/cards/route.ts,
    src/app/api/admin/cards/bulk-delete/route.ts,
    src/app/api/admin/import/commit/route.ts,
    src/app/api/admin/orders/[id]/route.ts,
    src/app/api/admin/orders/[id]/cancel/route.ts,
    src/lib/notifications.ts,
    src/lib/logger.ts
  </files>
  <read_first>
    src/lib/logger.ts,
    src/app/api/checkout/route.ts,
    src/lib/notifications.ts,
    src/app/api/admin/import/commit/route.ts,
    src/app/api/admin/cards/bulk-delete/route.ts
  </read_first>
  <behavior>
    - Checkout logs validation failure, stock conflict, DB failure, order committed, and notification failure states.
    - Import/delete/order workflow mutations log success/failure with safe counts/IDs.
    - Notification failure after order commit is logged as non-blocking but findable.
    - Logs remain useful in Vercel output without being noisy.
  </behavior>
  <action>
    Add structured logging calls around state transitions and catch blocks. Avoid logging raw request bodies. Prefer stable event names such as `checkout.order_committed`, `admin.bulk_delete.failed`, `notification.buyer_email_failed`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Existing tests pass.
    - At least one test verifies logging shape for checkout notification failure or admin mutation failure.
    - Build passes.
  </acceptance_criteria>
  <done>Critical workflows emit safe structured logs.</done>
</task>

<task type="checkpoint:verify" gate="blocking">
  <name>Task 4: Verify rate-limit and logging behavior</name>
  <files>src/app/api/checkout/route.ts</files>
  <action>
    Verification must include:
    1. Focused route tests for 429 behavior.
    2. A local request sequence that trips a test/dev rate limit without mutating data after the limit.
    3. A local checkout with notification failure configured safely, confirming order commit still succeeds and structured failure log appears.
    4. No secrets in captured logs.
  </action>
  <done>Rate limits and structured logs are verified.</done>
</task>

</tasks>

<verification>
- `git diff --check`
- `npx vitest run src/lib/__tests__/rate-limit.test.ts src/lib/__tests__/logger.test.ts`
- `npx tsc --noEmit`
- `npm test`
- `npm run build`
- Local request proof for 429 and notification-failure logging
</verification>

<success_criteria>
- [ ] OPS-01: Checkout and admin mutation APIs have production-compatible rate limits
- [ ] OPS-02: Critical workflows emit safe structured logs
- [ ] Rate-limited requests do not mutate state
- [ ] Notification failures remain non-blocking but diagnosable
</success_criteria>

<output>
After completion, create `.planning/phases/15-production-hardening/15-01-SUMMARY.md` with:
- rate-limit storage/threshold decisions
- structured event names
- verification evidence
- known limitations/deferred observability vendor ideas
</output>
