---
phase: 15-production-hardening
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/app/api/admin/health/route.ts
  - src/app/admin/health/page.tsx
  - src/app/admin/layout.tsx
  - scripts/smoke-production.ts
  - package.json
  - README.md
  - .planning/phases/15-production-hardening/15-SECURITY-REVIEW.md
autonomous: true
requirements:
  - OPS-03
  - OPS-04
  - OPS-05
must_haves:
  truths:
    - "Admin can view operational health without exposing sensitive details publicly"
    - "Health checks cover DB, auth config, Google OAuth config, email config, recent order/import/audit timestamps, and recent notification failures"
    - "A production smoke script can run read-only/guard-focused checks against a Vercel deployment"
    - "Operational docs explain required env vars, local verification, production smoke, and backup/export process"
    - "A security review artifact records admin/API threat-model findings and follow-ups"
  artifacts:
    - path: "src/app/api/admin/health/route.ts"
      provides: "Admin health JSON endpoint"
      contains: "database"
    - path: "src/app/admin/health/page.tsx"
      provides: "Admin health page"
      contains: "Health"
    - path: "scripts/smoke-production.ts"
      provides: "Repeatable production smoke script"
      contains: "bulk-delete"
    - path: "README.md"
      provides: "Operational runbook updates"
      contains: "Production smoke"
---

<objective>
Create durable operational surfaces: an admin health page/API, a repeatable production smoke script, docs for running the store, and a security review artifact.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-production-hardening/15-CONTEXT.md
@.planning/phases/15-production-hardening/15-01-SUMMARY.md
@.planning/phases/14-inventory-audit-trail/14-02-SUMMARY.md
</execution_context>

<context>
@src/app/api/admin/health/route.ts
@src/app/admin/layout.tsx
@src/db/orders.ts
@src/db/queries.ts
@src/lib/notifications.ts
@package.json
@README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add health endpoint tests</name>
  <files>
    src/app/api/admin/health/__tests__/route.test.ts,
    src/db/__tests__/admin-health.test.ts
  </files>
  <read_first>
    src/app/api/admin/health/route.ts,
    src/lib/auth/admin-check.ts,
    src/db/orders.ts,
    src/db/queries.ts,
    src/lib/notifications.ts
  </read_first>
  <behavior>
    - Health endpoint requires admin auth for detailed status.
    - Health response reports database reachability.
    - Health response reports auth secret, Google OAuth, and email configuration as configured/missing without exposing values.
    - Health response includes last order/import/audit timestamps when available.
    - Health response includes recent notification failure count when Phase 15 logging/audit data supports it.
  </behavior>
  <action>
    Write failing tests first. Mock config/env presence with key names only; never assert or print secret values.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/health/__tests__/route.test.ts src/db/__tests__/admin-health.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation for missing detailed health behavior.
    - Tests cover auth, configured/missing states, DB ok/error, and recent timestamps.
  </acceptance_criteria>
  <done>Red tests define admin health contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement health endpoint and admin page</name>
  <files>
    src/app/api/admin/health/route.ts,
    src/app/admin/health/page.tsx,
    src/app/admin/layout.tsx,
    src/db/queries.ts,
    src/db/orders.ts
  </files>
  <read_first>
    src/app/api/admin/health/route.ts,
    src/app/admin/layout.tsx,
    src/app/admin/page.tsx,
    src/app/admin/audit/page.tsx,
    src/db/queries.ts,
    src/db/orders.ts
  </read_first>
  <behavior>
    - `/admin/health` page and `/api/admin/health` detailed endpoint are admin-only.
    - UI shows clear pass/warn/error states.
    - Secret values are never rendered.
    - Health checks are fast and safe to run repeatedly.
  </behavior>
  <action>
    Expand the existing health route rather than creating a parallel system. Add an admin page linked from the admin nav.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/health/__tests__/route.test.ts src/db/__tests__/admin-health.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Health endpoint/page render admin-only detailed status.
    - Missing env/config warnings name keys but not values.
    - Focused tests and TypeScript pass.
  </acceptance_criteria>
  <done>Admin can inspect operational health.</done>
</task>

<task type="auto">
  <name>Task 3: Add production smoke script and runbook docs</name>
  <files>
    scripts/smoke-production.ts,
    package.json,
    README.md
  </files>
  <read_first>
    README.md,
    package.json,
    .env.local.example
  </read_first>
  <behavior>
    - Smoke script accepts a deployment URL or Vercel deployment ID.
    - Default smoke is read-only/guard-focused and safe for production.
    - Script checks app shell, admin login provider visibility, local password fields hidden in production, unauthenticated admin redirect, and unauthenticated admin mutation 401.
    - README documents required env keys by environment, local verification commands, production smoke command, and backup/export process.
  </behavior>
  <action>
    Implement script in TypeScript or plain Node using built-in fetch/child_process. If Vercel protection requires `vercel curl`, support that path explicitly.

    Add `npm run smoke:production`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && npm run build && npm run smoke:production -- --help</automated>
  </verify>
  <acceptance_criteria>
    - Smoke script can run help locally without secrets.
    - README gives a fresh operator enough detail to verify deployment without reading this conversation.
    - Full tests and build pass.
  </acceptance_criteria>
  <done>Production smoke and runbook are durable.</done>
</task>

<task type="manual-review" gate="blocking">
  <name>Task 4: Security review admin/API surfaces</name>
  <files>
    .planning/phases/15-production-hardening/15-SECURITY-REVIEW.md
  </files>
  <action>
    Perform a STRIDE-style review of:
    - Auth and admin route protection
    - Admin mutation APIs
    - Checkout API
    - Import preview/commit
    - Audit/history and health surfaces
    - Rate-limit bypass possibilities
    - Secret exposure risk in logs/health/docs

    Record findings with severity, exploit scenario, remediation, and whether fixed in Phase 15 or deferred.
  </action>
  <done>Security review artifact exists with concrete findings and follow-ups.</done>
</task>

<task type="checkpoint:verify" gate="blocking">
  <name>Task 5: Full hardening verification</name>
  <files>scripts/smoke-production.ts</files>
  <action>
    Verification must include:
    1. `git diff --check`.
    2. Full TypeScript, tests, and build.
    3. Browser verification of `/admin/health` locally.
    4. Run production smoke against the current Vercel deployment without authenticated mutation.
    5. Confirm no secrets appear in logs, health UI, or smoke output.
  </action>
  <done>Production hardening surfaces are verified locally and against deployment.</done>
</task>

</tasks>

<verification>
- `git diff --check`
- `npx vitest run src/app/api/admin/health/__tests__/route.test.ts src/db/__tests__/admin-health.test.ts`
- `npx tsc --noEmit`
- `npm test`
- `npm run build`
- `npm run smoke:production -- --help`
- Browser verification of `/admin/health`
- Production smoke against Vercel deployment
</verification>

<success_criteria>
- [ ] OPS-03: Admin can inspect operational health without exposing secrets
- [ ] OPS-04: Production smoke is repeatable and checked into the repo
- [ ] OPS-05: Security review records concrete admin/API findings
- [ ] Operational docs are sufficient for a fresh maintainer/operator
</success_criteria>

<output>
After completion, create `.planning/phases/15-production-hardening/15-02-SUMMARY.md` with:
- health endpoint/page contract
- smoke script usage and latest run evidence
- runbook docs summary
- security review findings/follow-ups
</output>
