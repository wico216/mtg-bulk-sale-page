---
phase: 05-checkout-and-deploy
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - .env.local
  - next.config.ts
autonomous: false
requirements: [CHKT-01, CHKT-02, CHKT-03, CHKT-04, CHKT-05]

must_haves:
  truths:
    - "Application builds successfully with all checkout routes"
    - "Full checkout flow works end-to-end: add items -> checkout -> fill form -> submit -> emails sent -> confirmation displayed"
    - "Application is deployed and accessible on the internet"
  artifacts:
    - path: "src/app/checkout/page.tsx"
      provides: "Checkout page renders without build errors"
    - path: "src/app/api/checkout/route.ts"
      provides: "API route processes orders and sends emails"
    - path: "src/app/confirmation/page.tsx"
      provides: "Confirmation page renders with order details"
  key_links:
    - from: "checkout-client.tsx"
      to: "/api/checkout"
      via: "POST fetch with order data"
      pattern: "fetch.*api/checkout"
    - from: "/api/checkout"
      to: "Resend API"
      via: "resend.emails.send()"
      pattern: "resend\\.emails\\.send"
    - from: "checkout-client.tsx"
      to: "/confirmation"
      via: "router.push after success"
      pattern: "router\\.push.*confirmation"
---

<objective>
Verify the full checkout flow works locally, configure environment for Resend email delivery, and deploy the application to Vercel. This is the integration and deployment plan that validates all checkout requirements (CHKT-01 through CHKT-05) work together end-to-end.

Purpose: Plans 01 and 02 built the backend and frontend separately. This plan wires them together with real environment configuration, validates the complete flow, and ships the application to production. The human checkpoint ensures visual and functional correctness before deploying.

Output: Working local checkout flow with real email delivery (Resend), production deployment on Vercel with environment variables configured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-checkout-and-deploy/05-CONTEXT.md
@.planning/phases/05-checkout-and-deploy/05-RESEARCH.md
@.planning/phases/05-checkout-and-deploy/05-01-SUMMARY.md
@.planning/phases/05-checkout-and-deploy/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and local environment setup</name>
  <files>
    next.config.ts
  </files>
  <read_first>
    next.config.ts,
    .env.local.example,
    package.json,
    node_modules/next/dist/docs/01-app/01-getting-started/17-deploying.md
  </read_first>
  <action>
1. Run full build to verify everything compiles:
   ```bash
   npm run build
   ```
   This runs `tsx scripts/generate-data.ts && next build` which validates:
   - Data generation script produces cards.json
   - All TypeScript files compile without errors
   - All pages (including new /checkout, /confirmation) render during SSG
   - API route at /api/checkout compiles

   If build fails, fix any TypeScript errors before proceeding.

2. Check `next.config.ts` for any needed updates. The application is transitioning from fully static (SSG) to hybrid per D-29: pages remain static, /api/checkout is a serverless function. In Next.js App Router, this happens automatically -- Route Handlers that use `POST` are dynamic by default (never statically exported). No config change should be needed, but verify:
   - `output: "export"` must NOT be present (that would prevent API routes from working)
   - If `output: "export"` is found, REMOVE it -- the app now needs server-side functionality for the API route

3. Start the dev server to verify local routing works:
   ```bash
   npm run dev &
   sleep 3
   # Verify checkout page loads
   curl -s http://localhost:3000/checkout | head -5
   # Verify API route responds (will return error without env vars, but should not 404)
   curl -s -X POST http://localhost:3000/api/checkout -H "Content-Type: application/json" -d '{"buyerName":"Test","buyerEmail":"test@test.com","items":[]}' | head -5
   # Verify confirmation page loads
   curl -s "http://localhost:3000/confirmation?ref=TEST-123&email=test@test.com&total=5.00&count=2&name=Test" | head -5
   ```
   Kill dev server after verification.

4. Verify `.env.local.example` exists with RESEND_API_KEY and SELLER_EMAIL documented.

5. If `.env.local` does not exist, create it from the example (user will fill in real values):
   ```
   # Get your API key at: https://resend.com/api-keys
   RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

   # Seller email address for order notifications
   SELLER_EMAIL=your-email@example.com
   ```
  </action>
  <verify>
    <automated>cd /Users/wiko/Documents/Projects/magic-bulk-sale && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits with code 0
    - next.config.ts does NOT contain 'output: "export"' (or output: 'export')
    - .env.local.example exists and contains "RESEND_API_KEY"
    - .env.local.example exists and contains "SELLER_EMAIL"
    - Build output shows /checkout, /api/checkout, and /confirmation routes
  </acceptance_criteria>
  <done>Application builds successfully with all checkout routes. next.config.ts supports hybrid (static pages + serverless API). Environment variable template exists for Resend configuration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Full checkout flow verification and Vercel deployment</name>
  <files>none</files>
  <action>
    Present the verification checklist to the user. The user must configure Resend credentials in .env.local, test the complete checkout flow locally, and deploy to Vercel with environment variables configured. This task cannot be automated because it requires: (1) the user's Resend API key from their account dashboard, (2) visual verification of the checkout UI and email templates, (3) Vercel deployment with the user's account credentials.
  </action>
  <what-built>
    Complete checkout flow across three plans:
    - Plan 01: API route at /api/checkout with Resend email delivery, order data pipeline, email templates
    - Plan 02: Checkout page (/checkout) with form, order summary, submit flow. Confirmation page (/confirmation) with receipt display.
    - Plan 03: Build verification and environment setup.

    All five CHKT requirements should now be functional:
    - CHKT-01: Checkout form with name/email (no account needed)
    - CHKT-02: Order summary displayed before submission
    - CHKT-03: Seller receives order email
    - CHKT-04: Buyer receives confirmation email
    - CHKT-05: Confirmation page with pay-in-person note
  </what-built>
  <how-to-verify>
    **Pre-requisite: Set up Resend (if not done yet)**
    1. Sign up at https://resend.com (free tier: 100 emails/day)
    2. Create an API key at https://resend.com/api-keys
    3. Edit `.env.local` and set:
       - RESEND_API_KEY=re_your_actual_key
       - SELLER_EMAIL=your-actual-email@example.com

    **Test the flow locally:**
    1. Run `npm run dev`
    2. Open http://localhost:3000 -- add 2-3 cards to cart
    3. Click "Checkout" in the cart summary bar
    4. Verify: Checkout page shows order summary with card thumbnails, quantities, prices, total
    5. Verify: "Edit cart" link goes back to /cart
    6. Fill in: Name, email (your real email for testing), optional message
    7. Click "Place order"
    8. Verify: Button shows "Placing order..." spinner during submission
    9. Verify: Redirected to /confirmation page with checkmark, "Order placed!", summary, pay-in-person note
    10. Check seller email inbox for order notification email
    11. Check buyer email inbox for confirmation email (may be in spam with default Resend domain)
    12. Verify: Cart is cleared (go to /cart -- should show empty state)

    **Test edge cases:**
    13. Navigate to /checkout with empty cart -- should show "Your cart is empty" with "Browse cards" link
    14. Refresh the confirmation page -- should still show summary from URL params (full item list may be missing if sessionStorage cleared)
    15. Try submitting with missing name/email -- HTML5 validation should prevent submission

    **Deploy to Vercel (D-28):**
    16. Run `vercel` CLI or connect GitHub repo to Vercel dashboard
    17. Set environment variables in Vercel dashboard: RESEND_API_KEY, SELLER_EMAIL (D-30)
    18. Verify: Production URL loads, checkout flow works on deployed site
    19. Verify: Emails are sent from production

    Report any issues with "describe issues" -- fixes will be applied before proceeding.
  </how-to-verify>
  <verify>Manual verification by user following the checklist above</verify>
  <acceptance_criteria>
    - User confirms checkout form renders with name, email, message fields
    - User confirms order summary shows thumbnails, quantities, prices, total
    - User confirms "Place order" button shows loading state during submission
    - User confirms confirmation page shows checkmark, "Order placed!", pay-in-person note
    - User confirms seller received order notification email
    - User confirms buyer received confirmation email
    - User confirms cart is cleared after successful order
    - User confirms empty cart shows "Your cart is empty" on checkout page
    - Application deployed to Vercel and accessible via production URL
  </acceptance_criteria>
  <done>User has approved the full checkout flow working end-to-end locally and on Vercel. All 5 CHKT requirements validated through manual walkthrough. Application is live on the internet.</done>
  <resume-signal>Type "approved" if flow works, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Local dev server serves /checkout, /api/checkout, /confirmation without errors
3. Full checkout flow tested manually: form submission -> emails -> confirmation page
4. Application deployed to Vercel and accessible via .vercel.app URL
5. Emails delivered via Resend (seller notification + buyer confirmation)
</verification>

<success_criteria>
- Build passes with all checkout routes compiled
- User confirms checkout form, order summary, email delivery, and confirmation page all work correctly
- Application is deployed and accessible on the internet per D-28
- Environment variables (RESEND_API_KEY, SELLER_EMAIL) configured in production per D-30
- All 5 CHKT requirements validated through manual walkthrough
</success_criteria>

<output>
After completion, create `.planning/phases/05-checkout-and-deploy/05-03-SUMMARY.md`
</output>
