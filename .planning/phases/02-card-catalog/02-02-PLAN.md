---
phase: 02-card-catalog
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/page.tsx
  - src/app/layout.tsx
  - src/components/header.tsx
  - src/components/card-grid.tsx
  - src/components/card-tile.tsx
autonomous: true
requirements: [CATL-01, CATL-08]

must_haves:
  truths:
    - "User sees a grid of card images with name, price, and condition+quantity below each"
    - "Grid shows 2 columns on mobile and 4-5 columns on desktop"
    - "Foil cards display a subtle FOIL badge on the tile"
    - "Cards with no price show N/A without visual distinction from priced cards"
    - "Site has a minimal header with store name"
  artifacts:
    - path: "src/components/card-grid.tsx"
      provides: "Client component rendering responsive grid of card tiles with modal state"
      contains: "use client"
    - path: "src/components/card-tile.tsx"
      provides: "Individual card tile with image, name, price, condition badge"
      contains: "CardTile"
    - path: "src/components/header.tsx"
      provides: "Minimal site header with store name"
      contains: "header"
    - path: "src/app/page.tsx"
      provides: "Server component loading cards.json and passing to CardGrid"
      contains: "CardGrid"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/card-grid.tsx"
      via: "passes cards array as prop"
      pattern: "CardGrid.*cards"
    - from: "src/components/card-grid.tsx"
      to: "src/components/card-tile.tsx"
      via: "maps cards to CardTile components"
      pattern: "CardTile"
    - from: "src/components/card-tile.tsx"
      to: "next/image"
      via: "renders Scryfall images via next/image"
      pattern: "import Image"
---

<objective>
Build the browsable card catalog: header, responsive card grid with image-dominant tiles.

Purpose: Users see the full card inventory for the first time as a visual grid resembling a card binder. This is the core catalog experience.
Output: Header component, CardGrid client component, CardTile component, rewritten page.tsx.
</objective>

<execution_context>
@/Users/wiko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wiko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-card-catalog/02-01-SUMMARY.md
@src/lib/types.ts
@src/app/page.tsx
@src/app/layout.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create header component and card-tile component</name>
  <files>src/components/header.tsx, src/components/card-tile.tsx</files>
  <action>
Create src/components/ directory if it doesn't exist.

**src/components/header.tsx** (Server Component, no 'use client'):
- Simple `<header>` element with left-aligned text: "Viki" in bold and "MTG Bulk Store" in lighter weight
- Horizontal line or subtle bottom border to separate from content
- Clean/minimal: `max-w-7xl mx-auto px-4 py-3` for consistent page width
- Use accent color for "Viki" text via `text-accent` Tailwind class

**src/components/card-tile.tsx** (no 'use client' -- will be used inside client component):
- Props: `card: Card`, `onClick: () => void`
- Renders as a `<button>` with `text-left` for accessibility (clickable tile)
- Image section: `relative aspect-[5/7] w-full overflow-hidden rounded-lg bg-zinc-100`
  - If card.imageUrl exists: `<Image>` from next/image with `fill`, `sizes="(max-width: 768px) 50vw, (max-width: 1024px) 25vw, 20vw"`, `className="object-cover"`, hover scale effect via `group-hover:scale-105 transition-transform`
  - If card.imageUrl is null: placeholder div with "No Image" text centered, text-zinc-400
  - If card.foil: absolute-positioned badge top-right: "FOIL" in `bg-amber-100 text-amber-700 text-xs font-medium rounded px-1.5 py-0.5`
- Metadata section below image (`mt-1.5 space-y-0.5`):
  - Card name: `text-sm font-medium leading-tight truncate`
  - Price: `text-sm text-zinc-500` -- format as `$X.XX` or "N/A" if null
  - Condition + quantity: `text-xs text-zinc-400` -- map condition strings to display names:
    - "near_mint" -> "NM", "lightly_played" -> "LP", "moderately_played" -> "MP", "heavily_played" -> "HP", "damaged" -> "DMG"
    - Format: "{condition} x{quantity}" (e.g., "NM x2")
  - Set name is NOT shown on the tile (per user decision -- shown in modal only)
  </action>
  <verify>Run `npx tsc --noEmit` to confirm no type errors in both new components.</verify>
  <done>Header renders store name with accent color. CardTile renders image-dominant card with name, price, condition badge, quantity, and foil indicator. Condition strings mapped to short display names.</done>
</task>

<task type="auto">
  <name>Task 2: Create card-grid client component and rewrite page.tsx</name>
  <files>src/components/card-grid.tsx, src/app/page.tsx, src/app/layout.tsx</files>
  <action>
**src/components/card-grid.tsx** ('use client'):
- Props: `{ cards: Card[], meta: CardData['meta'] }`
- State: `const [selectedCard, setSelectedCard] = useState<Card | null>(null)`
- Renders responsive CSS Grid: `grid grid-cols-2 gap-3 sm:gap-4 md:grid-cols-4 lg:grid-cols-5`
- Maps over cards, rendering `<CardTile>` for each with `onClick={() => setSelectedCard(card)}`
- Wraps grid in a container: `max-w-7xl mx-auto px-4 pb-8`
- If cards array is empty, render centered empty state: "No cards available. Run npm run generate to build inventory."
- For now, selectedCard state is set but modal rendering will be added in Plan 02-03. Add a comment: `{/* Modal rendered in Plan 02-03 */}`

**src/app/page.tsx** (Server Component -- full rewrite):
- Keep the existing `loadCardData()` function (reads data/generated/cards.json)
- Import and render `<Header />` and `<CardGrid cards={data.cards} meta={data.meta} />`
- If no data, render empty state with "No card data generated yet" message (similar to current but styled with new header)
- Remove ALL existing Pipeline Stats and Sample Cards table markup -- this page is now the catalog

**src/app/layout.tsx**:
- Add `<Header />` import and render it above `{children}` inside the body, so it appears on every page
- Actually, better to keep Header in page.tsx only for now since there's only one page. Put Header inside page.tsx above CardGrid.
- No changes to layout.tsx needed unless adding the header there makes more sense. Keep it in page.tsx.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm no type errors. Run `npm run build` to verify the page builds successfully with static generation. Open the dev server and confirm the grid renders with card images.</verify>
  <done>Page shows a header with "Viki" branding and a responsive grid of card tiles. 2 columns on mobile viewport, 4-5 on desktop. All 136 cards visible with images, names, prices, and condition info.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` completes with static page generation
- Card grid renders with images from Scryfall (next/image working)
- Grid is responsive: 2 cols on small screens, 4-5 on larger
- Foil cards show FOIL badge
- Cards with null price show "N/A"
- Condition shows abbreviated form (NM, LP, etc.)
</verification>

<success_criteria>
User can see the full 136-card inventory in an image-dominant responsive grid with card name, price, condition, and quantity visible on each tile. The layout feels like browsing a card binder.
</success_criteria>

<output>
After completion, create `.planning/phases/02-card-catalog/02-02-SUMMARY.md`
</output>
