---
phase: 02-card-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/enrichment.ts
  - next.config.ts
  - src/app/globals.css
autonomous: true
requirements: [CATL-07]

must_haves:
  truths:
    - "cards.json contains oracleText field for every card"
    - "next/image can load images from cards.scryfall.io without errors"
    - "Accent color theme variables are available for Tailwind classes"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "Card interface with oracleText field, ScryfallCard with oracle_text"
      contains: "oracleText"
    - path: "src/lib/enrichment.ts"
      provides: "getOracleText extraction with double-faced card handling"
      contains: "getOracleText"
    - path: "next.config.ts"
      provides: "remotePatterns for cards.scryfall.io"
      contains: "cards.scryfall.io"
    - path: "src/app/globals.css"
      provides: "Accent color CSS variables for indigo theme"
      contains: "--color-accent"
    - path: "data/generated/cards.json"
      provides: "Regenerated card data with oracleText field"
  key_links:
    - from: "src/lib/enrichment.ts"
      to: "src/lib/types.ts"
      via: "imports ScryfallCard with oracle_text field"
      pattern: "oracle_text"
---

<objective>
Extend the data model with oracle text, configure next/image for Scryfall, and add accent theme colors.

Purpose: The detail modal (Plan 02-03) needs oracle text which is missing from the pipeline. The card grid (Plan 02-02) needs next/image configured for Scryfall URLs and accent colors for the UI theme. This foundation must exist before any UI work.
Output: Updated types, enrichment with oracleText, regenerated cards.json, next.config with remotePatterns, globals.css with accent colors.
</objective>

<execution_context>
@/Users/wiko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wiko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/types.ts
@src/lib/enrichment.ts
@next.config.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add oracleText to Card type, ScryfallCard type, and enrichment pipeline</name>
  <files>src/lib/types.ts, src/lib/enrichment.ts</files>
  <action>
In src/lib/types.ts:
1. Add `oracleText: string | null;` to the Card interface (after `imageUrl`), with JSDoc: `/** Oracle rules text, null if unavailable */`
2. Add `oracle_text?: string;` to the ScryfallCard interface (top-level field)
3. Add `oracle_text?: string;` inside the card_faces array element type (alongside existing `name` and `image_uris`)

In src/lib/enrichment.ts:
1. Add a `getOracleText(card: ScryfallCard): string | null` function (same pattern as getImageUrl):
   - If `card.oracle_text` exists, return it
   - Else if `card.card_faces` exists, collect `oracle_text` from each face that has it, join with ` // `, return the result (or null if none found)
   - Else return null
2. In the enrichCards loop, after setting colorIdentity, add: `card.oracleText = getOracleText(scryfallData);`

Note: card_faces already has `oracle_text` typed (we added it), so the extraction is type-safe.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm no type errors. Grep types.ts for "oracleText" and enrichment.ts for "getOracleText".</verify>
  <done>Card interface has oracleText field. ScryfallCard has oracle_text at top level and in card_faces. enrichCards extracts oracle text with double-faced card handling.</done>
</task>

<task type="auto">
  <name>Task 2: Configure next/image remotePatterns, add accent theme colors, and regenerate cards.json</name>
  <files>next.config.ts, src/app/globals.css</files>
  <action>
In next.config.ts:
1. Add `images.remotePatterns` array with one entry: `{ protocol: 'https', hostname: 'cards.scryfall.io', pathname: '/**' }`

In src/app/globals.css:
1. Add to the existing `@theme inline {}` block:
   - `--color-accent: #4f46e5;` (indigo-600 -- blue/indigo accent per user decision)
   - `--color-accent-hover: #4338ca;` (indigo-700 for hover states)
   - `--color-accent-light: #eef2ff;` (indigo-50 for subtle backgrounds)

Then run `npm run generate` to regenerate cards.json with the new oracleText field included. Verify the output shows oracleText populated for cards.
  </action>
  <verify>Run `npm run generate` and confirm it completes without errors. Check that data/generated/cards.json contains "oracleText" fields. Run `npx tsc --noEmit` to confirm no type errors. Run `npm run build` to verify the full build succeeds.</verify>
  <done>next.config.ts has Scryfall remotePatterns. globals.css has accent color variables. cards.json is regenerated with oracleText for all 136 cards.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` completes successfully
- data/generated/cards.json contains oracleText in card objects
- next.config.ts references cards.scryfall.io
- globals.css contains --color-accent variables
</verification>

<success_criteria>
Card data model includes oracleText, enrichment extracts it from Scryfall (with DFC handling), cards.json is regenerated with oracle text, next/image is configured for Scryfall images, and accent colors are defined in the theme.
</success_criteria>

<output>
After completion, create `.planning/phases/02-card-catalog/02-01-SUMMARY.md`
</output>
