---
phase: 17-parser-etched
plan: 01
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - src/lib/binder-name.ts
  - src/lib/__tests__/binder-name.test.ts
  - src/lib/types.ts
  - src/lib/csv-parser.ts
  - src/lib/__tests__/csv-parser-content.test.ts
  - src/lib/enrichment.ts
  - src/lib/__tests__/enrichment-progress.test.ts
  - src/db/queries.ts
  - src/db/__tests__/queries.test.ts
  - src/db/seed.ts
  - src/db/__tests__/seed.test.ts
  - src/db/__tests__/replace-all-cards.test.ts
  - src/lib/store/filter-store.ts
  - src/components/card-tile.tsx
  - src/components/card-modal.tsx
  - src/components/cart-item.tsx
  - src/components/filter-rail.tsx
  - src/app/api/admin/export/route.ts
  - src/app/api/admin/export/__tests__/route.test.ts
  - src/app/api/admin/import/__tests__/preview.test.ts
  - src/app/api/admin/import/__tests__/commit.test.ts
autonomous: true
requirements:
  - CSV-05
  - CSV-06
  - CSV-07
  - CSV-08
must_haves:
  truths:
    - "ManaboxRow accepts optional 'Binder Name' and 'Binder Type' columns; legacy exports without these columns parse with binder defaulting to 'unsorted' and binderType defaulting to 'binder' (D-02, CSV-05)"
    - "rowToCardOrSkip skips rows where Binder Type != 'binder' with SkippedRow.reason = 'non-binder row' (D-04, CSV-06)"
    - "rowToCardOrSkip skips rows where Quantity === 0 with SkippedRow.reason = 'zero quantity' (D-05)"
    - "Binder names are normalized at parse time via the SHARED helper normalizeBinderName(raw: string): string exported from src/lib/binder-name.ts (Phase 19 picker UI consumes the same helper); the algorithm is exactly trim().toLowerCase().replace(/\\s+/g, ' ').replace(/-/g, '_') (D-03, CSV-07)"
    - "Composite id format is the 5-segment ${setCode}-${collectorNumber}-${finish}-${condition}-${binder} where finish is the literal enum value 'normal' | 'foil' | 'etched' (D-06, matches Phase 16 D-05)"
    - "Card.finish replaces Card.foil throughout the application; the foil-derived shim in src/db/queries.ts rowToCard is REMOVED (no backward-compat getter, no virtual property — D-07)"
    - "Etched rows from Manabox (Foil column literal 'etched', verified D-01 against the operator's 12,749-row export) parse to finish = 'etched', distinct from normal and foil for the same (setCode, collectorNumber, condition, binder) tuple — no PK collision, no silent merge (CSV-08, Pitfall 7)"
    - "Scryfall price selection in src/lib/enrichment.ts picks usd_etched for finish === 'etched', usd_foil for finish === 'foil', usd for finish === 'normal' — with documented fallback ladders for each (Phase 17 D-08, fixes the v1.2 latent etched mispricing)"
    - "Display surfaces (card-tile, card-modal, cart-item, filter-rail) read card.finish directly and render Normal (blank label or 'Nonfoil' for the modal Finish field) / Foil (existing yellow/ink pill) / Etched (new bg-purple-200 text-purple-900 pill) (D-08, D-09)"
    - "filter-store.ts and filter-rail.tsx Finish facet is extended to three checkboxes (Normal / Foil / Etched), keyed by the literal finish enum values, so etched cards are findable in the storefront facet"
    - "src/app/api/admin/export/route.ts CSV export emits the 3-value 'normal' | 'foil' | 'etched' literal in the Foil column directly from row.finish — the Phase 16 transitional 'foil OR etched -> foil' coercion is removed (resolves Phase 16 SUMMARY deferred item)"
  artifacts:
    - path: "src/lib/binder-name.ts"
      provides: "Shared normalizeBinderName helper exported for parser (Phase 17) and picker UI (Phase 19)"
      exports: ["normalizeBinderName"]
      contains: "trim, toLowerCase, whitespace collapse, hyphen-to-underscore"
    - path: "src/lib/__tests__/binder-name.test.ts"
      provides: "Pure-function coverage for normalizeBinderName edge cases"
      contains: "trim, lowercase, whitespace collapse, hyphen replacement, multilingual preservation"
    - path: "src/lib/types.ts"
      provides: "Updated ManaboxRow (+ Binder Name, + Binder Type optional), Finish type alias, updated Card with finish replacing foil"
      contains: "Finish = 'normal' | 'foil' | 'etched'; Card.finish: Finish; Card.binder: string"
    - path: "src/lib/csv-parser.ts"
      provides: "rowToCardOrSkip extended for binder + finish + skip branches; 5-segment composite id; SkippedRow reasons added"
      contains: "Binder Type skip, Quantity 0 skip, normalizeBinderName call, etched literal handling, 5-segment id"
    - path: "src/lib/__tests__/csv-parser-content.test.ts"
      provides: "Extended parser tests covering the eight CONTEXT D-12 scenarios as inline-CSV cases"
      contains: "etched-row, multi-binder-same-card, non-binder-skip, name-normalization, zero-quantity-skip, multilingual-binder, mixed-foil-values, legacy-no-binder-columns"
    - path: "src/lib/enrichment.ts"
      provides: "getPrice rewritten to take finish: Finish; Scryfall price ladder per finish; enrichCards passes card.finish"
      contains: "finish === 'etched' ? usd_etched ladder, finish === 'foil' ? usd_foil ladder, else usd ladder"
    - path: "src/db/queries.ts"
      provides: "rowToCard returns finish + binder directly; Phase 16 foil shim REMOVED"
      contains: "finish: row.finish, binder: row.binder"
    - path: "src/db/seed.ts"
      provides: "cardToRow reads card.finish + card.binder directly (no derivation)"
      contains: "finish: card.finish, binder: card.binder"
    - path: "src/lib/store/filter-store.ts"
      provides: "Finish facet keyed by 'normal' | 'foil' | 'etched' (extended from 'foil' | 'nonfoil')"
      contains: "selectedFinishes: Set<Finish>; filter compares card.finish"
    - path: "src/components/{card-tile,card-modal,cart-item,filter-rail}.tsx"
      provides: "All four display surfaces read card.finish; etched gets a distinct pill (bg-purple-200 text-purple-900)"
      contains: "card.finish === 'etched' branches, badge styling"
    - path: "src/app/api/admin/export/route.ts"
      provides: "CSV export emits 3-value finish literal from row.finish (removes Phase 16 transitional coercion)"
      contains: "row.finish in the Foil column"
---

<objective>
Land the v1.3 parser fix: extend the Manabox CSV parser to ingest `Binder Name` + `Binder Type`, normalize binder names through a shared helper, skip non-binder and zero-quantity rows with named reasons, emit `finish: 'normal' | 'foil' | 'etched'` (fixing the v1.2 etched-as-normal mispricing), produce 5-segment composite ids, and rip the legacy `card.foil: boolean` field out of the entire codebase — replacing it with `card.finish` directly at every reader (parser, queries, seed, enrichment, store, four display components, admin export, and all tests). Ship in the same v1.3 PR as Phase 16's schema migration.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-parser-etched/17-CONTEXT.md
@.planning/phases/17-parser-etched/17-DISCUSSION-LOG.md
@.planning/phases/16-schema-migration/16-CONTEXT.md
@.planning/phases/16-schema-migration/16-01-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/research/SUMMARY.md
</execution_context>

<context>
@src/lib/csv-parser.ts
@src/lib/types.ts
@src/lib/import-contract.ts
@src/lib/enrichment.ts
@src/lib/store/filter-store.ts
@src/lib/__tests__/csv-parser-content.test.ts
@src/lib/__tests__/enrichment-progress.test.ts
@src/db/schema.ts
@src/db/queries.ts
@src/db/seed.ts
@src/db/__tests__/seed.test.ts
@src/db/__tests__/queries.test.ts
@src/db/__tests__/replace-all-cards.test.ts
@src/components/card-tile.tsx
@src/components/card-modal.tsx
@src/components/cart-item.tsx
@src/components/filter-rail.tsx
@src/app/api/admin/export/route.ts
@src/app/api/admin/export/__tests__/route.test.ts
@src/app/api/admin/import/__tests__/preview.test.ts
@src/app/api/admin/import/__tests__/commit.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: normalizeBinderName helper + unit tests (red → green)</name>
  <files>
    src/lib/binder-name.ts,
    src/lib/__tests__/binder-name.test.ts
  </files>
  <read_first>
    .planning/phases/17-parser-etched/17-CONTEXT.md,
    src/lib/__tests__/csv-parser-content.test.ts
  </read_first>
  <behavior>
    - New module `src/lib/binder-name.ts` exports `normalizeBinderName(raw: string): string`.
    - Algorithm (D-03, CSV-07) applied IN THIS EXACT ORDER:
      1. `String(raw)` (defensive coerce — PapaParse may hand back numbers if `dynamicTyping: true` ever sees a numeric label like `"02"` get auto-typed to `2`)
      2. `.trim()`
      3. `.toLowerCase()`
      4. `.replace(/\s+/g, ' ')` — collapse internal runs of whitespace to single space
      5. `.replace(/-/g, '_')` — replace hyphens with underscores so the Phase 20 cart-key segment-strip (hyphen-separated id) stays unambiguous
    - Empty input (`""`, `null`, `undefined`, all-whitespace) returns the literal `'unsorted'` (the same default the parser applies when the `Binder Name` column is missing entirely — D-02). This makes the helper safe for the picker UI (Phase 19) too: a buyer who deletes the binder name in the picker doesn't accidentally key cards under an empty string.
    - Multilingual / accented characters PRESERVED: `"compré titán"` stays `"compré titán"` after normalization. Lowercase uses default JS `toLowerCase()` (locale-independent; sufficient for the operator's known binder set per D-12 fixture #6).
    - Pure function, no side effects, no dependencies. Importable from both server (parser) and client (Phase 19 picker UI).
    - Tests cover at minimum:
      a. `'A07'` → `'a07'` (lowercase)
      b. `'A07 '` → `'a07'` (trailing trim)
      c. `' A07'` → `'a07'` (leading trim)
      d. `'A  07'` → `'a 07'` (internal whitespace collapse)
      e. `'A-07'` → `'a_07'` (hyphen → underscore)
      f. `'A-02'` and `'a02'` and `'A02 '` and `'A02'` all produce DIFFERENT canonical forms vs each other in the obvious way: hyphenated → `'a_02'`, others → `'a02'` (CONTEXT D-12 fixture 4 says `"A02"`, `"A02 "`, `"a02"`, `"A-02"` all collapse to `a_02` — the hyphenated input collapses to `a_02`, the non-hyphenated inputs collapse to `a02`; document this asymmetry in the test name and rely on the parser-level fixture to prove buyer-visible behavior. RECONCILIATION OF CONTEXT TEXT: the CONTEXT bullet's "all collapse to canonical binder `a_02`" sentence is imprecise — only the hyphenated input grows the underscore. The parser fixture must split the four inputs into two equivalence classes (`a02` ×3 and `a_02` ×1). See Task 4 for the corrected fixture.)
      g. `'compré titán'` → `'compré titán'` (multilingual preserved through lowercase + trim)
      h. `''`, `'   '`, `null as unknown as string`, `undefined as unknown as string` → all return `'unsorted'`
      i. A binder name containing both hyphens and spaces: `'  Lord Of The - Rings  '` → `'lord of the _ rings'` (whitespace collapses around the hyphen-replacement; documented as the canonical behavior — the operator's known label `"Lord Of The Rings"` has no hyphens so this is an edge case)
    - Time-box: 30 minutes. The helper is ~10 lines.
  </behavior>
  <action>
    1. Create `src/lib/binder-name.ts` with the exported `normalizeBinderName` function and a top-of-file JSDoc that:
       - Names the two callers (Phase 17 parser, Phase 19 picker UI)
       - Documents the algorithm in order
       - Explains the hyphen-to-underscore choice (cart-key segment-strip safety)
       - Calls out the empty-input → `'unsorted'` default
    2. Create `src/lib/__tests__/binder-name.test.ts` with the cases above as separate `it()` blocks. Use vitest's `describe.each` for the table-driven cases (a–g, i) and individual `it` for the empty-input cases (h).
    3. Run the tests; confirm they pass.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/binder-name.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All test cases above pass.
    - `npx tsc --noEmit` is green.
    - The helper is exported as a named export `normalizeBinderName` (default export prohibited; named exports keep the import call site readable in the parser and the picker).
    - The fixture-asymmetry note (case f) is captured in either a test comment or a JSDoc remark on the function so a future reader doesn't re-litigate the CONTEXT wording.
  </acceptance_criteria>
  <done>Shared helper exists, is unit-tested, and is ready for both the parser (Task 4) and the Phase 19 picker UI to consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Type updates — ManaboxRow, Finish, Card (red — downstream tests fail)</name>
  <files>
    src/lib/types.ts
  </files>
  <read_first>
    src/lib/types.ts,
    src/db/schema.ts,
    .planning/phases/17-parser-etched/17-CONTEXT.md
  </read_first>
  <behavior>
    - `src/lib/types.ts` edits (in declaration order):
      1. Add a top-level type alias: `export type Finish = "normal" | "foil" | "etched";`. Co-locate immediately above `ManaboxRow` so both types reference each other.
      2. `ManaboxRow.Foil` widens from `"foil" | "normal"` to `Finish` (`"normal" | "foil" | "etched"`). The literal `"etched"` is verified per D-01 against the operator's real CSV.
      3. Add `"Binder Name"?: string` and `"Binder Type"?: string` as OPTIONAL fields on `ManaboxRow` (D-02; older Manabox exports lack these columns).
      4. `Card.foil: boolean` is REMOVED. Replace with `finish: Finish` and `binder: string`.
         - Update the JSDoc above `Card.id` to read: ``/** Composite key: `${setCode}-${collectorNumber}-${finish}-${condition}-${binder}` */`` (5-segment, matches Phase 16 D-05).
         - Add JSDoc above `finish`: `/** Card finish — drives Scryfall price selection and display badge. */`
         - Add JSDoc above `binder`: `/** Normalized binder name (lowercase, whitespace-collapsed, hyphens→underscores). Defaults to 'unsorted' for legacy or pre-binder-aware imports. See src/lib/binder-name.ts. */`
    - `ScryfallCard.prices.usd_etched` already exists (line 91 per CONTEXT canonical_refs); no change there.
    - DO NOT add a backward-compat `foil?: boolean` derived getter, virtual property, or computed accessor anywhere. The CONTEXT D-07 ("RIP IT OUT cleanly") and the deferred-ideas section ("Backward-compat foil derived getter — explicitly rejected") are explicit on this point.
  </behavior>
  <action>
    Edit `src/lib/types.ts` per the spec above. After saving, run `npx tsc --noEmit` and CAPTURE the failing call sites (expected: csv-parser, enrichment, queries, seed, four display components, filter-store, admin export, and tests for each). Tasks 3 onward fix each cluster.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | tee /tmp/17-task2-tsc.log; echo "Failing files (expected red):"; grep -oE "src/[^:]+" /tmp/17-task2-tsc.log | sort -u</automated>
  </verify>
  <acceptance_criteria>
    - `Finish` type alias exists.
    - `ManaboxRow.Foil` accepts `"etched"`; the new optional binder fields exist.
    - `Card.foil` is gone; `Card.finish` and `Card.binder` exist.
    - `npx tsc --noEmit` is RED at this point — that's the contract; the failing call sites are the work backlog for Tasks 3–7. The captured list of failing files is the work checklist.
  </acceptance_criteria>
  <done>The type contract for the rest of the phase is locked. Every downstream task is now mechanical: a failing tsc error → a fix → green again at the end of Task 7.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Parser — extend ManaboxRow consumption + skip branches + 5-segment id (red → green for parser tests only)</name>
  <files>
    src/lib/csv-parser.ts,
    src/lib/__tests__/csv-parser-content.test.ts
  </files>
  <read_first>
    src/lib/csv-parser.ts,
    src/lib/binder-name.ts,
    src/lib/__tests__/csv-parser-content.test.ts,
    .planning/phases/17-parser-etched/17-CONTEXT.md
  </read_first>
  <behavior>
    - `rowToCardOrSkip` rewrite (preserves the existing skip-on-missing-required-field branches above; the changes below extend it):
      a. Compute `quantity = row.Quantity ?? 0` early. If `quantity === 0`, return `{ skipped: { rowNumber, reason: 'zero quantity', name, setCode: bestEffortSetCode, collectorNumber: bestEffortCollectorNumber } }`. (D-05)
      b. Compute `binderType = row["Binder Type"] ?? 'binder'`. If `binderType !== 'binder'`, return `{ skipped: { rowNumber, reason: 'non-binder row', name, setCode: bestEffortSetCode, collectorNumber: bestEffortCollectorNumber } }`. The check is case-sensitive against the literal lowercase `'binder'` (Manabox emits lowercase per CONTEXT; non-binder values like `'deck'`, `'list'` skip). (D-04)
      c. Compute `finish: Finish = row.Foil`. The Manabox Foil column literal is one of `'normal' | 'foil' | 'etched'` (D-01). Apply a defensive runtime guard:
         - If `row.Foil` is one of the three literals, use it directly.
         - If `row.Foil` is anything else (legacy CSV with old values, missing column, typo): default to `'normal'` and DO NOT skip (the row still has a name + setCode + collectorNumber + condition; mis-coding finish is a soft error). Log a one-line `console.warn` to surface unexpected values during dev; production runs through PapaParse warnings already.
         - Co-locate the guard with a comment explaining the D-01 verification.
      d. Compute `binderRaw = row["Binder Name"]`. Pass it through `normalizeBinderName(binderRaw ?? '')` (the helper returns `'unsorted'` for empty input, satisfying the D-02 default).
      e. Compute `condition = row.Condition || 'unknown'` (existing behavior, unchanged).
      f. Build the 5-segment composite id: `` `${setCode}-${collectorNumber}-${finish}-${condition}-${binder}` `` (D-06).
      g. Return `{ card: { id, name, setCode, setName, collectorNumber, price: null, condition, quantity, colorIdentity: [], imageUrl: null, oracleText: null, rarity, finish, binder } }`. The `foil` field is GONE from the output (Card.foil no longer exists).
    - The line-87 bug (`const foil = row.Foil === "foil"`) is DELETED in the same edit. The line-91 id template literal is DELETED and replaced.
    - `mergeCards` does NOT need restructuring. Its dedup key is `card.id`, and the new id includes binder + finish, so the merge automatically does the right thing (per CONTEXT code_context: "mergeCards function automatically does the right thing once binder is part of composite id"). Update its JSDoc to read "Duplicates are identified by the composite ID (set-collector-finish-condition-binder)" (the comment on line 139 currently says "set-collector-foil-condition" — the wording rolls forward).
    - `parseSingleCsv` (build-time path) and `parseManaboxCsvContent` / `parseManaboxCsvContents` (admin import path) consume the same `rowToCardOrSkip`; no behavior change in those wrappers, just the new card shape flows through them.
    - **Parser test extensions** in `src/lib/__tests__/csv-parser-content.test.ts`. Established convention is **inline CSV strings** (not a separate fixtures dir) — confirmed by reading the existing file. CONTEXT D-12's "fixtures matrix" maps to ONE `it()` per scenario, each building its CSV inline via the existing `makeCsv` helper or a small extension that adds the new columns. Mapping:

      1. **`'parses Foil="etched" as finish="etched" with a 5-segment id distinct from normal/foil twin'`** (D-12 fixture 1, CSV-08, Pitfall 7) — three rows for the same `(setCode, collectorNumber, condition, binder)`: one `Foil="normal"`, one `Foil="foil"`, one `Foil="etched"`. Assert all three cards exist in `result.cards`, with three distinct `id` values, all 5-segment, all sharing the same first three header-derived segments.

      2. **`'same setCode+collectorNumber+finish+condition in two binders produces two distinct rows'`** (D-12 fixture 2) — two rows with `Binder Name` `"a01"` and `"a02"`, otherwise identical. Assert `result.cards.length === 2` and the two ids end in `-a01` and `-a02`.

      3. **`'skips Binder Type="deck" and Binder Type="list" rows with reason "non-binder row"'`** (D-12 fixture 3, CSV-06, D-04) — two skip rows (one `Binder Type="deck"`, one `Binder Type="list"`) plus one valid row. Assert `result.cards.length === 1`, `result.skippedRows.length === 2`, both skip entries have `reason === 'non-binder row'`, and `rowNumber` values match the spreadsheet positions (2 and 3 if both skips precede the valid row).

      4. **`'normalizes binder names: "A02" / "A02 " / "a02" all share id; "A-02" produces a different id'`** (D-12 fixture 4, CSV-07) — four rows with the four binder name variants from CONTEXT D-12. Assert: ids ending in `-a02` total 1 (because `mergeCards` collapses the three identical-after-normalization rows into one, summing `Quantity`), and one id ends in `-a_02` (the hyphenated variant). The summed `Quantity` of the `-a02` row equals the sum of the three input rows' quantities. (This concretely proves the CONTEXT D-12 wording asymmetry, with a comment cross-referencing Task 1 case f.)

      5. **`'skips Quantity=0 rows with reason "zero quantity"'`** (D-12 fixture 5, D-05) — one `Quantity=0` row plus one valid row. Assert one card, one skip with `reason === 'zero quantity'`.

      6. **`'preserves multilingual binder name through normalization'`** (D-12 fixture 6) — one row with `Binder Name="Compré Titán"`. Assert the resulting card's `binder === 'compré titán'` and the id ends in `-compré titán` (yes, with the space — internal whitespace is collapsed but a single internal space survives).

      7. **`'mixed normal/foil/etched in one CSV produces three cards with three finish values'`** (D-12 fixture 7) — three rows differing only by `Foil` value. Assert each card's `finish` matches the expected literal and the three ids are distinct.

      8. **`'legacy export without Binder Name / Binder Type columns defaults binder=unsorted and Binder Type defaults to binder'`** (D-12 fixture 8, D-02 graceful degradation) — CSV using the original 8-column header (no `Binder Name`, no `Binder Type`). Assert all rows parse, each card's `binder === 'unsorted'`, and the id ends in `-unsorted`.

    - **Existing test updates** in the same file:
      - Test A ("parses a fully valid CSV into Card[] with correct composite IDs and no skippedRows") — the expected card objects on lines 39–69 must drop `foil` and add `finish` + `binder`. Ids change from `"lea-232-normal-near_mint"` to `"lea-232-normal-near_mint-unsorted"` (5-segment) and from `"mh2-45-foil-lightly_played"` to `"mh2-45-foil-lightly_played-unsorted"`.
      - Any other `expect(result.cards[N]).toEqual({...foil: ...})` assertions in the file — same rewrite (drop `foil`, add `finish` and `binder: 'unsorted'`).
      - The `makeCsv` helper extends to support `"Binder Name"` and `"Binder Type"` columns (when those keys appear in the test's row dict). Order: keep the existing 8 headers, then append the two new headers IF any row provides one of them; this keeps Test A and other legacy tests untouched while the new tests opt in by including those keys in their row dicts.
  </behavior>
  <action>
    1. Edit `src/lib/csv-parser.ts` per the spec.
    2. Update the `mergeCards` JSDoc for the new composite id wording.
    3. Extend the test file with the eight new `it()` blocks above. Update Test A and any other affected legacy assertions to the new card shape.
    4. Extend `makeCsv` to accept the two new columns optionally.
    5. Run the parser tests and the typechecker to confirm green for THIS file's tests (other files will still be red until Tasks 4–7 fix them).
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/csv-parser-content.test.ts &amp;&amp; npx vitest run src/lib/__tests__/binder-name.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All parser tests pass (the eight new + the updated existing).
    - Composite id is 5-segment in every produced card.
    - SkippedRow uses the new reasons exactly as specified.
    - The CONTEXT D-12 fixture-4 wording asymmetry is explicitly proven by test 4 above and documented in a code comment.
    - `Card.foil` does NOT appear in any expected-object literal in this file.
  </acceptance_criteria>
  <done>Parser is fully migrated; the eight CONTEXT scenarios are pinned by tests; the v1.2 etched bug is corrected at the parser layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Database layer — rip out the Phase 16 foil-derived shim</name>
  <files>
    src/db/queries.ts,
    src/db/seed.ts,
    src/db/__tests__/seed.test.ts,
    src/db/__tests__/queries.test.ts,
    src/db/__tests__/replace-all-cards.test.ts
  </files>
  <read_first>
    src/db/queries.ts,
    src/db/seed.ts,
    src/db/__tests__/seed.test.ts,
    src/db/__tests__/queries.test.ts,
    src/db/__tests__/replace-all-cards.test.ts,
    .planning/phases/16-schema-migration/16-01-SUMMARY.md
  </read_first>
  <behavior>
    - `src/db/queries.ts` `rowToCard` (Phase 16 deviation cleanup, D-07):
      - REMOVE the `foil: row.finish === "foil"` line.
      - ADD `finish: row.finish, binder: row.binder` (typed as `Finish` and `string`).
      - Update the JSDoc above the function to remove the "Phase 16 schema migration" backward-compat block; replace with a one-paragraph note: "Maps a Drizzle cards row to the application Card shape. The DB columns map 1:1 to the application fields after the v1.3 migration (binder + finish enum). See `src/lib/types.ts` for the Card contract."
    - `src/db/seed.ts` `cardToRow`:
      - REMOVE the `(card.foil ? "foil" : "normal")` derivation.
      - ADD `finish: card.finish` and replace the hard-coded `binder: "unsorted"` with `binder: card.binder` (the parser now plumbs the real binder through; for legacy callers that build a Card by hand without a binder, TypeScript will surface the missing field at compile time — that's the intended contract).
      - Update the JSDoc to remove the Phase 16 transitional note; replace with a brief note that Card and the DB row are now 1:1 on finish and binder.
    - **Test updates** (seed, queries, replace-all-cards):
      - In `src/db/__tests__/seed.test.ts`: the `makeCard` factory currently sets `foil: false`. Change to `finish: "normal", binder: "unsorted"`. The test "derives finish='foil' when card.foil is true (Phase 16 D-07 backfill)" becomes "passes through card.finish='foil' to row.finish" (no derivation now). The test "maps all Card fields to row fields (Phase 16: foil -> finish, +binder)" updates its expected `binder` assertion to read from the input card's binder, not the hard-coded default.
      - In `src/db/__tests__/queries.test.ts`: the `makeCard` factory at line 33 (`foil: false`) → `finish: "normal", binder: "unsorted"`. Any test asserting `card.foil` becomes assertions against `card.finish`. Add one new test: "rowToCard passes through binder column to Card.binder" — assert that a row with `binder: 'a07'` produces `card.binder === 'a07'`.
      - In `src/db/__tests__/replace-all-cards.test.ts`: the `makeCard` factory at line 43 (`foil: false`) → `finish: "normal", binder: "unsorted"`. The assertion at line 123 (`{ id: "mh2-45-foil-lightly_played" }`) becomes `{ id: "mh2-45-foil-lightly_played-unsorted" }` (5-segment).
    - DO NOT touch `replaceAllCards` itself in this phase — Phase 19 will rename it to `replaceCardsForBinders` and add the per-binder scoped delete (CONTEXT integration_points). This phase only migrates the row-shape.
    - The `Card.foil` references in `src/db/__tests__/queries.test.ts` line 97 (`foil: false` in another factory or fixture) and any other lines flagged by tsc are mechanical: drop `foil`, add `finish` + `binder` with sensible defaults.
  </behavior>
  <action>
    1. Edit `src/db/queries.ts` `rowToCard` and its JSDoc.
    2. Edit `src/db/seed.ts` `cardToRow` and its JSDoc.
    3. Update the three test files' `makeCard` factories and any per-test assertions.
    4. Add the new "binder passthrough" test in `queries.test.ts`.
    5. Run the affected tests and tsc; confirm DB-layer tests are green.
  </action>
  <verify>
    <automated>npx vitest run src/db/__tests__/seed.test.ts src/db/__tests__/queries.test.ts src/db/__tests__/replace-all-cards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rowToCard` returns `finish` + `binder`; no derived `foil` field.
    - `cardToRow` reads `card.finish` + `card.binder` directly; no derivation.
    - All three DB-layer test files are green.
    - The Phase 16 SUMMARY's deferred item "Card type still carries `foil: boolean` … deferred to Phase 17" is now resolved (the JSDoc no longer references it).
  </acceptance_criteria>
  <done>The Phase 16 transitional shim is gone. The DB-to-Card mapping is 1:1 on the new fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Enrichment — Scryfall price ladder per finish (fix the etched mispricing)</name>
  <files>
    src/lib/enrichment.ts,
    src/lib/__tests__/enrichment-progress.test.ts
  </files>
  <read_first>
    src/lib/enrichment.ts,
    src/lib/__tests__/enrichment-progress.test.ts,
    src/lib/types.ts
  </read_first>
  <behavior>
    - `getPrice(prices: ScryfallCard["prices"], finish: Finish): number | null` (rename the second parameter from `foil: boolean` to `finish: Finish`).
    - Selection ladder (extends D-08 from the CONTEXT bullets):
      - `finish === 'etched'`: `prices.usd_etched ?? prices.usd_foil ?? prices.usd` (etched cards prefer their dedicated price; fall back to foil since etched is structurally a foil treatment; final fallback to non-foil).
      - `finish === 'foil'`: `prices.usd_foil ?? prices.usd_etched ?? prices.usd` (preserve the existing v1.2 foil-first fallback ladder verbatim).
      - `finish === 'normal'`: `prices.usd ?? prices.usd_foil ?? prices.usd_etched` (preserve the existing v1.2 normal-first fallback ladder verbatim).
    - Update the JSDoc: replace the "Foil rows pull usd_foil first … non-foil rows pull usd first" wording with a 3-branch description matching the new function. Call out that the etched fix is the v1.2 latent bug correction — pre-Phase 17, etched cards (treated as `foil: false`) silently took the `prices.usd` (non-foil) price, which is wrong for the 11 etched cards in the operator's collection.
    - In `enrichCards`, change the call site from `getPrice(scryfallData.prices, card.foil)` to `getPrice(scryfallData.prices, card.finish)`.
    - **Test extensions** in `src/lib/__tests__/enrichment-progress.test.ts`:
      - The `makeCard` helper at line 18 currently sets `foil: false`. Update to `finish: "normal", binder: "unsorted"`. Any test that overrides `foil: true` becomes `finish: "foil"` (e.g., lines 147, 167, 168). The composite ids in tests (`"lea-232-foil-near_mint"`, `"lea-1-foil-near_mint"`, etc.) change to 5-segment ending in `-unsorted`.
      - The two existing tests "foil cards prefer usd_foil over usd" and "foil cards fall back to usd_etched then usd when usd_foil missing" stay valid for `finish === 'foil'` rows; rename them to "foil finish prefers usd_foil over usd" and "foil finish falls back to usd_etched then usd when usd_foil missing" for clarity.
      - ADD a new test: **"etched finish prefers usd_etched over usd_foil and usd"** — fixture card with `finish: 'etched'`, Scryfall response `{ usd: '1.00', usd_foil: '5.00', usd_etched: '8.00' }`; assert resulting `card.price === 8.00`.
      - ADD a new test: **"etched finish falls back to usd_foil then usd when usd_etched missing"** — fixture card with `finish: 'etched'`, Scryfall response `{ usd: '1.00', usd_foil: '5.00', usd_etched: null }`; assert `card.price === 5.00`. Then second card with `{ usd: '1.00', usd_foil: null, usd_etched: null }`; assert `card.price === 1.00`.
      - The "non-foil cards still prefer usd over usd_foil" test (line 175) becomes "normal finish prefers usd over usd_foil and usd_etched"; the inputs/outputs are unchanged conceptually, only the fixture card's `finish` field changes shape.
  </behavior>
  <action>
    1. Edit `src/lib/enrichment.ts` per the spec.
    2. Update `src/lib/__tests__/enrichment-progress.test.ts` `makeCard` and per-test card constructions; add the two new etched tests.
    3. Run the enrichment tests; confirm green.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/enrichment-progress.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `getPrice` takes `finish: Finish` and selects per the three-branch ladder above.
    - Etched cards prefer `usd_etched`; the fix to the v1.2 mispricing is pinned by the new tests.
    - All enrichment-progress tests are green.
  </acceptance_criteria>
  <done>The 11 etched cards in the operator's collection (Wrath of God, Cultist of the Absolute, etc., per CONTEXT specifics) will now display the correct etched price after the next import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Display surfaces — card-tile, card-modal, cart-item, filter-rail (etched badge + finish facet)</name>
  <files>
    src/components/card-tile.tsx,
    src/components/card-modal.tsx,
    src/components/cart-item.tsx,
    src/components/filter-rail.tsx,
    src/lib/store/filter-store.ts
  </files>
  <read_first>
    src/components/card-tile.tsx,
    src/components/card-modal.tsx,
    src/components/cart-item.tsx,
    src/components/filter-rail.tsx,
    src/lib/store/filter-store.ts
  </read_first>
  <behavior>
    - **`src/components/card-tile.tsx`** (lines 77–94, the absolute-positioned FOIL pill):
      - Replace `{card.foil && (...FOIL pill...)}` with a switch on `card.finish`:
        - `'normal'`: render nothing.
        - `'foil'`: render the existing pill verbatim (background `var(--ink)`, color `var(--bg)`, label `FOIL`).
        - `'etched'`: render a sibling pill with the same positioning + typography but distinct color: `background: '#e9d5ff'` (Tailwind `bg-purple-200`) and `color: '#581c87'` (Tailwind `text-purple-900`), label `ETCHED`. The component currently uses inline `style={{}}` (per its existing pill at lines 78–93), so the etched pill follows the same inline-style pattern — do NOT introduce Tailwind classes here unless the file already uses them. Keep the implementation consistent with the file's prevailing style. (D-09)
      - Refactor: extract a tiny inline helper `function FinishPill({ finish }: { finish: Finish })` ABOVE the `CardTile` component declaration to keep the JSX clean. The helper returns `null` for `'normal'` and the appropriate pill JSX for the other two values.
    - **`src/components/card-modal.tsx`** (line 263, the Finish dt/dd pair):
      - Replace `{card.foil ? "Foil" : "Nonfoil"}` with `{card.finish === 'etched' ? 'Etched' : card.finish === 'foil' ? 'Foil' : 'Nonfoil'}`.
      - Optionally render the etched value with the matching purple ink color (`color: '#581c87'`) so the Finish row visually echoes the storefront pill — keep the styling minimal (one inline color rule), do not introduce a separate badge in the modal.
    - **`src/components/cart-item.tsx`** (line 191, the set-line suffix):
      - Replace `{card.foil ? " · Foil" : ""}` with `{card.finish === 'etched' ? ' · Etched' : card.finish === 'foil' ? ' · Foil' : ''}`. Preserve the leading ` · ` separator for both non-normal cases.
    - **`src/lib/store/filter-store.ts`** (lines 35, 47, 159, and any related reducer paths):
      - The `Finish` filter facet extends from two values (`'foil' | 'nonfoil'`) to three (`'normal' | 'foil' | 'etched'`), keyed by the literal enum values from `Finish`. Update the `selectedFinishes: Set<string>` type to `Set<Finish>` (import `Finish` from `@/lib/types`).
      - Filter logic at line 157–162: the comparator becomes `selectedFinishes.has(card.finish)` directly (no more `card.foil ? 'foil' : 'nonfoil'` derivation).
      - Update any reset/initial-state logic accordingly (Set is still empty by default).
      - Update the JSDoc on line 35 (`Finish filter — "foil" | "nonfoil"`) to read `Finish filter — 'normal' | 'foil' | 'etched'`.
    - **`src/components/filter-rail.tsx`** (lines 707–724, the Finish FilterSection):
      - Replace the two existing checkboxes (Foil + Nonfoil) with three: Normal, Foil, Etched. Keys: `'normal'`, `'foil'`, `'etched'` matching the Finish enum.
      - Order: Normal first (most common), Foil second, Etched third (to match the visual prominence of the badge).
      - Each checkbox: `<Checkbox label="Normal" checked={selectedFinishes.has("normal")} onToggle={() => toggleFinish("normal")} />`, etc.
    - **Tests** — display components do not currently have unit tests in this repo (verified via `find src/components -name '*.test.*'` returning nothing). Do NOT create new test files for them; the assurance is `tsc --noEmit` + the parser/queries tests proving the data flows through correctly + the Task 8 build gate.
    - **filter-store.ts** has no existing test file either (verified). Same approach: rely on tsc + integration via the storefront page render.
  </behavior>
  <action>
    1. Edit each component file per the spec.
    2. Edit `filter-store.ts` to extend the facet to three values.
    3. Run `npx tsc --noEmit` to confirm display-layer compiles.
    4. Spot-check the new `FinishPill` helper renders as expected by reading the diff carefully (no DOM tests in scope per the existing convention).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `card-tile.tsx` renders the etched purple pill for `finish === 'etched'` cards.
    - `card-modal.tsx` Finish row shows `Etched` (with optional purple color).
    - `cart-item.tsx` set-line suffix shows ` · Etched` for etched cards.
    - `filter-store.ts` `selectedFinishes` is `Set<Finish>`; comparator reads `card.finish` directly.
    - `filter-rail.tsx` shows three Finish checkboxes (Normal, Foil, Etched).
    - `npx tsc --noEmit` is green for the entire display layer.
  </acceptance_criteria>
  <done>Buyers can see and filter etched cards as a distinct treatment; the Wrath of God / Cultist of the Absolute style etched listings now visibly differ from their normal/foil twins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Admin export route — emit 3-value finish literal (resolve Phase 16 deferred coercion)</name>
  <files>
    src/app/api/admin/export/route.ts,
    src/app/api/admin/export/__tests__/route.test.ts,
    src/app/api/admin/import/__tests__/preview.test.ts,
    src/app/api/admin/import/__tests__/commit.test.ts
  </files>
  <read_first>
    src/app/api/admin/export/route.ts,
    src/app/api/admin/export/__tests__/route.test.ts,
    src/app/api/admin/import/__tests__/preview.test.ts,
    src/app/api/admin/import/__tests__/commit.test.ts,
    .planning/phases/16-schema-migration/16-01-SUMMARY.md
  </read_first>
  <behavior>
    - **`src/app/api/admin/export/route.ts`** (lines 33, 44–49):
      - Header on line 33: replace `,Foil` with `,Finish` (the column now carries 3 values; the header name should match the source-of-truth field). This IS a breaking change to the CSV contract documented as deferred in Phase 16's SUMMARY ("CSV export header redesign with first-class etched support — deferred to Phase 17"). The downstream consumer is the operator (single user), and the import path can already read both `Foil` and a `Finish` column if Manabox exports one (the parser only consumes `Foil`; this admin export is a round-trip aid, not a Manabox-replacement).
        - **Rationale for breaking the header name now**: The Phase 16 deferred-item list calls this out as a Phase-17 task. The operator is the only consumer. A 3-value column under a header literally named `Foil` would be misleading.
      - Lines 44–49: replace the transitional ternary `row.finish === "foil" || row.finish === "etched" ? "foil" : "normal"` with `row.finish` directly. The CSV-escape helper handles it (it's already a plain string).
      - Remove the Phase 16 transitional comment block; replace with a one-line note: "Finish column emits the 3-value enum literal directly (Phase 17 D-08)."
    - **`src/app/api/admin/export/__tests__/route.test.ts`**:
      - The test card factories (lines 54, 66) currently use `id: "m21-001-foil-lightly_played-unsorted"` and `finish: "foil" as const`. The 5-segment id is already correct (Phase 16 set this up). Update any factory line that still has `foil: ...` to drop it (Card no longer has the field).
      - Update the test "renders foil as 'foil' and non-foil as 'normal'" (line 137) to match the new export contract:
        - Rename to "renders the 3-value finish enum literal in the Finish column".
        - First data row: `finish: "normal"` → `lines[1]` ends in `,normal`.
        - Second data row: `finish: "foil"` → `lines[2]` ends in `,foil`.
        - ADD a third data row with `finish: "etched"` and a corresponding `lines[3]` assertion ending in `,etched`.
      - Update the header-row assertion (if present) to expect `,Finish` in place of `,Foil`.
    - **`src/app/api/admin/import/__tests__/preview.test.ts`** and **`src/app/api/admin/import/__tests__/commit.test.ts`**:
      - The mock card factories (preview line 90, commit line 65) currently set `foil: false`. Update to `finish: "normal", binder: "unsorted"`. Any sample-card assertion that references `foil` becomes `finish`. Any sample id like `"mh2-45-foil-lightly_played"` (preview line 141) becomes `"mh2-45-foil-lightly_played-unsorted"` (5-segment, matching what the parser now produces).
    - The Phase 16 SUMMARY's deferred item "CSV export `Foil` header still says `Foil` even though `etched` rows export under it" is RESOLVED in this phase. Update the work item in the 17-01-SUMMARY (Task 9) to confirm.
  </behavior>
  <action>
    1. Edit `src/app/api/admin/export/route.ts` per the spec.
    2. Update the three test files' card factories and the export route test's per-row assertions.
    3. Run the affected tests and tsc; confirm green.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/export/__tests__/route.test.ts src/app/api/admin/import/__tests__/preview.test.ts src/app/api/admin/import/__tests__/commit.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The CSV export emits `,Finish` as the header.
    - Each data row's last column is the literal `normal`, `foil`, or `etched`.
    - All three test files are green.
    - `npx tsc --noEmit` is green at this point — every Card consumer in the codebase reads `card.finish` and `card.binder`; `card.foil` no longer exists anywhere in `src/`.
  </acceptance_criteria>
  <done>The admin export round-trips etched cards faithfully; the Phase 16 SUMMARY's transitional coercion is removed.</done>
</task>

<task type="auto">
  <name>Task 8: Whole-repo gate — tsc, vitest, build, foil-grep proof</name>
  <files>
    src/**
  </files>
  <read_first>
    .planning/phases/17-parser-etched/17-CONTEXT.md
  </read_first>
  <behavior>
    - The whole-repo gate proves the codebase is internally consistent after the seven-step rip-out.
    - The `foil`-grep proof is the strongest single signal that the cleanup is complete:
      - `grep -rn "card\\.foil\\|\\.foil\\b" src/ --include="*.ts" --include="*.tsx"` MUST return zero matches OUTSIDE of (a) comments documenting the historical change and (b) the `Foil:` PapaParse column key in `ManaboxRow` (which is the CSV header literal, not the Card field). The grep output is captured into the SUMMARY as evidence.
      - `grep -rn "foil:\\s*boolean\\|foil:\\s*true\\|foil:\\s*false" src/ --include="*.ts" --include="*.tsx"` MUST return zero matches anywhere — every test factory was rewritten in Tasks 4, 5, 6, 7.
    - Build verification: `npm run build` must succeed (Next.js compiles with the new types).
    - Test verification: `npm test` (full vitest run) must pass.
  </behavior>
  <action>
    1. Run `npx tsc --noEmit` and capture output.
    2. Run `npm test` and capture pass count + skip count.
    3. Run `npm run build` and capture the success line.
    4. Run the two grep commands above and capture the output (expected: zero matches except for documented exceptions).
    5. If any grep returns unexpected matches, FIX them (drop `foil` from the test factory or component) and rerun until zero. Do NOT add ad-hoc allowlists; the rule is "every reader of `card.foil` is rewritten."
    6. Capture all four artifacts (tsc, test pass count, build success, grep output) for the Task 9 SUMMARY.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; bash -c 'set -eo pipefail; OUT=$(grep -rn "card\\.foil\\|\\.foil\\b" src/ --include="*.ts" --include="*.tsx" | grep -v "Foil:" | grep -v "// " || true); if [ -n "$OUT" ]; then echo "FAIL: residual .foil readers:"; echo "$OUT"; exit 1; fi; OUT2=$(grep -rn "foil:\\s*boolean\\|foil:\\s*true\\|foil:\\s*false" src/ --include="*.ts" --include="*.tsx" || true); if [ -n "$OUT2" ]; then echo "FAIL: residual foil:boolean usages:"; echo "$OUT2"; exit 1; fi; echo "OK: no residual foil readers."'</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` is green.
    - `npm test` passes (no new failures vs the Phase 16 baseline).
    - `npm run build` succeeds.
    - The two `foil`-grep commands return zero non-comment, non-PapaParse-key matches.
    - The grep output is captured for the SUMMARY.
  </acceptance_criteria>
  <done>The codebase is verifiably free of the legacy `card.foil` field; every reader was migrated; build + tests are green end-to-end.</done>
</task>

<task type="checkpoint:verify" gate="blocking">
  <name>Task 9: Verify CONTEXT 12 decisions + SUMMARY</name>
  <files>
    .planning/phases/17-parser-etched/17-01-SUMMARY.md
  </files>
  <action>
    Walk the 12 CONTEXT decisions and confirm each is satisfied (D-01 etched literal verified ✓ — Tasks 3, 5, 7; D-02 ManaboxRow optional fields ✓ — Task 2; D-03 normalize algorithm ✓ — Task 1; D-04 non-binder skip ✓ — Task 3; D-05 zero-quantity skip ✓ — Task 3; D-06 5-segment id ✓ — Task 3; D-07 no foil shim ✓ — Task 4; D-08 sweep complete ✓ — Tasks 4–7; D-09 etched display ✓ — Task 6; D-10 ships with Phase 16 ✓ — git history; D-11 hand-crafted fixtures (inline CSVs) ✓ — Task 3; D-12 fixture matrix ✓ — Task 3).

    Create `.planning/phases/17-parser-etched/17-01-SUMMARY.md` with these sections:
    - **Decisions table**: 12 rows, one per CONTEXT decision, each with status + the task(s) that satisfied it + a one-line evidence pointer (file path or test name).
    - **Files modified**: ordered list of every file touched, with a one-line "what changed" per file.
    - **Test counts**: vitest pass count before vs after; net new tests added (count and brief description).
    - **`foil`-grep proof**: the captured output from Task 8's two grep commands (expected: empty, with a note about the allowed PapaParse `Foil:` key in `ManaboxRow`).
    - **Phase 16 deferred items resolved**: enumerate the items from Phase 16 SUMMARY's "Known limitations / deferred items" section that this phase resolves (Card type carrying foil — resolved Task 4; admin export Foil header — resolved Task 7).
    - **Known limitations / deferred items in Phase 17**:
      - The parser's `mergeCards` `binder` field is not yet split into `replaceCardsForBinders` — that's Phase 19's scope (CONTEXT integration_points).
      - The cart-key migration (hyphen-to-underscore segment safety) is realized at parse time (D-03) but the cart-side reconciler that strips legacy 4-segment ids → 5-segment ids is Phase 20's scope.
      - The binder picker UI consuming `normalizeBinderName` is Phase 19's scope (the helper is exported and ready).
      - "Did-you-mean" hint at import time for near-duplicate binders (CONTEXT deferred section, IMP-FUT-01) — explicitly deferred to v1.3.x.
      - The 11 etched cards in the operator's collection now display correctly AFTER the next CSV import + Scryfall enrichment (the existing DB rows still carry whatever `finish` they had post-Phase-16; Phase 16 backfilled all rows to `'normal'` or `'foil'` from the legacy boolean, so etched cards in the operator's CSV are first marked `'etched'` on the next import — call this out as the user-visible improvement from CONTEXT specifics).
    - **Cutover note**: Phase 17 ships in the same git PR / Vercel deploy as Phase 16's migration. The deployment sequence is: (1) `npm run migrate:v1.3 -- --dry-run` on Neon branch, (2) `npm run migrate:v1.3` on prod, (3) push v1.3 code (Phases 16 + 17 combined) to main, (4) Vercel deploys; (5) operator runs the next CSV import to populate `finish='etched'` rows from the `Foil="etched"` literal in their Manabox export.

    If any of the 12 decisions is NOT satisfied, list the gap and propose a follow-up task within Phase 17 — do NOT mark the phase complete.
  </action>
  <done>The 12 CONTEXT decisions are pinned; the SUMMARY captures evidence; deferred items are explicit.</done>
</task>

</tasks>

<verification>
- `git diff --check`
- `npx vitest run src/lib/__tests__/binder-name.test.ts`
- `npx vitest run src/lib/__tests__/csv-parser-content.test.ts`
- `npx vitest run src/lib/__tests__/enrichment-progress.test.ts`
- `npx vitest run src/db/__tests__/seed.test.ts src/db/__tests__/queries.test.ts src/db/__tests__/replace-all-cards.test.ts`
- `npx vitest run src/app/api/admin/export/__tests__/route.test.ts src/app/api/admin/import/__tests__/preview.test.ts src/app/api/admin/import/__tests__/commit.test.ts`
- `npx tsc --noEmit`
- `npm test`
- `npm run build`
- `grep -rn "card\\.foil\\|\\.foil\\b" src/ --include="*.ts" --include="*.tsx"` returns zero non-comment, non-`Foil:` (PapaParse key) matches
- `grep -rn "foil:\\s*boolean\\|foil:\\s*true\\|foil:\\s*false" src/ --include="*.ts" --include="*.tsx"` returns zero matches
</verification>

<success_criteria>
- [ ] CSV-05: ManaboxRow has optional `Binder Name` + `Binder Type` fields; parser ingests them; legacy CSVs without these columns parse with binder defaulting to `'unsorted'`
- [ ] CSV-06: Rows with `Binder Type != 'binder'` are skipped with `SkippedRow.reason = 'non-binder row'`; the count flows through `SkippedRow[]` for the import preview to surface
- [ ] CSV-07: Binder names are normalized via `normalizeBinderName()` exported from `src/lib/binder-name.ts`; algorithm is exactly `trim().toLowerCase().replace(/\s+/g, ' ').replace(/-/g, '_')`; the helper is consumable by Phase 19's picker UI
- [ ] CSV-08: `Foil = 'etched'` rows parse to `finish = 'etched'`; the composite id is distinct from both normal and foil twins for the same `(setCode, collectorNumber, condition, binder)`; the v1.2 latent bug at `csv-parser.ts:87` is gone
- [ ] D-06 5-segment composite id format `${setCode}-${collectorNumber}-${finish}-${condition}-${binder}` is produced by the parser
- [ ] D-07 No backward-compat `foil` derived getter or virtual property exists anywhere in `src/`; `card.foil` is unreachable
- [ ] D-08 Every grep-discovered `card.foil` reader is rewritten: csv-parser, types, enrichment, queries, seed, cart-item, card-tile, card-modal, filter-rail, filter-store, admin export, and all corresponding test factories
- [ ] D-09 Etched display shows `Etched` label with `bg-purple-200 text-purple-900` (or the inline-style equivalent) distinct from foil's existing color
- [ ] Scryfall price selection picks `usd_etched` for etched cards; the v1.2 etched-as-normal mispricing is corrected
- [ ] Phase 16 SUMMARY deferred items "Card type still carries foil" and "CSV export Foil header" are resolved
- [ ] Whole-repo `npx tsc --noEmit` + `npm test` + `npm run build` are green; the two `foil` grep commands return zero matches
</success_criteria>

<output>
After completion, create `.planning/phases/17-parser-etched/17-01-SUMMARY.md` with:
- The 12-decision check table (D-01..D-12, each with status + satisfying task + evidence)
- The complete files-modified list with one-line descriptions
- Test count delta (before/after) and a list of new tests added (binder-name × ~9, csv-parser × 8 new + ~3 updated, enrichment × 2 new + factory updates, db tests factory updates + 1 new binder-passthrough test, admin export +1 etched row test, import test factory updates)
- The captured `foil`-grep proof output (expected empty)
- The Phase 16 deferred items this phase resolves (Card type carries foil; admin export Foil header)
- Phase 17 deferred items (`replaceCardsForBinders` rename → Phase 19; cart-key reconciler → Phase 20; binder picker UI → Phase 19; did-you-mean → v1.3.x)
- The cutover note: Phase 17 ships with Phase 16 in one v1.3 PR; etched cards become first-class on the next CSV import after deploy
</output>
</content>
</invoke>