---
phase: 21-admin-visibility-audit
plan: 01
type: execute
wave: 1
depends_on: ["20-01", "20-02"]
files_modified:
  - src/db/queries.ts
  - src/db/__tests__/dashboard-stats.test.ts
  - src/db/__tests__/admin-cards-binder-filter.test.ts
  - src/app/api/admin/cards/route.ts
  - src/app/api/admin/cards/__tests__/route.test.ts
  - src/app/admin/_components/action-bar.tsx
  - src/app/admin/_components/inventory-table.tsx
  - src/app/admin/_components/dashboard-summary.tsx
autonomous: true
requirements:
  - ADM-02
must_haves:
  truths:
    - "src/db/queries.ts `AdminCardsParams` adds an OPTIONAL `binder?: string` field (no default; absent or empty string means no filter — matches the existing `set?: string` and `condition?: string` pattern at queries.ts:218-225). NO new public type — `binder` slots into the existing params interface so the API surface delta is one optional field"
    - "src/db/queries.ts `getAdminCards()` (queries.ts:768-815) extends the `conditions: SQL[]` array with `if (binder) conditions.push(eq(cards.binder, binder));` placed after the existing `condition` filter push (line 784). NO change to ordering, sorting, pagination, or rowToCard — the binder column already exists on InventoryRow (Phase 16 D-09 / Phase 20 D-03) and is returned verbatim; this task adds the FILTER, not the column"
    - "src/db/queries.ts `AdminDashboardStats.breakdowns` adds a new `byBinder: Array<AdminDashboardBreakdown & { binder: string }>` field positioned LAST in the breakdowns object (matches the existing bySet/byColor/byRarity shape at queries.ts:653-657); NO existing breakdown shape changes"
    - "src/db/queries.ts `getAdminDashboardStats()` (queries.ts:713-765) extends the `db.select(...)` to include `binder: cards.binder` and the for-loop adds `addBreakdownEntry(byBinder, row.binder, row.quantity, valueCents);` alongside the existing bySet/byColor/byRarity calls; the return assembles `byBinder: mapBreakdown(byBinder, \"binder\")` and `byBinder` is sorted by `(value desc, binder asc)` per the existing `mapBreakdown` sort pattern (quantity desc fallback to label asc — D-12 spec: 'sort by ... default mapBreakdown order'). Single SELECT pass; NO extra DB round-trip"
    - "src/app/api/admin/cards/route.ts GET handler reads `binder = url.searchParams.get(\"binder\") ?? \"\"` (mirrors the existing `set` and `condition` reads at lines 19-20) and passes it through to `getAdminCards({ ..., binder })`; NO validation/normalization of the value (server SQL `eq()` is parameterized — same trust model as set/condition)"
    - "src/app/admin/_components/inventory-table.tsx adds `const [binderFilter, setBinderFilter] = useState(\"\")` alongside the existing `setFilter` and `conditionFilter` states (line 104-105); the `fetchCards` callback (line 122-151) appends `if (binderFilter) params.set(\"binder\", binderFilter)` to the URLSearchParams build; the dependency array adds `binderFilter`; the filter-reset effect (line 195-200) and the page reset effect adds `binderFilter` to its deps (so changing the binder dropdown resets to page 1 + clears selection — matches existing setFilter/conditionFilter behavior)"
    - "src/app/admin/_components/inventory-table.tsx adds a new `availableBinders: string[]` state populated by extending the existing `fetchSets` effect (line 170-188) to ALSO derive the distinct binders set from the same /api/admin/cards?limit=200 response: `const binders = [...new Set<string>(data.cards.map((c: InventoryRow) => c.binder))].sort();`; D-02 acceptance is `SELECT DISTINCT binder FROM cards ORDER BY binder ASC` — this client-side derive over the limit=200 sample is acceptable for v1.3 (matches the already-shipped availableSets pattern; total inventory is ~136-12,749 rows so limit=200 covers the vast majority of binder names; if a binder appears only in a row >200 the operator can still filter via URL ?binder=...). DO NOT add a new dedicated /api/admin/binders endpoint — that's premature; this matches the existing availableSets pattern verbatim and avoids one route per filter-dropdown"
    - "src/app/admin/_components/action-bar.tsx ActionBarProps gains TWO new props: `binderFilter: string` and `onBinderFilterChange: (value: string) => void` plus `availableBinders: string[]` — placement: directly after the existing `availableSets` prop in the interface and the destructured arg list (matches set/condition group pattern). The component renders a third `<select>` between the existing Set dropdown (line 78-89) and Condition dropdown (line 92-103) with `<option value=\"\">All binders</option>` followed by `availableBinders.map((b) => <option key={b} value={b}>{b}</option>)`. The `<option>` text renders the binder name verbatim (lowercase normalized form per Phase 17 D-04 + CONTEXT D-01)"
    - "src/app/admin/_components/inventory-table.tsx adds a new column header `<th>Binder</th>` (text label, no sort) positioned AFTER the Cond column header (line 642-644) and BEFORE the Qty column header (line 645-656) — matches CONTEXT D-01 placement directive; the corresponding `<td>` in the row body renders `{card.binder}` in lowercase normalized form per CONTEXT D-01 — Tailwind class matches the adjacent text-zinc-500 style of the Set cell (line 714: `<td className=\"px-4 py-2 text-zinc-500 dark:text-zinc-400\">`). The deletingId row's `<td colSpan={8}>` (line 670) becomes `colSpan={9}` because the table now has 9 visible columns instead of 8"
    - "src/app/admin/_components/dashboard-summary.tsx adds a fourth `BreakdownSection` for `byBinder` mirroring the existing bySet/byColor/byRarity pattern (line 137-163). Title: \"Breakdown by binder\". emptyLabel: \"No binder breakdown yet.\". Row label maps verbatim from `row.binder` (no `.toUpperCase()` because binder names are normalized lowercase per Phase 17 D-04). The grid container at line 133-134 becomes `lg:grid-cols-4` instead of `lg:grid-cols-3` so the four breakdown tiles fit one row on lg screens (D-12 + Claude's Discretion — keeps the dashboard at-a-glance)"
    - "src/db/__tests__/dashboard-stats.test.ts is EXTENDED (not replaced): the existing 'mixed cards' fixture (line 24-49) gains a `binder` field on each row — `binder: 'a02'`, `binder: 'a05'`, `binder: 'unsorted'` (chosen for sort verification: a02 < a05 < unsorted alphabetically) — and the `select(...)` mock returns those binders; the asserted result (line 51-77) gains a `byBinder` array sorted by `(quantity desc, binder asc)` matching the mapBreakdown contract. The empty-fixture test (line 79-96) gains `byBinder: []` to its asserted result. NO existing assertion is removed; both tests still cover the same baseline behavior"
    - "src/db/__tests__/admin-cards-binder-filter.test.ts is a NEW file pinning the binder filter SQL behavior. Pattern mirrors src/db/__tests__/dashboard-stats.test.ts (vi.mock 'server-only', vi.mock '@/db/client'). Tests pin: (a) `getAdminCards({ binder: 'a02' })` calls `eq(cards.binder, 'a02')` in the conditions array (verified by spying on the where() call shape or by intercepting the conditions in the SQL builder); (b) `getAdminCards({ binder: '' })` does NOT push a binder eq into conditions (empty string = no filter); (c) `getAdminCards({ binder: undefined })` does NOT push a binder eq; (d) `getAdminCards({ binder: 'a02', set: 'sld', condition: 'near_mint' })` produces ALL THREE conditions (set + condition + binder) ANDed together. EXECUTOR NOTE: if the test infra makes intercepting `and(...conditions)` awkward (drizzle-orm SQL builder is opaque), substitute with an INTEGRATION test that seeds 3 cards with binders a02/a05/unsorted and asserts `(await getAdminCards({ binder: 'a02' })).cards` length === 1 + the returned card has binder === 'a02'. Either approach satisfies ADM-02 traceability; pick (a) the SQL-shape spy if the existing test infra supports it, else (b) the integration form. Recommend looking at how the search/set/condition existing filters are tested (if at all) and matching their pattern"
    - "src/app/api/admin/cards/__tests__/route.test.ts gains ONE new assertion in the existing 'passes query params to getAdminCards' test (line 118-148): the mocked params object now includes `binder: 'a02'`, the makeGetRequest call adds `binder: 'a02'` to the params record, and the `expect(mockGetAdminCards).toHaveBeenCalledWith(...)` literal includes `binder: 'a02'` in the expected object. This is a one-line extension of an existing test — does NOT add a separate it() block to keep test churn minimal"
    - "Existing `vitest run` baseline (443 passed + 2 skipped = 445 across 41 files per Phase 20 VERIFICATION) continues to pass; new tests added by this plan EXCEED the baseline; no existing test is deleted or skipped. Net delta target: +1 file (admin-cards-binder-filter), +5 to +8 tests minimum (4 binder-filter tests + 2 extended dashboard-stats assertions + 1 extended route test = ~7 new assertions; counted as new tests if added as new it() blocks)"
  artifacts:
    - path: "src/db/queries.ts"
      provides: "AdminCardsParams.binder optional filter field; getAdminCards binder eq() condition; AdminDashboardStats.breakdowns.byBinder field; getAdminDashboardStats single-pass byBinder accumulation"
      exports: ["getAdminCards", "getAdminDashboardStats", "AdminCardsParams", "AdminCardsResult", "AdminDashboardStats", "AdminDashboardBreakdown"]
      contains: "AdminCardsParams gains optional binder; getAdminCards conditions push eq(cards.binder, binder) when binder is non-empty; AdminDashboardStats.breakdowns gains byBinder: Array<AdminDashboardBreakdown & { binder: string }>; getAdminDashboardStats select() includes binder column; for-loop accumulates byBinder; mapBreakdown(byBinder, 'binder') returned"
    - path: "src/db/__tests__/dashboard-stats.test.ts"
      provides: "Existing tests extended with byBinder coverage"
      contains: "Mixed-cards fixture rows gain binder: 'a02' | 'a05' | 'unsorted'; assertion gains byBinder array sorted by (quantity desc, binder asc); empty-inventory assertion gains byBinder: []"
    - path: "src/db/__tests__/admin-cards-binder-filter.test.ts"
      provides: "Pins binder filter SQL behavior; covers absent/empty/non-empty binder cases + combined-filter case"
      contains: "vi.mock server-only + @/db/client; 4 it() tests covering binder='a02' (filter applied), binder='' (no filter), binder=undefined (no filter), and combined set+condition+binder (all three conditions ANDed); test approach matches the existing dashboard-stats pattern (mock data layer, intercept call shape) OR uses the integration variant if the SQL-builder spy is awkward"
    - path: "src/app/api/admin/cards/route.ts"
      provides: "GET handler reads binder from URL searchParams and passes it through to getAdminCards"
      contains: "const binder = url.searchParams.get('binder') ?? ''; getAdminCards({ ..., binder })"
    - path: "src/app/api/admin/cards/__tests__/route.test.ts"
      provides: "Existing 'passes query params' test extended with binder='a02' assertion"
      contains: "makeGetRequest params record adds binder: 'a02'; expect(mockGetAdminCards).toHaveBeenCalledWith(...) literal adds binder: 'a02'"
    - path: "src/app/admin/_components/action-bar.tsx"
      provides: "ActionBarProps gains binderFilter, onBinderFilterChange, availableBinders; renders a third <select> dropdown between Set and Condition"
      contains: "Three new props in the interface and destructure; the new <select> element with <option value=''>All binders</option> and availableBinders.map for the rest; matches the established Set/Condition select Tailwind classes verbatim"
    - path: "src/app/admin/_components/inventory-table.tsx"
      provides: "binderFilter state + URL search param wiring; availableBinders derive in fetchSets effect; Binder column header + cell; ActionBar prop wiring (4 sites: hasFilters check, two empty-state ActionBar renders, main ActionBar render)"
      contains: "useState binderFilter; fetchCards URLSearchParams gains binder; availableBinders derived from cards data; column header <th>Binder</th> after Cond before Qty; <td>{card.binder}</td>; ActionBar receives binderFilter/onBinderFilterChange/availableBinders at all four call sites; deletingId row colSpan changes 8 -> 9; hasFilters check (line 502) gains || binderFilter; clear-filters button (line 532) clears binderFilter too"
    - path: "src/app/admin/_components/dashboard-summary.tsx"
      provides: "Fourth BreakdownSection for byBinder; grid column count bumped 3 -> 4 on lg screens"
      contains: "BreakdownSection title='Breakdown by binder' emptyLabel='No binder breakdown yet.' rows mapped from stats.breakdowns.byBinder; grid container className gains lg:grid-cols-4"
---

<objective>
Land the v1.3 Phase 21 admin visibility extensions for the inventory binder column, filter, and dashboard breakdown. Specifically: (1) extend `getAdminCards()` with an optional `binder` filter that pushes `eq(cards.binder, binder)` into the conditions array, (2) extend `getAdminDashboardStats()` to compute a `byBinder` breakdown alongside the existing bySet/byColor/byRarity in the same single-pass select, (3) add a `Binder` column to the admin inventory table positioned after Condition and before Quantity (lowercase normalized binder name per Phase 17 D-04), (4) add a third single-select dropdown to the inventory action-bar between Set and Condition with options derived from the distinct binder set, persisted via the existing URLSearchParams pattern, (5) add a fourth "Breakdown by binder" tile to the admin dashboard alongside the existing By set / By color / By rarity tiles. Pin the new SQL filter behavior with admin-cards-binder-filter.test.ts; extend the dashboard-stats test fixture to cover byBinder; extend the admin/cards route test to cover the new query param. All existing tests continue to pass; baseline 443 passed + 2 skipped (445 across 41 files) is preserved or exceeded.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-admin-visibility-audit/21-CONTEXT.md
@.planning/phases/21-admin-visibility-audit/21-DISCUSSION-LOG.md
@.planning/phases/16-schema-migration/16-CONTEXT.md
@.planning/phases/17-parser-etched/17-CONTEXT.md
@.planning/phases/19-import-preview-picker/19-CONTEXT.md
@.planning/phases/20-storefront-aggregation-cart-migration/20-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/SUMMARY.md
</execution_context>

<context>
@src/db/queries.ts
@src/db/schema.ts
@src/db/__tests__/dashboard-stats.test.ts
@src/db/__tests__/admin-queries.test.ts
@src/lib/types.ts
@src/lib/binder-name.ts
@src/app/api/admin/cards/route.ts
@src/app/api/admin/cards/__tests__/route.test.ts
@src/app/admin/page.tsx
@src/app/admin/_components/action-bar.tsx
@src/app/admin/_components/inventory-table.tsx
@src/app/admin/_components/dashboard-summary.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add binder filter to getAdminCards (TDD via admin-cards-binder-filter.test.ts)</name>
  <files>
    src/db/queries.ts,
    src/db/__tests__/admin-cards-binder-filter.test.ts
  </files>
  <read_first>
    src/db/queries.ts,
    src/db/schema.ts,
    src/db/__tests__/dashboard-stats.test.ts,
    .planning/phases/21-admin-visibility-audit/21-CONTEXT.md
  </read_first>
  <behavior>
    - FIRST: write `src/db/__tests__/admin-cards-binder-filter.test.ts` mirroring the dashboard-stats.test.ts mock pattern. Mock surface needed: `vi.mock("server-only", () => ({}))` and `vi.mock("@/db/client", () => ({ db: {...} }))`. The `db` shape needs `select`, `from`, `where`, `orderBy`, `limit`, `offset` chainables that record the `where` clause for assertion.
    - FOUR tests:
      1. `getAdminCards({ binder: 'a02' })` invokes `db.select().from(cards).where(...)` with a where clause that includes `eq(cards.binder, 'a02')`. Assertion approach: spy on the chain, capture the SQL or the `and(...)` argument shape, assert at least one element targets the binder column.
      2. `getAdminCards({ binder: '' })` does NOT include a binder predicate (the conditions array stays whatever it would be without binder).
      3. `getAdminCards({ binder: undefined })` (or `getAdminCards({})` — same effect) does NOT include a binder predicate.
      4. `getAdminCards({ binder: 'a02', set: 'sld', condition: 'near_mint', search: 'lightning' })` produces FOUR predicates ANDed together (search ilike + set eq + condition eq + binder eq) — verifies the new filter composes with the existing three.
    - EXECUTOR FALLBACK: if intercepting drizzle-orm's `eq()` / `and()` construction is awkward (the SQL builder is opaque), substitute an integration test using the existing `replace-cards-for-binders.test.ts` test-DB pattern: seed 3 cards with binders 'a02', 'a05', 'unsorted'; call `getAdminCards({ binder: 'a02' })`; assert `result.cards.length === 1 && result.cards[0].binder === 'a02'`. Repeat for empty/undefined/combined cases. Same 4 tests, different mocking strategy. Pick whichever approach the existing repo already uses for getAdminCards-style coverage (look at admin-queries.test.ts and bulk-delete-cards.test.ts for prior art).
    - SECOND: in `src/db/queries.ts`:
      - Extend `AdminCardsParams` (line 218-225) with `binder?: string;` placed after `condition?: string;`.
      - In `getAdminCards()` (line 768-815), destructure `binder = ""` from params (after the existing `condition = ""` destructure on line 776).
      - In the conditions block (line 781-784), append:
        ```ts
        if (binder) conditions.push(eq(cards.binder, binder));
        ```
        — placed after the existing `condition` push so the AND order is search → set → condition → binder.
    - NO change to ordering, sorting, pagination, or rowToCard. The `binder` field already exists on `InventoryRow` (Phase 16 / Phase 20) so the row data is unchanged — only the WHERE filter is new.
  </behavior>
  <success_criteria>
    - `vitest run src/db/__tests__/admin-cards-binder-filter.test.ts` passes all 4 new tests
    - `vitest run src/db/__tests__/dashboard-stats.test.ts` continues to pass (no breakage from queries.ts edits)
    - `tsc --noEmit` passes for the queries.ts file
    - `grep -n "if (binder)" src/db/queries.ts` returns one hit inside getAdminCards
  </success_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add byBinder breakdown to getAdminDashboardStats (TDD via extended dashboard-stats.test.ts)</name>
  <files>
    src/db/queries.ts,
    src/db/__tests__/dashboard-stats.test.ts
  </files>
  <read_first>
    src/db/queries.ts,
    src/db/__tests__/dashboard-stats.test.ts
  </read_first>
  <behavior>
    - FIRST: extend `src/db/__tests__/dashboard-stats.test.ts`:
      - Mixed-cards fixture (line 24-49): add `binder: 'a02'`, `binder: 'a05'`, `binder: 'unsorted'` to the three rows respectively. (Choice rationale: a02 < a05 < unsorted alphabetically; gives deterministic sort verification when quantities tie.)
      - Mixed-cards assertion (line 51-77): add to `breakdowns`:
        ```ts
        byBinder: [
          { binder: "a02", quantity: 3, uniqueCards: 1, value: 3.75 },
          { binder: "a05", quantity: 2, uniqueCards: 1, value: 5 },
          { binder: "unsorted", quantity: 1, uniqueCards: 1, value: 0 },
        ],
        ```
        — order matches the existing `mapBreakdown` sort (`b.quantity - a.quantity || a.label.localeCompare(b.label)`): a02 leads with quantity 3, then a05 with quantity 2, then unsorted with quantity 1. This pin doubles as a sort-stability assertion for byBinder.
      - Empty-fixture assertion (line 82-96): add `byBinder: []` to `breakdowns`.
    - SECOND: in `src/db/queries.ts`:
      - Extend `AdminDashboardStats.breakdowns` (line 653-657) with:
        ```ts
        byBinder: Array<AdminDashboardBreakdown & { binder: string }>;
        ```
        positioned LAST in the breakdowns object literal type.
      - In `getAdminDashboardStats()` (line 713-765):
        - Add `binder: cards.binder` to the `db.select({...})` (line 715-722).
        - Add `const byBinder = new Map<string, DashboardBreakdownAccumulator>();` after the existing three Maps (line 727).
        - Add `addBreakdownEntry(byBinder, row.binder, row.quantity, valueCents);` inside the for-loop (line 734-748) alongside the existing addBreakdownEntry calls.
        - Add `byBinder: mapBreakdown(byBinder, "binder"),` inside the returned `breakdowns` object (line 759-763), positioned LAST.
    - Single SELECT pass; the existing query already scans every card row, so the byBinder accumulator is free.
    - No change to the `inventory.uniqueCards/totalQuantity/totalValue/lowStockCount/missingPriceCount` aggregates.
  </behavior>
  <success_criteria>
    - `vitest run src/db/__tests__/dashboard-stats.test.ts` passes both extended tests
    - `vitest run src/db/__tests__/admin-cards-binder-filter.test.ts` (Task 1) continues to pass
    - `tsc --noEmit` passes (AdminDashboardStats type now includes byBinder; consumers in dashboard-summary.tsx update in Task 6)
    - `grep -n "byBinder" src/db/queries.ts` returns at least 3 hits (type, accumulator, return)
  </success_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire binder query param through GET /api/admin/cards route</name>
  <files>
    src/app/api/admin/cards/route.ts,
    src/app/api/admin/cards/__tests__/route.test.ts
  </files>
  <read_first>
    src/app/api/admin/cards/route.ts,
    src/app/api/admin/cards/__tests__/route.test.ts
  </read_first>
  <behavior>
    - In `src/app/api/admin/cards/route.ts` GET handler:
      - Add `const binder = url.searchParams.get("binder") ?? "";` after the existing `condition` read (line 20).
      - Add `binder` to the `getAdminCards({...})` call object (line 39-47), positioned after `condition`.
    - NO validation/normalization (matches the existing trust model for `set` and `condition` — drizzle-orm parameterizes via `eq()`).
    - In `src/app/api/admin/cards/__tests__/route.test.ts`:
      - Extend the existing 'passes query params to getAdminCards' test (line 118-148): add `binder: "a02"` to the `makeGetRequest` params record AND to the `expect(mockGetAdminCards).toHaveBeenCalledWith(...)` literal.
      - Do NOT add a separate it() block — keeping test churn minimal.
  </behavior>
  <success_criteria>
    - `vitest run src/app/api/admin/cards/__tests__/route.test.ts` passes the extended test (and all other existing tests)
    - `grep -n 'searchParams.get("binder")' src/app/api/admin/cards/route.ts` returns one hit
  </success_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Extend ActionBar with binder filter dropdown</name>
  <files>
    src/app/admin/_components/action-bar.tsx
  </files>
  <read_first>
    src/app/admin/_components/action-bar.tsx
  </read_first>
  <behavior>
    - Extend `ActionBarProps` (line 4-20):
      - Add `binderFilter: string;` after `setFilter: string;`.
      - Add `onBinderFilterChange: (value: string) => void;` after `onSetFilterChange`.
      - Add `availableBinders: string[];` after `availableSets: string[];`.
    - Extend the destructured component arg list (line 22-38) to include the three new props in the same order.
    - Render a third `<select>` element between the existing Set dropdown (line 78-89) and Condition dropdown (line 92-103). Markup:
      ```tsx
      {/* Binder filter dropdown (Phase 21 D-02) */}
      <select
        value={binderFilter}
        onChange={(e) => onBinderFilterChange(e.target.value)}
        className="rounded-md border border-zinc-300 bg-transparent px-3 py-1.5 text-sm focus:outline-none focus:ring-2 focus:ring-accent dark:border-zinc-700"
      >
        <option value="">All binders</option>
        {availableBinders.map((b) => (
          <option key={b} value={b}>
            {b}
          </option>
        ))}
      </select>
      ```
    - The `<option>` text renders the binder name verbatim (lowercase normalized form per Phase 17 D-04 and CONTEXT D-01) — DO NOT call `.toUpperCase()` on it (set codes are uppercased; binder names are NOT).
  </behavior>
  <success_criteria>
    - `tsc --noEmit` passes (ActionBarProps interface change is consumed by inventory-table.tsx in Task 5; until then, callers will be type errors — Task 5 closes them in the same plan)
    - `grep -n "binderFilter" src/app/admin/_components/action-bar.tsx` returns at least 3 hits
    - `grep -n "All binders" src/app/admin/_components/action-bar.tsx` returns one hit
  </success_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Wire binderFilter state + Binder column into InventoryTable</name>
  <files>
    src/app/admin/_components/inventory-table.tsx
  </files>
  <read_first>
    src/app/admin/_components/inventory-table.tsx,
    src/app/admin/_components/action-bar.tsx
  </read_first>
  <behavior>
    - Add state hooks immediately after the existing `conditionFilter` state (line 105):
      ```ts
      const [binderFilter, setBinderFilter] = useState("");
      const [availableBinders, setAvailableBinders] = useState<string[]>([]);
      ```
    - In `fetchCards` callback (line 122-151):
      - After the existing `if (conditionFilter) params.set("condition", conditionFilter);` line (line 131), add:
        ```ts
        if (binderFilter) params.set("binder", binderFilter);
        ```
      - Add `binderFilter` to the deps array (line 151) — positioned after `conditionFilter`.
    - In the `fetchSets` effect (line 170-188):
      - After the existing `setAvailableSets(sets);` line (line 181), add:
        ```ts
        const binders = [
          ...new Set<string>(
            data.cards.map((c: InventoryRow) => c.binder),
          ),
        ].sort();
        setAvailableBinders(binders);
        ```
      - Rename the function for clarity if appropriate, or add a code comment: `// Phase 21 D-02: also derive distinct binders from the same response`.
    - Filter-reset effect (line 196-200):
      - Add `binderFilter` to the deps array.
    - hasFilters check (line 502):
      ```ts
      const hasFilters = debouncedSearch || setFilter || conditionFilter || binderFilter;
      ```
    - Clear-filters button (line 532):
      ```ts
      onClick={() => {
        setSearch("");
        setSetFilter("");
        setConditionFilter("");
        setBinderFilter("");
      }}
      ```
    - All FOUR `<ActionBar ... />` JSX instances (line 506-523, 547-564, 580-596, plus deletingAll-confirmation flows if any) gain three new props:
      ```tsx
      binderFilter={binderFilter}
      onBinderFilterChange={(v) => { setBinderFilter(v); setPage(1); }}
      availableBinders={availableBinders}
      ```
      positioned after the existing `availableSets` prop.
    - Add the new column to the table header (line 642-644) — INSERT between Cond (line 642) and Qty (line 645):
      ```tsx
      <th className="px-4 py-3 text-sm font-semibold text-zinc-600 dark:text-zinc-400 w-24">
        Binder
      </th>
      ```
      Width `w-24` chosen as a compact-but-readable middle ground (matches the Set column's w-20 sibling but allows for "unsorted"-length names).
    - Add the new cell to the row body (between line 736 Cond `<td>` and line 737 Qty `<td>`):
      ```tsx
      <td className="px-4 py-2 text-zinc-500 dark:text-zinc-400">
        {card.binder}
      </td>
      ```
      Verbatim render (no toUpperCase, no transform) — matches CONTEXT D-01 lowercase normalized form.
    - Update the deletingId row's `<td colSpan={8}>` (line 670) to `<td colSpan={9}>` because the table now has 9 visible columns (was 8: select / img / name / set / price / cond / qty / actions; now 9 with the binder column inserted).
  </behavior>
  <success_criteria>
    - `tsc --noEmit` passes (ActionBar interface mismatch from Task 4 is now resolved)
    - The InventoryTable renders the Binder column for each card
    - URL search params correctly include `binder=...` when the dropdown is non-default
    - Selecting a binder from the dropdown resets to page 1 and clears selection (matches set/condition behavior)
    - The hasFilters/clear-filters branch correctly handles binderFilter
  </success_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Add "Breakdown by binder" tile to DashboardSummary</name>
  <files>
    src/app/admin/_components/dashboard-summary.tsx
  </files>
  <read_first>
    src/app/admin/_components/dashboard-summary.tsx,
    src/db/queries.ts
  </read_first>
  <behavior>
    - Update the breakdowns grid container (line 133):
      ```tsx
      <div className="grid gap-4 lg:grid-cols-4">
      ```
      (was `lg:grid-cols-3`; now 4 to fit the new tile alongside set/color/rarity on lg screens)
    - Add a fourth `<BreakdownSection>` after the existing rarity section (after line 163, before the closing `</div>` on line 164):
      ```tsx
      <BreakdownSection
        title="Breakdown by binder"
        emptyLabel="No binder breakdown yet."
        rows={stats.breakdowns.byBinder.map((row) => ({
          label: row.binder,
          quantity: row.quantity,
          uniqueCards: row.uniqueCards,
          value: row.value,
        }))}
      />
      ```
    - DO NOT call `.toUpperCase()` on the label (matches the lowercase normalized binder name convention per Phase 17 D-04). Compare with `bySet.map(...row.setCode.toUpperCase())` (line 138) which IS uppercased — set codes vs binder names diverge here intentionally.
    - NO collapsibility wrapper for v1.3 Phase 21 (CONTEXT Claude's Discretion item: "Whether to make the dashboard 'By binder' tile collapsible (likely yes if the table is long; planner picks)" — picking NO for v1.3 because (a) the existing breakdown sections are not collapsible either; (b) the operator's likely binder count is ~10-30 which fits without scroll; (c) one less moving part for v1.3 minimal viable scope). If the operator's actual binder count grows >50 in v1.3.x, add a collapsible wrapper as a follow-up.
  </behavior>
  <success_criteria>
    - `tsc --noEmit` passes (AdminDashboardStats from Task 2 now provides byBinder)
    - The /admin dashboard renders the new "Breakdown by binder" tile alongside set/color/rarity
    - On lg screens, the four tiles render in one row
  </success_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Run full test suite and verify baseline + new tests all pass</name>
  <files>
    (none — verification only)
  </files>
  <read_first>
    (none)
  </read_first>
  <behavior>
    - Run `npx vitest run` and verify:
      - Baseline (443 passed + 2 skipped across 41 files per Phase 20 VERIFICATION) is at minimum preserved
      - New tests from Tasks 1, 2, 3 are counted in the pass column
      - No previously-passing test now fails
      - The 2 baseline skipped tests stay skipped
    - Run `npx tsc --noEmit` and verify zero TypeScript errors
    - Run `npm run build` and verify a clean Next 16 production build (the dashboard route includes the new tile; the inventory route includes the new column + filter)
    - If any failure, fix the root cause IN THIS TASK and re-run.
  </behavior>
  <success_criteria>
    - vitest: at least 443 + ~7 new = 450 passed minimum, 2 skipped, 0 failed
    - tsc: 0 errors
    - npm run build: success
  </success_criteria>
</task>

</tasks>

<verification>
After all tasks complete:
1. `git diff --stat` shows changes scoped to the files in `files_modified`
2. `npx vitest run` passes all existing + new tests (>= 450 passed)
3. `npx tsc --noEmit` succeeds with no type errors
4. `npm run build` succeeds
5. Manual smoke (deferred to Plan 21-02 + browser UAT in Phase 22):
   - `/admin` dashboard renders four breakdown tiles in one row on lg screens
   - `/admin/inventory` shows the Binder column between Cond and Qty
   - The binder filter dropdown above the table populates from distinct binders
   - Selecting a binder appends `?binder=...` to the URL and filters the table; clearing returns to "All binders"
6. Plan 21-02 (order detail [binder] pill + audit metadata rendering) can begin against this baseline.

**Requirements satisfied:**
- ADM-02: admin inventory binder column + dropdown — Tasks 1 (filter), 4 (dropdown UX), 5 (table column + URL wiring); ADM-02 dashboard binder breakdown — Tasks 2 (query), 6 (tile)

**Decisions traceability:**
- D-01 ✓ (Task 5 column placement after Condition before Quantity; lowercase render)
- D-02 ✓ (Task 4 single-select dropdown; Task 5 derives distinct binders mirroring availableSets pattern)
- D-03 ✓ (Tasks 1 + 3 + 5: server-side eq() filter; URL search param via the existing fetchCards URLSearchParams build)
- D-04 ✓ (Bulk-edit-binder explicitly out of scope for this plan; deferred per CONTEXT D-04)
- D-12 ✓ (Tasks 2 + 6: byBinder dashboard breakdown alongside existing tiles)
- D-13 ✓ (~50-line addition; chaos-sort fulfillment workflow visible at-a-glance)

**Decisions covered by Plan 21-02:**
- D-05 through D-11 (order detail [binder] pill + audit page metadata rendering)

**Decisions out of scope (deferred per CONTEXT):**
- D-04 — Bulk-edit-binder workflow → v1.3.x
</verification>

---

*Plan 21-01: Inventory Binder Column + Filter + Dashboard Breakdown*
*Created: 2026-05-11 (Phase 21, v1.3 Binder-Aware Inventory)*
