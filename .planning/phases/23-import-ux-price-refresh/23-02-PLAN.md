---
phase: 23-import-ux-price-refresh
plan: 02
type: execute
wave: 2
depends_on: ['23-01']
files_modified:
  - src/app/admin/import/_components/binder-picker.tsx
  - src/app/admin/import/_components/import-client.tsx
  - src/lib/store/binder-import-store.ts
  - src/app/admin/import/_components/__tests__/binder-picker.test.tsx
  - src/app/admin/import/_components/__tests__/import-client.test.tsx
autonomous: true
requirements:
  - IMPORT-UX-01
  - IMPORT-UX-02
  - IMPORT-UX-03
  - IMPORT-UX-04
  - IMPORT-UX-05
tags:
  - import-ux
  - binder-picker
  - zustand
  - controlled-component
must_haves:
  truths:
    - "Fresh-session import opens the binder picker with every binder unchecked, regardless of any localStorage state from prior sessions (D-05 — `defaultCheckedFor` memory dropped entirely, Option A from PITFALLS Pitfall 3)"
    - "Returning-session import opens the binder picker with every binder unchecked — prior `lastSelection` localStorage has zero influence on the picker's initial check state"
    - "Picker `<header>` contains a 'Select all' button that, when clicked, sets every binder in the current upload to checked in a SINGLE render (via `onBulkSet(names, true)`, NOT N `onToggle` calls)"
    - "Picker `<header>` contains a 'Deselect all' button that, when clicked, sets every binder in the current upload to unchecked in a SINGLE render"
    - "Live 'X of Y selected' counter in the picker header updates within the same render as any click (existing `selectedCount` derivation already covers this — just verify the count surface stays wired)"
    - "Continue button on the picker stage is disabled when `selectedBinders.length === 0 && willDeleteBinders.length === 0`, with adjacent helper text explaining what to do (Use Select all to begin)"
    - "Will-delete amber panel default-CHECKED behavior at import-client.tsx:255-256 is UNCHANGED (D-05 explicit — only the picker's per-binder memory is dropped; will-delete defaults remain as-is)"
    - "BinderPicker remains a controlled `\"use client\"` component — no internal state added (D-15)"
    - "Picker keyboard tab order is: filename → 'Select all' button → 'Deselect all' button → first checkbox → … → Continue (native `<button type=\"button\">` elements, no `role=\"button\"` shims)"
    - "`useBinderImportStore` no longer exposes `defaultCheckedFor` (or exposes it as `() => false` with a deprecation comment); `lastSelection`, `recordCommit`, and `knownBinderNames` are RETAINED because the will-delete panel still consumes them"
  artifacts:
    - path: "src/app/admin/import/_components/binder-picker.tsx"
      provides: "Controlled picker with Select all + Deselect all + onBulkSet prop"
      contains: "onBulkSet"
      exports: ["BinderPicker", "BinderPickerProps", "computeMissingBinders"]
    - path: "src/app/admin/import/_components/import-client.tsx"
      provides: "Parent state holder; init loop sets all binders to false; onBulkSet wired via single setPickerSelection"
      contains: "onBulkSet"
    - path: "src/lib/store/binder-import-store.ts"
      provides: "Zustand store with defaultCheckedFor dropped (or stubbed to () => false) and a docblock explaining D-05"
    - path: "src/app/admin/import/_components/__tests__/binder-picker.test.tsx"
      provides: "Picker behavior tests (Select all, Deselect all, counter, disabled-Continue, no-memory)"
  key_links:
    - from: "src/app/admin/import/_components/binder-picker.tsx"
      to: "src/app/admin/import/_components/import-client.tsx"
      via: "onBulkSet prop"
      pattern: "onBulkSet:\\s*\\("
    - from: "src/app/admin/import/_components/import-client.tsx"
      to: "src/app/admin/import/_components/binder-picker.tsx"
      via: "<BinderPicker onBulkSet={...} />"
      pattern: "onBulkSet=\\{"
    - from: "src/app/admin/import/_components/import-client.tsx"
      to: "binder-import-store"
      via: "knownBinderNames + recordCommit + lastSelection retained"
      pattern: "useBinderImportStore"
---

<objective>
Replace the import binder picker's "remembered selection memory" with explicit opt-in: drop `defaultCheckedFor` entirely (D-05, Option A from PITFALLS Pitfall 3), add Select all / Deselect all buttons in the picker header backed by a new `onBulkSet(names, checked)` callback (D-15 — single render not N renders), surface a live "X of Y selected" counter (existing derivation already covers it; verify wiring), and keep the Continue button disabled with helper text when nothing actionable is selected.

Purpose: Operator opens every import with zero ambiguity about what's about to be imported. The Select all button is the recovery affordance for the "I want everything" case. The will-delete amber panel default-CHECKED behavior is untouched.

Output: Three files modified (picker, parent, zustand store), one test file extended (or created) covering five new behaviors. No new dependencies, no schema change, no API change.
</objective>

<execution_context>
@/home/wiko/Projects/active/mtg-bulk-sale-page/.claude/get-shit-done/workflows/execute-plan.md
@/home/wiko/Projects/active/mtg-bulk-sale-page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/23-import-ux-price-refresh/23-CONTEXT.md
@.planning/phases/23-import-ux-price-refresh/23-PATTERNS.md

@.planning/research/SUMMARY.md
@.planning/research/FEATURES.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

@./CLAUDE.md
@./AGENTS.md

<interfaces>
<!-- Extracted from codebase. Executor should use these directly. -->

From `src/app/admin/import/_components/binder-picker.tsx:19-27` (current props — extend with `onBulkSet`):
```typescript
export interface BinderPickerProps {
  binders: BinderSummary[];
  /** Operator's prior selection (read-only here; updates flow through onToggle). */
  knownBinderNames: string[];
  /** Current checked state — `Record<binderName, boolean>`. Controlled by parent. */
  selection: Record<string, boolean>;
  /** Toggle callback. */
  onToggle: (binderName: string, checked: boolean) => void;
  // ADD per D-15:
  // onBulkSet: (binderNames: string[], checked: boolean) => void;
}
```

From `src/app/admin/import/_components/binder-picker.tsx:73-80` (current `<header>` block — extend):
```tsx
<header className="flex items-center justify-between mb-3">
  <h2 id="binder-picker-heading" className="text-sm font-semibold text-zinc-700 dark:text-zinc-300">
    Select binders to import ({selectedCount} of {binders.length})
  </h2>
</header>
```
The `selectedCount` derivation at line 66 already powers the live counter:
```typescript
const selectedCount = binders.filter((b) => selection[b.name]).length;
```

From `src/app/admin/import/_components/import-client.tsx:140` (selector to REMOVE per D-05):
```typescript
const defaultCheckedFor = useBinderImportStore((s) => s.defaultCheckedFor);
```

From `src/app/admin/import/_components/import-client.tsx:244-248` (init loop — replace `defaultCheckedFor(b)` with `false`):
```typescript
const initialSelection: Record<string, boolean> = {};
for (const b of binders as BinderSummary[]) {
  initialSelection[b.name] = defaultCheckedFor(b);   // ← per D-05 becomes `false`
}
setPickerSelection(initialSelection);
```

From `src/app/admin/import/_components/import-client.tsx:624-631` (current `<BinderPicker>` usage — wire `onBulkSet`):
```tsx
<BinderPicker
  binders={stage.binders}
  knownBinderNames={knownBinderNamesFn()}
  selection={pickerSelection}
  onToggle={(name, checked) =>
    setPickerSelection((prev) => ({ ...prev, [name]: checked }))
  }
  // ADD: onBulkSet={(names, checked) => setPickerSelection((prev) => { const next = {...prev}; for (const n of names) next[n] = checked; return next; })}
/>
```

From `src/app/admin/import/_components/import-client.tsx:633-648` (existing button styling — mirror for Select/Deselect; use SMALLER size):
```tsx
className="px-4 py-2 text-sm font-semibold rounded-md border border-zinc-300 dark:border-zinc-600 hover:bg-zinc-100 dark:hover:bg-zinc-800 transition-colors"
```

From `src/app/admin/import/_components/import-client.tsx:250-257` (will-delete init — DO NOT TOUCH per D-05):
```typescript
const willDelete = computeMissingBinders(binders as BinderSummary[], knownBinders);
// Default-CHECKED per D-11 (Phase 19) — UNCHANGED in v1.4.
const initialWillDelete: Record<string, boolean> = {};
for (const name of willDelete) initialWillDelete[name] = true;
setWillDeleteSelection(initialWillDelete);
```

From `src/app/admin/import/_components/import-client.tsx:270-284` (`handleConfirmPicker` guard — exists already; verify Continue button stays disabled when both are empty):
```typescript
if (selectedBinders.length === 0 && willDeleteBinders.length === 0) {
  // Stay on the picker; rely on the disabled state of the Continue button.
  return;
}
```
There is an existing `canContinue` boolean computed elsewhere in the file (line 644 — `disabled={!canContinue}`). Locate it via Grep and extend to ensure `canContinue === false` exactly when `selectedCount === 0 && willDeleteCount === 0`.

From `src/lib/store/binder-import-store.ts:18-32` (current interface — REMOVE `defaultCheckedFor`):
```typescript
export interface BinderImportState {
  lastSelection: Record<string, boolean>;
  lastUsedAt: string | null;
  setLastSelection: (selection: Record<string, boolean>) => void;
  recordCommit: (selection: Record<string, boolean>) => void;
  clearSelection: () => void;
  defaultCheckedFor: (binder: { name: string; isNew: boolean }) => boolean;   // ← REMOVE per D-05 (preferred)
  knownBinderNames: () => string[];
}
```

From `src/lib/store/binder-import-store.ts:53-58` (implementation to REMOVE):
```typescript
defaultCheckedFor: ({ name, isNew }) => {
  if (name === "unsorted") return false;
  const prior = get().lastSelection[name];
  return prior ?? isNew;
},
```

From `src/lib/store/binder-import-store.ts:4-17` (file-header docblock to REWRITE — mentions D-09/D-10 memory semantics that no longer apply):
```typescript
/**
 * Phase 19 D-09 / D-10 — operator's binder selection memory.
 * ...
 * D-08: `unsorted` is FORCED to default-UNCHECKED on every render regardless
 * of `lastSelection` content...
 */
```
After Plan 23-02: `lastSelection`, `recordCommit`, and `knownBinderNames` are RETAINED because `computeMissingBinders` at `binder-picker.tsx:33-39` still depends on `knownBinderNames()` to compute the will-delete set (`import-client.tsx:149` + `:250`). Only the picker's per-binder check memory is removed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop defaultCheckedFor from the zustand store; rewrite the file-header docblock</name>
  <files>src/lib/store/binder-import-store.ts</files>
  <read_first>
    - src/lib/store/binder-import-store.ts (in full — 75 lines)
    - .planning/phases/23-import-ux-price-refresh/23-PATTERNS.md (section "src/lib/store/binder-import-store.ts" — Shape A vs Shape B; PATTERNS recommends Shape B)
    - .planning/research/PITFALLS.md (Pitfall 3 — explicit "deliberate, documented re-architecture" warning; the file-header docblock MUST be rewritten or future maintainers will re-implement the dead feature)
    - src/app/admin/import/_components/import-client.tsx (search for `defaultCheckedFor` usages — should be exactly two: the selector at line 140 and the call at line 246; both go away in Task 2)
  </read_first>
  <action>
    Apply **Shape B** from PATTERNS.md (preferred: delete from interface AND implementation):

    1. **Interface (line 30):** REMOVE the line `defaultCheckedFor: (binder: { name: string; isNew: boolean }) => boolean;`. Leave `lastSelection`, `lastUsedAt`, `setLastSelection`, `recordCommit`, `clearSelection`, `knownBinderNames` UNCHANGED — they are still consumed (`recordCommit` by `handleConfirmPicker`, `knownBinderNames` by `computeMissingBinders` via `import-client.tsx:149`).

    2. **Implementation (lines 53-58):** REMOVE the entire `defaultCheckedFor: ({ name, isNew }) => { ... }` member from the `create()` body. Leave the other members (`setLastSelection`, `recordCommit`, `clearSelection`, `knownBinderNames`) UNCHANGED. The `persist()` config (lines 62-72) and `partialize` (lines 66-69) stay untouched.

    3. **Rewrite the file-header docblock (lines 4-17).** The current docblock references Phase 19 D-09/D-10/D-08 "operator's binder selection memory" semantics that no longer apply to the picker. Replace it with text that:
       - Cites Phase 23 / v1.4 D-05 by name.
       - States the new invariant explicitly: "Picker opens with every binder UNCHECKED on every session, regardless of any prior `lastSelection` content. Select all / Deselect all in `binder-picker.tsx` are the operator's recovery affordances."
       - Explains why `lastSelection` and `recordCommit` are RETAINED: "Required by `computeMissingBinders` at `binder-picker.tsx:33-39` (consumed via `knownBinderNames()` to compute the will-delete amber panel set). Removing `lastSelection` would break the v1.3 D-11 will-delete behavior, which is UNCHANGED in v1.4."
       - Mentions the storage key (`BINDER_IMPORT_STORAGE_KEY = "viki-binder-import-selection"`) and version (`BINDER_IMPORT_STORE_VERSION = 1`) are unchanged — no migration needed; the orphan `defaultCheckedFor` key was never persisted (it was a derived getter, not state) so removing it has zero on-disk consequence.

    4. Do NOT bump `BINDER_IMPORT_STORE_VERSION` — `defaultCheckedFor` was a derived function, not persisted state. The `partialize` config (lines 66-69) only persists `lastSelection` and `lastUsedAt`; both are retained.

    5. Do NOT delete `BINDER_IMPORT_STORAGE_KEY` or `BINDER_IMPORT_STORE_VERSION` exports — they are imported by tests.

    NEVER:
    - Touch `setLastSelection`, `recordCommit`, `clearSelection`, `knownBinderNames`, `lastSelection`, or `lastUsedAt` (D-05 explicit: the will-delete panel still depends on `knownBinderNames`).
    - Bump the persistence version (no on-disk schema change).
    - Convert the file to a partial removal (e.g., `defaultCheckedFor: () => false` stub). Shape B (full deletion) is preferred per PATTERNS.md to prevent future maintainers from reading stale documentation and resurrecting the feature.
  </action>
  <verify>
    <automated>! pnpm tsc --noEmit 2>&amp;1 | grep -E "binder-import-store\.ts" >/dev/null &amp;&amp; pnpm vitest run src/lib/store --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "defaultCheckedFor" src/lib/store/binder-import-store.ts` returns 0 (member fully removed from interface + body)
    - Source assertion: `grep -nE "(lastSelection|recordCommit|knownBinderNames|clearSelection|setLastSelection)" src/lib/store/binder-import-store.ts` returns &ge; 5 matches (all retained members still present)
    - Source assertion: `grep -nE "(D-05|v1.4|Phase 23)" src/lib/store/binder-import-store.ts` returns &ge; 1 match (docblock rewritten to cite the new decision)
    - Source assertion: `grep -E "BINDER_IMPORT_STORE_VERSION\\s*=\\s*1" src/lib/store/binder-import-store.ts` returns 1 match (version NOT bumped)
    - Source assertion: `grep -nE "Phase 19 D-09|Phase 19 D-10" src/lib/store/binder-import-store.ts` returns 0 matches (stale docblock fully replaced)
    - Behavior: tsc errors mentioning `binder-import-store.ts`: 0 lines (`pnpm tsc --noEmit 2>&1 | grep -E "binder-import-store\.ts"` produces no output); errors in OTHER files referencing `defaultCheckedFor` are EXPECTED at this point — they will be fixed by Task 2.
    - Behavior: `pnpm vitest run src/lib/store` exits 0 (any existing zustand-store unit tests under `src/lib/store/__tests__/` still pass; if a test imports `defaultCheckedFor`, expect a typecheck failure that flags the test for update in Task 2 — but the runtime suite for the store itself must pass)
  </acceptance_criteria>
  <done>
    `defaultCheckedFor` is fully removed from `binder-import-store.ts` (interface declaration + implementation). The file-header docblock now cites Phase 23 / D-05 and explains why `lastSelection` / `recordCommit` / `knownBinderNames` are retained. The storage key, version, and `partialize` config are untouched. TypeScript errors from `import-client.tsx` referencing the now-missing `defaultCheckedFor` are deferred to Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Select all / Deselect all to BinderPicker (new onBulkSet prop), wire from import-client.tsx (single-render batch update), drop the defaultCheckedFor call site</name>
  <files>src/app/admin/import/_components/binder-picker.tsx, src/app/admin/import/_components/import-client.tsx</files>
  <read_first>
    - src/app/admin/import/_components/binder-picker.tsx (in full — 124 lines)
    - src/app/admin/import/_components/import-client.tsx (offset 120 limit 30 for `defaultCheckedFor` selector at :140; offset 240 limit 40 for the init loop at :244-248; offset 600 limit 80 for the `<BinderPicker>` usage + Continue button at :624-648; Grep first for the `canContinue` definition before reading the surrounding range — do NOT read the whole 700+-line file)
    - .planning/phases/23-import-ux-price-refresh/23-PATTERNS.md (sections "src/app/admin/import/_components/binder-picker.tsx", "src/app/admin/import/_components/import-client.tsx", "Pattern 3: Controlled checkbox-group + batch selection actions")
    - .planning/phases/23-import-ux-price-refresh/23-CONTEXT.md (D-05, D-15)
    - .planning/research/PITFALLS.md (Pitfalls 3, 8, 15 — picker memory, empty-selection commit, keyboard nav)
    - node_modules/next/dist/docs (only if Next conventions become relevant — these are pure React 19 client components, no Next-specific surface)
  </read_first>
  <action>
    Two related files; do BinderPicker first so the new prop exists, then wire it from the parent. Both edits are surgical (no full rewrites).

    **A. `src/app/admin/import/_components/binder-picker.tsx`:**

    1. **Extend `BinderPickerProps` (lines 19-27)** with a new required field:
       - `onBulkSet: (binderNames: string[], checked: boolean) => void;`
       - Add a JSDoc line above it: `/** Bulk set callback — used by Select all / Deselect all to flip every binder in ONE parent render (D-15). */`

    2. **Destructure `onBulkSet`** in the function signature at line 43-47 (currently destructures `binders, selection, onToggle`).

    3. **Extend the `<header>` block at lines 73-80** to add two buttons on the right (counter `({selectedCount} of {binders.length})` stays on the LEFT — Claude's Discretion per CONTEXT.md):
       - Wrap the existing `<h2>` and the new `<div>` of buttons under the existing `flex items-center justify-between` parent.
       - Add a `<div className="flex items-center gap-2">` containing two `<button type="button">` elements.
       - First button text: "Select all". `onClick={() => onBulkSet(binders.map((b) => b.name), true)}`.
       - Second button text: "Deselect all". `onClick={() => onBulkSet(binders.map((b) => b.name), false)}`.
       - Both buttons share these Tailwind classes (smaller weight than the primary CTA): `"text-xs px-2 py-1 rounded border border-zinc-300 dark:border-zinc-600 hover:bg-zinc-100 dark:hover:bg-zinc-800 transition-colors"` — derived from the existing Cancel button styling at `import-client.tsx:636-637`, sized down because they are secondary affordances.
       - Native `<button type="button">` (NOT `role="button"` or `<a>` per PITFALLS Pitfall 15 — tab order is filename → Select all → Deselect all → first checkbox → ... → Continue).

    4. **Counter wiring (IMPORT-UX-05) — verify, don't rewrite:** The header already renders `({selectedCount} of {binders.length})` from the derivation at line 66 `const selectedCount = binders.filter((b) => selection[b.name]).length;`. Because `selection` is a controlled prop, any parent state update re-renders the picker and recomputes `selectedCount` in the same render. No additional work needed here — but the binder-picker test (Task 3) MUST assert the counter updates from "0 of N" to "N of N" within one click cycle.

    5. **No internal state added** (D-15 — picker stays controlled). The picker remains a pure render-from-props leaf.

    **B. `src/app/admin/import/_components/import-client.tsx`:**

    1. **Remove the `defaultCheckedFor` selector at line 140:** delete the line `const defaultCheckedFor = useBinderImportStore((s) => s.defaultCheckedFor);`. Verify no other reference to `defaultCheckedFor` remains in this file via Grep before saving.

    2. **Replace the init loop at lines 244-247:** change `initialSelection[b.name] = defaultCheckedFor(b);` to `initialSelection[b.name] = false;`. Keep the surrounding `for (const b of binders as BinderSummary[]) { ... }` structure intact. Update the inline comment at line 242-243 from "Initialize the picker state from the store's default-checked derivation." to "D-05 / v1.4: Picker opens with every binder UNCHECKED on every session. Select all is the recovery affordance." The will-delete init at lines 250-257 is UNCHANGED per D-05.

    3. **Wire `onBulkSet` on the `<BinderPicker>` usage at lines 624-631.** Add a new prop after `onToggle`:
       - The callback must apply the bulk update in a SINGLE `setPickerSelection` call (D-15 — avoid N renders):
         - Pseudocode: `onBulkSet={(names, checked) => setPickerSelection((prev) => { const next = { ...prev }; for (const name of names) next[name] = checked; return next; })}`
       - This pattern matches the existing functional-updater style at line 629 (`onToggle`), just extended to loop the spread inside one updater call.

    4. **Confirm Continue button stays disabled when empty (IMPORT-UX-04):** Grep for `canContinue` in this file; it currently exists (used at line 644 `disabled={!canContinue}`). Verify it evaluates to `false` exactly when `Object.values(pickerSelection).filter(Boolean).length === 0 && Object.values(willDeleteSelection).filter(Boolean).length === 0`. If the existing `canContinue` derivation already covers this (it should — the existing v1.3 disabled-when-empty behavior is preserved), make NO change; just verify and document with an inline comment near `canContinue` referring to D-05 / IMPORT-UX-04. If `canContinue` does NOT already check both arrays, extend it now to do so. Either way, add helper text directly above or below the Continue button (visible only when `!canContinue`): a `<p className="text-xs text-zinc-500 dark:text-zinc-400">` with text "Select at least one binder to continue. Use Select all to start with everything checked." — placed between the picker `<BinderPicker>` and the action-button row at `flex items-center justify-end gap-3` (lines 633). The helper text MUST be associated with the Continue button via `aria-describedby` (give the helper text `id="continue-disabled-helper"` and add `aria-describedby="continue-disabled-helper"` to the Continue `<button>` when disabled). This satisfies PITFALLS Pitfall 8.

    5. **Update the on-pass to `<BinderPicker>` — full props block** (after Task 2 edits) MUST include the new `onBulkSet` prop AND continue to pass `binders`, `knownBinderNames`, `selection`, and `onToggle`. Do NOT alter `knownBinderNames={knownBinderNamesFn()}` — the will-delete computation still needs it.

    NEVER:
    - Touch the will-delete amber panel logic at lines 250-257 or anywhere else (D-05 explicit; will-delete default-CHECKED is UNCHANGED).
    - Add internal state to `BinderPicker` (D-15: picker stays controlled).
    - Re-read or duplicate `defaultCheckedFor` logic anywhere — it's gone.
    - Call `onToggle` in a loop from Select all / Deselect all (would cause N renders — D-15 explicitly requires the single-call pattern).
    - Persist any picker selection back to `lastSelection` from inside this task — `recordCommit` is still called by the existing `handleConfirmPicker` flow at commit time; that wiring is unchanged.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&amp;1 | tail -30 ; pnpm vitest run src/app/admin/import 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "onBulkSet" src/app/admin/import/_components/binder-picker.tsx` returns &ge; 3 matches (interface declaration + destructure + two button onClick handlers — counted via separate matches per onClick)
    - Source assertion: `grep -nE "Select all|Deselect all" src/app/admin/import/_components/binder-picker.tsx` returns 2 matches
    - Source assertion: `grep -cE "<button type=\"button\"" src/app/admin/import/_components/binder-picker.tsx` returns &ge; 2 matches (Select all + Deselect all)
    - Source assertion: `grep -n "selectedCount" src/app/admin/import/_components/binder-picker.tsx` still returns &ge; 1 match (counter derivation preserved — IMPORT-UX-05)
    - Source assertion: `grep -c "defaultCheckedFor" src/app/admin/import/_components/import-client.tsx` returns 0 (selector AND call site both removed)
    - Source assertion: `grep -n "onBulkSet" src/app/admin/import/_components/import-client.tsx` returns &ge; 1 match (wired to BinderPicker)
    - Source assertion: `grep -nE "initialSelection\\[b\\.name\\]\\s*=\\s*false" src/app/admin/import/_components/import-client.tsx` returns 1 match (init loop sets to literal `false`)
    - Source assertion: `grep -nE "aria-describedby" src/app/admin/import/_components/import-client.tsx` returns &ge; 1 match (Continue button references helper text id)
    - Source assertion: the will-delete init block at lines 250-257 is UNCHANGED (run `grep -c "initialWillDelete\\[name\\] = true" src/app/admin/import/_components/import-client.tsx` — should still return 1)
    - Behavior: `pnpm tsc --noEmit` exits 0 (the typecheck failures introduced by Task 1 are resolved by Task 2's removal of the `defaultCheckedFor` references)
    - Behavior: existing import-related tests under `src/app/admin/import/_components/__tests__/` still pass (some assertions may need updates in Task 3, but typecheck must pass HERE)
  </acceptance_criteria>
  <done>
    `BinderPicker` exposes a new `onBulkSet` prop and renders Select all / Deselect all native `<button>` elements in its header. `import-client.tsx` no longer references `defaultCheckedFor`, initializes every binder to `false` on picker open, wires `onBulkSet` via a single `setPickerSelection` functional updater (one render not N), preserves the will-delete amber panel default-CHECKED behavior, and shows helper text + `aria-describedby` when the Continue button is disabled. `pnpm tsc --noEmit` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend / create binder-picker + import-client test files covering Select all, Deselect all, live counter, disabled Continue, and zero-memory across sessions</name>
  <files>src/app/admin/import/_components/__tests__/binder-picker.test.tsx, src/app/admin/import/_components/__tests__/import-client.test.tsx (extend if exists; create if not)</files>
  <read_first>
    - src/app/admin/import/_components/binder-picker.tsx (final state after Task 2)
    - src/app/admin/import/_components/import-client.tsx (read the picker stage render — offset 580 limit 80 — and the `canContinue` derivation; locate via Grep first)
    - Any existing `src/app/admin/import/_components/__tests__/*.test.tsx` (list with `ls`; if `binder-picker.test.tsx` exists, extend; if `import-client.test.tsx` exists, extend; otherwise create)
    - src/lib/store/binder-import-store.ts (final state after Task 1 — confirm `defaultCheckedFor` is gone)
    - One existing client-component test in the project to mirror import patterns (e.g., search for a `.test.tsx` file under `src/app/admin/*/_components/__tests__/` and use that as the local convention; do NOT introduce a new testing-library wrapper if the existing tests use a different setup)
  </read_first>
  <action>
    Create/extend two test files. Use `@testing-library/react` (project convention) + `vi.fn()` for callback assertions. NEVER env-gate; NEVER use `describe.skip` / `describe.runIf` / `it.skip`. Standard default-run vitest.

    **A. `binder-picker.test.tsx`** — pure component tests with controlled props. Use a small `renderWithControlledSelection` helper that wraps the picker with `useState` so click handlers can mutate state via callbacks. Each test case names the IMPORT-UX requirement it covers in the test description.

    Test cases:
    1. **IMPORT-UX-05 + initial render** — Given `binders = [{name: "a", isNew: false, rowCount: 10, sampleNames: []}, {name: "b", isNew: true, rowCount: 5, sampleNames: []}]` and `selection = { a: false, b: false }`, the header text contains "0 of 2".
    2. **IMPORT-UX-05 + live counter** — Click the checkbox for binder "a" (this fires `onToggle("a", true)` → wrapper updates state → re-render). Assert the header text now contains "1 of 2" within the same render cycle (testing-library's auto-flush handles this).
    3. **IMPORT-UX-01 + Select all** — Click the "Select all" button. Assert `onBulkSet` was called exactly once with `(["a", "b"], true)`. Then update the wrapper's `selection` to reflect the callback, re-render, and assert the header shows "2 of 2".
    4. **IMPORT-UX-02 + Deselect all** — After Select all has run, click "Deselect all". Assert `onBulkSet` was called with `(["a", "b"], false)`. Update wrapper state, re-render, assert header shows "0 of 2".
    5. **IMPORT-UX-01/02 + single-render guarantee (D-15)** — Spy on the wrapper's setState. Click Select all once. Assert setState was called exactly ONCE (not N times). This is the D-15 single-render invariant verification.
    6. **Native `<button type="button">` (PITFALLS Pitfall 15)** — Query the buttons by role+name; assert `tagName === "BUTTON"` and `type === "button"`. Assert tab order: rendering the picker into the DOM, then `userEvent.tab()` from the heading goes Select all → Deselect all → first checkbox (use `@testing-library/user-event` if available; otherwise verify `document.activeElement` after sequential `.focus()` calls).
    7. **`unsorted` binder rendering preserved** — When `binders` includes a binder named "unsorted", it renders last; Select all checks it too (no special-casing now that `defaultCheckedFor`'s D-08 "unsorted always unchecked" rule is gone — the operator must explicitly Deselect it via the row checkbox or by Deselect all + manual re-selection).

    **B. `import-client.test.tsx`** — parent-state behavior tests. If a file exists, extend it; if not, create with the minimum testing-library setup mirroring other client-component tests in the repo.

    Test cases (mock `next/navigation`'s `useRouter` and `next/dynamic` if needed):
    8. **IMPORT-UX-03 + fresh session, no localStorage** — Set up a fresh JSDOM `localStorage` (clear it before the test). Mount `<ImportClient currentTotal={0} />`. Simulate the stage transition to `picker` by mocking the fetch + stream consumer to return two binders `["binder-a", "binder-b"]`. Assert every checkbox in the rendered picker is UNCHECKED. Assert the Continue button is disabled.
    9. **IMPORT-UX-03 + returning session with prior localStorage** — Pre-populate `localStorage["viki-binder-import-selection"]` with `{ state: { lastSelection: { "binder-a": true, "binder-b": true }, lastUsedAt: "2026-05-19T00:00:00Z" }, version: 1 }`. Mount `<ImportClient>` and drive it to the picker stage with the same two binders. Assert: BOTH checkboxes are STILL unchecked (the prior `lastSelection` is IGNORED by the picker init per D-05). Assert: the will-delete panel default-CHECKED behavior is preserved when a prior binder is MISSING from the upload (e.g., upload only contains "binder-a"; will-delete contains "binder-b" and its checkbox is CHECKED — D-05 will-delete unchanged).
    10. **IMPORT-UX-04 + Continue disabled when both empty** — Same as test 8 (no selection, no will-delete). Assert: the Continue `<button>` is rendered with `disabled` attribute; helper text "Select at least one binder to continue. Use Select all to start with everything checked." is in the document; the button has `aria-describedby` pointing at the helper text's id.
    11. **IMPORT-UX-04 + Continue enabled after Select all** — Mount in the same fresh-session setup as test 8; click "Select all" in the picker header. Assert: Continue button is now enabled (no `disabled` attribute); helper text is no longer rendered.
    12. **IMPORT-UX-04 + Continue stays enabled with only will-delete checked** — Upload contains "binder-a" only; localStorage prior `lastSelection = { "binder-a": true, "binder-b": true }`. Will-delete panel auto-checks "binder-b" (D-05 unchanged behavior). Assert: even with zero picker selection, Continue is ENABLED because `willDeleteBinders.length > 0`.
    13. **D-05 + `defaultCheckedFor` removal — type-level guard** — Importing `useBinderImportStore` and accessing `.defaultCheckedFor` is a TypeScript error. Use a `// @ts-expect-error` line above the access to assert the compiler rejects it (this guards against future re-introduction). The test body asserts the property is `undefined` at runtime as a secondary check.

    For each test that covers a labeled requirement, include the literal requirement ID (`IMPORT-UX-NN`) in the test name string so the audit trail surfaces clean greps.

    NEVER:
    - Env-gate any of these tests (no `describe.skip` / `runIf`).
    - Mock the zustand store with a stub that re-introduces `defaultCheckedFor` (would defeat test 13).
    - Test the will-delete amber panel's NEW default behavior — D-05 explicitly leaves it UNCHANGED; only test 9 + test 12 confirm the existing v1.3 D-11 behavior is preserved.
    - Add a snapshot test for the picker — snapshots drift; assert specific text/role/attribute presence instead.
  </action>
  <verify>
    <automated>pnpm vitest run src/app/admin/import/_components 2>&amp;1 | tail -60 ; pnpm tsc --noEmit 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -cE "IMPORT-UX-0[1-5]" src/app/admin/import/_components/__tests__/binder-picker.test.tsx src/app/admin/import/_components/__tests__/import-client.test.tsx` returns &ge; 6 (each of the 5 requirement IDs cited at least once across the test files, with at least one having two cases)
    - Source assertion: `grep -cE "(describe\\.skip|describe\\.runIf|it\\.skip|test\\.skip)" src/app/admin/import/_components/__tests__/binder-picker.test.tsx src/app/admin/import/_components/__tests__/import-client.test.tsx` returns 0
    - Source assertion: `grep -nE "@ts-expect-error" src/app/admin/import/_components/__tests__/import-client.test.tsx` returns &ge; 1 (the type-level guard against future defaultCheckedFor reintroduction — test 13)
    - Source assertion: tests reference the literal storage key `viki-binder-import-selection` in test 9 setup: `grep -c "viki-binder-import-selection" src/app/admin/import/_components/__tests__/import-client.test.tsx` returns &ge; 1
    - Behavior: `pnpm vitest run src/app/admin/import/_components` exits 0 with all new cases passing
    - Behavior assertion (test 5): the `setState` spy in the Select all path is called exactly ONCE per click (D-15 single-render verification)
    - Behavior assertion (tests 8-9): the picker opens UNCHECKED in both fresh-localStorage and prior-`lastSelection`-populated cases (D-05 zero-memory)
    - Behavior assertion (test 12): Continue is enabled when only will-delete entries are checked (D-05 will-delete unchanged)
    - Behavior: `pnpm tsc --noEmit` exits 0 (including the `@ts-expect-error` directive being valid — i.e., accessing `.defaultCheckedFor` IS a typecheck error, otherwise `@ts-expect-error` itself errors)
    - Behavior: full `pnpm vitest run` exits 0 (no regression in the broader 464-test suite per STATE.md baseline)
  </acceptance_criteria>
  <done>
    `binder-picker.test.tsx` exercises Select all / Deselect all / live counter / native button + tab order / single-render (D-15). `import-client.test.tsx` exercises fresh-session + returning-session both opening UNCHECKED (D-05 zero-memory), Continue disabled-with-helper when both arrays are empty (IMPORT-UX-04 + Pitfall 8), Continue enabled paths, and a `@ts-expect-error` type-level guard preventing future re-introduction of `defaultCheckedFor`. All cases default-run; no env gates. `pnpm vitest run` and `pnpm tsc --noEmit` both exit 0; the broader test suite is regression-free.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → `BinderPicker` controlled state | Pure UI; no network surface introduced by this plan |
| Operator browser → `localStorage["viki-binder-import-selection"]` | Existing zustand persist; this plan REDUCES the surface (drops `defaultCheckedFor` consumer); no new persistence introduced |
| `import-client.tsx` → `/api/admin/import/preview` and `/api/admin/import/commit` | Existing v1.3 surface; this plan does NOT modify request shapes (Continue button still sends the same `selectedBinders` + `willDeleteBinders` arrays) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-15 | Tampering | localStorage `viki-binder-import-selection` content | accept | `lastSelection` is operator-controlled by design; attacker control over an admin's own localStorage requires already-elevated browser access. D-05 INDEPENDENTLY ensures the picker opens unchecked regardless of `lastSelection` content — even a tampered store cannot pre-check binders. |
| T-23-16 | Elevation of Privilege | Empty-commit edge case | mitigate | IMPORT-UX-04 + Pitfall 8: Continue button is `disabled` with `aria-describedby` helper text when both `selectedBinders.length === 0 && willDeleteBinders.length === 0`. The defensive guard inside `handleConfirmPicker` (line 280) returns early on the same condition — defense in depth. Task 3 test 10 verifies. |
| T-23-17 | Information Disclosure | Will-delete amber panel showing prior binder names | accept | The names come from `lastSelection` (operator's own prior imports), shown only to the same admin that authenticated. No cross-tenant or cross-user leakage. v1.3 baseline behavior; UNCHANGED by D-05. |
| T-23-18 | Repudiation | Operator confusion: "did I actually select these binders?" | mitigate | Live "X of Y selected" counter in the picker header (IMPORT-UX-05) gives same-render feedback for every click. Will-delete panel separately surfaces destructive consequences. Both surfaces verified by Task 3 tests 1-2 + 12. |
| T-23-19 | Denial of Service | Repeated Select all / Deselect all flips causing excessive re-renders | mitigate | D-15 single-`setPickerSelection` batch (single render per click, regardless of binder count). Task 3 test 5 explicitly verifies setState is called once per Select all click. |
</threat_model>

<verification>
## Phase-level checks (after all three tasks complete)

1. **TypeScript:** `pnpm tsc --noEmit` exits 0 — confirms `defaultCheckedFor` is gone everywhere AND the `@ts-expect-error` directive in test 13 successfully suppresses the expected type error (which proves the access IS a type error).
2. **Test suite:** `pnpm vitest run src/app/admin/import/_components` exits 0 with all new cases passing.
3. **Full suite (no regression):** `pnpm vitest run` exits 0 with prior baseline (464 tests / 2 skipped per STATE.md) plus the new cases added in this plan.
4. **`defaultCheckedFor` complete removal:** `grep -rE "defaultCheckedFor" src/ --include="*.ts" --include="*.tsx" | grep -v "@ts-expect-error\\|// removed\\|# removed"` returns 0 lines (the `@ts-expect-error` line is the ONLY allowed mention, in test 13).
5. **Header counter still wired:** `grep -n "of {binders.length}" src/app/admin/import/_components/binder-picker.tsx` returns &ge; 1 match (IMPORT-UX-05).
6. **Will-delete behavior unchanged:** `grep -n "initialWillDelete\\[name\\] = true" src/app/admin/import/_components/import-client.tsx` returns 1 match (D-05 explicit non-modification of the amber panel default).
7. **Controlled-component invariant (D-15):** `grep -cE "useState\\b" src/app/admin/import/_components/binder-picker.tsx` returns 0 — no new internal state inside the picker.
8. **Native `<button>` for new affordances (Pitfall 15):** `grep -nE "<(div|span|a)[^>]*onClick=\\{(\\(\\)|.*onBulkSet)" src/app/admin/import/_components/binder-picker.tsx` returns 0 matches — neither Select all nor Deselect all uses a non-button element.
</verification>

<success_criteria>
- All 5 IMPORT-UX requirements (IMPORT-UX-01..05) are covered by code and tests in this plan.
- All 10 must_haves.truths verifiable by automated grep/test assertions.
- `defaultCheckedFor` is fully removed from the zustand store; runtime mention count in `src/` is 0 (excluding the test 13 `@ts-expect-error` guard).
- Picker opens with every binder unchecked in both fresh-session AND prior-localStorage cases (Task 3 tests 8-9).
- Select all / Deselect all flip every binder in ONE parent render (D-15; Task 3 test 5).
- Continue button disabled-with-helper text behavior + `aria-describedby` linkage holds for the empty-selection case (Task 3 test 10).
- Will-delete amber panel default-CHECKED behavior is unchanged from v1.3 (Task 3 tests 9 + 12).
- `pnpm tsc --noEmit` and `pnpm vitest run` both exit 0 with no regression in the broader test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/23-import-ux-price-refresh/23-02-SUMMARY.md` containing:
1. Standard summary template content (work completed, files changed, tests added, decisions implemented with D-05 / D-15 references).
2. Explicit note that `defaultCheckedFor` was fully REMOVED (Shape B from PATTERNS.md), with a one-line citation to PITFALLS Pitfall 3 explaining why a partial stub `() => false` was rejected (future maintainers would re-implement the dead feature).
3. Explicit note that the will-delete amber panel default-CHECKED behavior is UNCHANGED (cross-reference to Phase 19 D-11 invariant; cross-reference to D-05 explicit non-modification clause).
4. Test coverage map: each of IMPORT-UX-01..05 → which test case(s) cover it.
5. Tab-order verification result (manual or automated) — confirms PITFALLS Pitfall 15 mitigation.
</output>
</output>
