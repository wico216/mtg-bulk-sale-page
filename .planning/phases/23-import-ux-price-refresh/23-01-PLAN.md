---
phase: 23-import-ux-price-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/price-refresh.ts
  - src/app/api/cron/refresh-prices/route.ts
  - src/app/api/admin/prices/refresh/route.ts
  - src/app/admin/health/_components/refresh-prices-button.tsx
  - vercel.json
  - src/lib/enrichment.ts
  - src/db/admin-health.ts
  - src/db/queries.ts
  - src/app/api/admin/health/route.ts
  - src/app/admin/health/page.tsx
  - src/lib/price-refresh.test.ts
  - src/app/api/cron/refresh-prices/__tests__/route.test.ts
  - src/app/api/admin/prices/refresh/__tests__/route.test.ts
autonomous: true
requirements:
  - PRICE-REFRESH-01
  - PRICE-REFRESH-02
  - PRICE-REFRESH-03
  - PRICE-REFRESH-04
  - PRICE-REFRESH-05
  - PRICE-REFRESH-06
  - PRICE-REFRESH-07
  - PRICE-REFRESH-08
  - PRICE-REFRESH-09
  - PRICE-REFRESH-10
  - PRICE-REFRESH-11
tags:
  - cron
  - scryfall
  - price-refresh
  - admin-health
  - audit-log
  - postgres-advisory-lock
user_setup:
  - service: vercel-cron
    why: "Daily price refresh scheduling + Bearer-token authentication"
    env_vars:
      - name: CRON_SECRET
        source: "Operator-generated locally with `openssl rand -hex 32`, pasted into Vercel project env (Production + Preview)"
    dashboard_config:
      - task: "Set CRON_SECRET in Vercel project environment (Production scope at minimum)"
        location: "Vercel Dashboard -> Project -> Settings -> Environment Variables"
      - task: "Redeploy after setting CRON_SECRET so the env var is injected into the function bundle"
        location: "Vercel Dashboard -> Deployments -> Redeploy latest"
      - task: "Verify cronSecret reports 'configured' on /admin/health after redeploy"
        location: "https://wikos-spellbinder.vercel.app/admin/health"
must_haves:
  truths:
    - "GET /api/cron/refresh-prices with no Authorization header returns 401 (D-12)"
    - "GET /api/cron/refresh-prices when CRON_SECRET env is missing returns 401 (fails closed) (D-12, D-13)"
    - "GET /api/cron/refresh-prices with valid `Authorization: Bearer ${CRON_SECRET}` returns 200 and writes one admin_audit_log row with action='price_refresh'"
    - "POST /api/admin/prices/refresh without an admin session returns 401, with admin but rate-limited returns 429"
    - "Postgres advisory lock single-flights concurrent cron+manual; the second caller returns HTTP 409 from /api/admin/prices/refresh (and a quiet 200 with reason='locked' from the cron route) (D-08)"
    - "runPriceRefresh UPDATEs by 5-segment cards.id composite PK (NEVER by scryfall_id) and applies getPrice(prices, finish) per row (D-09)"
    - "When Scryfall returns 'not_found' for a card, the refresh preserves the prior cards.price (NEVER writes NULL); rows with no scryfallId are skipped entirely; only writes NULL when Scryfall explicitly returns prices.usd === null (D-10)"
    - "Audit metadata is exactly {trigger, updated, unchanged, failed, skipped, durationMs} — no failedSample[] and no errors[] (D-04)"
    - "AdminAuditAction TS union AND the runtime allowed-list in queries.ts both include 'price_refresh' — otherwise audit reads coerce to 'inventory.update' (D-16)"
    - "envChecks() in /api/admin/health route AND the page-local envChecks() both report cronSecret as the literal 'configured' or 'missing' (never the value) (D-13)"
    - "/admin/health top-level ok flips to false when cronSecret is 'missing' (D-13)"
    - "/admin/health renders a 'Last price refresh' tile that REPLACES the dead 'Notification failures (24h)' tile; the dl grid stays lg:grid-cols-4 (D-06)"
    - "RefreshPricesButton renders next to the Last price refresh tile, shows 'Refreshing…' while POST is in flight, calls router.refresh() on 200, shows inline error for 5s on 409 / 5xx (D-03)"
    - "Cron handler test file's first 5 lines literally contain the string 'NOT env-gated' and the test runs by default (no describe.skip / vi.runIf gating) (D-01, D-11)"
    - "src/lib/price-refresh.ts is imported via 'server-only'; no client component can import it (D-12)"
    - "vercel.json exists at repo root with a single crons[] entry: path='/api/cron/refresh-prices', schedule='0 9 * * *' (D-07)"
    - "23-01-SUMMARY.md contains a '## Operator Setup' section with the openssl rand -hex 32 runbook AND a section explaining WHY no Tier 2 live-DB test exists (v1.3.5 retrospective) (D-02, D-01)"
    - "cards.price is integer cents; runPriceRefresh converts Scryfall USD to cents via Math.round(parseFloat(usd) * 100) — never stores floats or dollars (D-14)"
    - "This plan is the wave-1 ship-first deliverable; Plan 23-02 depends on it per orchestrator wave-ordering (D-17)"
    - "Both route handlers export `export const maxDuration = 300;` to use the Vercel Hobby 2026 fluid-compute ceiling, giving ~11x headroom over the ~26s cold-cache refresh (D-18)"
  artifacts:
    - path: "src/lib/price-refresh.ts"
      provides: "server-only runPriceRefresh({trigger, actorEmail?}) shared service"
      contains: "import \"server-only\""
      exports: ["runPriceRefresh", "PriceRefreshSummary", "PriceRefreshLockedError"]
    - path: "src/app/api/cron/refresh-prices/route.ts"
      provides: "Bearer-gated cron GET handler"
      contains: "export const maxDuration = 300"
      exports: ["GET", "runtime", "maxDuration"]
    - path: "src/app/api/admin/prices/refresh/route.ts"
      provides: "Admin POST handler with requireAdmin + ADMIN_BULK rate-limit"
      exports: ["POST", "runtime", "maxDuration"]
    - path: "src/app/admin/health/_components/refresh-prices-button.tsx"
      provides: "Client mutation button with button-local state"
      contains: "\"use client\""
      exports: ["RefreshPricesButton"]
    - path: "vercel.json"
      provides: "Daily cron schedule"
      contains: "0 9 * * *"
    - path: "src/lib/enrichment.ts"
      provides: "Exported getPrice(prices, finish) ladder — single source of truth"
      contains: "export function getPrice"
    - path: "src/db/admin-health.ts"
      provides: "AdminHealthSnapshot with lastPriceRefreshAt field"
      contains: "lastPriceRefreshAt"
    - path: "src/db/queries.ts"
      provides: "AdminAuditAction union and runtime allowed-list extended with 'price_refresh'"
      contains: "price_refresh"
    - path: "src/app/api/admin/health/route.ts"
      provides: "API exposes cronSecret check + lastPriceRefreshAt; ok=false when cronSecret missing"
      contains: "cronSecret"
    - path: "src/app/admin/health/page.tsx"
      provides: "Replaces dead Notification-failures tile with Last price refresh tile + RefreshPricesButton"
      contains: "Last price refresh"
    - path: "src/app/api/cron/refresh-prices/__tests__/route.test.ts"
      provides: "Tier 1 default-run cron handler test"
      contains: "NOT env-gated"
  key_links:
    - from: "src/app/api/cron/refresh-prices/route.ts"
      to: "src/lib/price-refresh.ts"
      via: "import { runPriceRefresh } from '@/lib/price-refresh'"
      pattern: "runPriceRefresh\\(\\s*\\{\\s*trigger:\\s*['\"]cron['\"]"
    - from: "src/app/api/admin/prices/refresh/route.ts"
      to: "src/lib/price-refresh.ts"
      via: "import { runPriceRefresh } from '@/lib/price-refresh'"
      pattern: "runPriceRefresh\\(\\s*\\{\\s*trigger:\\s*['\"]manual['\"]"
    - from: "src/lib/price-refresh.ts"
      to: "src/lib/enrichment.ts"
      via: "import { getPrice } from '@/lib/enrichment'"
      pattern: "import \\{[^}]*getPrice[^}]*\\} from"
    - from: "src/lib/price-refresh.ts"
      to: "src/lib/scryfall.ts"
      via: "fetchCardsByScryfallIds(uniqueIds)"
      pattern: "fetchCardsByScryfallIds"
    - from: "src/lib/price-refresh.ts"
      to: "src/db/queries.ts"
      via: "createAdminAuditEntry({ action: 'price_refresh', ... })"
      pattern: "action:\\s*['\"]price_refresh['\"]"
    - from: "src/app/admin/health/page.tsx"
      to: "src/app/admin/health/_components/refresh-prices-button.tsx"
      via: "<RefreshPricesButton />"
      pattern: "<RefreshPricesButton\\s*/>"
    - from: "src/app/admin/health/_components/refresh-prices-button.tsx"
      to: "/api/admin/prices/refresh"
      via: "fetch('/api/admin/prices/refresh', { method: 'POST' })"
      pattern: "fetch\\(\\s*['\"]/api/admin/prices/refresh['\"]"
    - from: "src/db/admin-health.ts"
      to: "admin_audit_log"
      via: "SELECT MAX(created_at) WHERE action='price_refresh'"
      pattern: "action\\s*=\\s*['\"]price_refresh['\"]"
---

<objective>
Ship the daily price-refresh vertical slice: a shared `runPriceRefresh` service callable from two thin route handlers (Bearer-gated cron GET + admin-gated POST), backed by Postgres advisory-lock single-flighting, a per-row finish-aware Scryfall price ladder that updates by 5-segment `cards.id`, and an `/admin/health` surface that exposes `lastPriceRefreshAt` plus a fail-closed `cronSecret` env check.

Purpose: Make card prices autonomous (Vercel Cron daily at 09:00 UTC) and observable (operator can see "last refreshed" + click "Refresh now" on `/admin/health` + know when `CRON_SECRET` is missing). Avoid the v1.2 etched-mispricing bug (per-row, per-finish ladder; UPDATE by composite PK) and the v1.3.5 silent-skip bug (default-run unit tests; "NOT env-gated" header literally in the file).

Output: One new shared service, two new route handlers, one new client button, one new `vercel.json`, four MODIFY in lib/db/route/page, three new test files. Operator runbook for `CRON_SECRET` ships inline in `23-01-SUMMARY.md`.
</objective>

<execution_context>
@/home/wiko/Projects/active/mtg-bulk-sale-page/.claude/get-shit-done/workflows/execute-plan.md
@/home/wiko/Projects/active/mtg-bulk-sale-page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/23-import-ux-price-refresh/23-CONTEXT.md
@.planning/phases/23-import-ux-price-refresh/23-PATTERNS.md

@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/FEATURES.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

@./CLAUDE.md
@./AGENTS.md

<interfaces>
<!-- Extracted from codebase. Executor should use these directly. -->

From `src/db/queries.ts:258-266` (TypeScript union — MUST extend with 'price_refresh'):
```typescript
export type AdminAuditAction =
  | "inventory.update"
  | "inventory.delete_one"
  | "inventory.delete_many"
  | "inventory.delete_all"
  | "inventory.import_commit"
  | "order.status_update"
  | "order.cancel"
  | "order.restore_inventory";
```

From `src/db/queries.ts:441-455` (runtime allowed-list — MUST extend with 'price_refresh'):
```typescript
function normalizeAdminAuditAction(value: string): AdminAuditAction {
  const allowed: readonly AdminAuditAction[] = [
    "inventory.update",
    "inventory.delete_one",
    "inventory.delete_many",
    "inventory.delete_all",
    "inventory.import_commit",
    "order.status_update",
    "order.cancel",
    "order.restore_inventory",
  ];
  return allowed.includes(value as AdminAuditAction)
    ? (value as AdminAuditAction)
    : "inventory.update";   // <-- silent coercion if 'price_refresh' is missing from this list
}
```

From `src/db/queries.ts:281-288` (input shape for `createAdminAuditEntry`):
```typescript
export interface CreateAdminAuditEntryInput {
  action: AdminAuditAction;
  actorEmail?: string | null;
  targetType: AdminAuditTargetType;       // 'card' | 'inventory' | 'order' | 'import'
  targetId?: string | null;
  targetCount?: number | null;
  metadata?: Record<string, unknown>;
}
```

From `src/lib/enrichment.ts:114-128` (currently file-private; MUST flip to `export function`):
```typescript
function getPrice(
  prices: ScryfallCard["prices"],
  finish: Finish,
): number | null {
  const raw =
    finish === "etched"
      ? prices.usd_etched ?? prices.usd_foil ?? prices.usd
      : finish === "foil"
        ? prices.usd_foil ?? prices.usd_etched ?? prices.usd
        : prices.usd ?? prices.usd_foil ?? prices.usd_etched;
  if (raw == null) return null;
  const parsed = parseFloat(raw);
  return Number.isNaN(parsed) ? null : parsed;
}
```

From `src/lib/scryfall.ts:226-279` (reused as-is; rate-gated batched fetcher):
```typescript
export async function fetchCardsByScryfallIds(
  ids: string[],
  opts?: { onBatchComplete?: (batchCardCount: number) => void },
): Promise<Map<string, ScryfallCard>>;
// Returns a Map keyed by Scryfall ID; ids that Scryfall returns as `not_found`
// are simply absent from the Map (callers MUST `.has(id)` check).
```

From `src/db/admin-health.ts:20-25` + `:59-63` (extend):
```typescript
export interface AdminHealthSnapshot {
  database: "ok" | "error";
  lastOrderAt: string | null;
  lastImportAt: string | null;
  lastAuditAt: string | null;
  // ADD: lastPriceRefreshAt: string | null;
}

// Promise.all currently has 3 elements at lines 59-63; ADD a 4th:
//   lastTimestamp(sql`SELECT MAX(created_at) AS last_at FROM admin_audit_log WHERE action = 'price_refresh'`),
```

From `src/app/api/admin/health/route.ts:22-34, 46-57, 90-94`:
```typescript
export interface AdminHealthCheckStatuses {
  database: "ok" | "error";
  authSecret: "configured" | "missing";
  googleOAuth: "configured" | "missing";
  email: "configured" | "missing";
  // ADD: cronSecret: "configured" | "missing";
}

export interface AdminHealthRecent {
  lastOrderAt: string | null;
  lastImportAt: string | null;
  lastAuditAt: string | null;
  // REMOVE: notificationFailuresLast24h: number | null;
  // ADD:    lastPriceRefreshAt: string | null;
}

// envChecks() — ADD the line `const cronSecret = isPresent(process.env.CRON_SECRET) ? "configured" : "missing";`
// ok composition — ADD `&& checks.cronSecret === "configured"`
```

From `src/app/admin/health/page.tsx:27-43, 105-148, 207-244` (page-local envChecks() is a SEPARATE function from the route's — both need extending; checks[] array gets a 5th row; overallOk gets cronSecret; the dl grid at lines 207-234 stays `lg:grid-cols-4`; the dead tile at 226-233 + the explanatory paragraph at 235-244 are replaced).

From `src/lib/rate-limit.ts:634-638` (existing bucket, no change):
```typescript
RATE_LIMIT_BUCKETS.ADMIN_BULK  // 20 requests / minute
```

From `src/app/api/admin/cards/bulk-delete/route.ts:36-87` (canonical admin-route wrapper — copy shape into prices/refresh/route.ts; drop body parsing).

From `src/lib/auth/admin-check.ts:13`:
```typescript
export async function requireAdmin(): Promise<AdminSession | Response>;
// Result is `Response` when unauth/forbidden — pattern is `if (result instanceof Response) return result;`
```

From `src/db/schema.ts:43` (cards.price is `integer` cents, nullable):
```typescript
price: integer("price"),   // null means "Price N/A"
```

From `src/db/schema.ts:88-110` (admin_audit_log shape unchanged; `action TEXT` accepts any literal at SQL layer).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export getPrice, extend AdminAuditAction, add lastPriceRefreshAt to admin-health, wire health endpoint + page (read-side scaffolding)</name>
  <files>src/lib/enrichment.ts, src/db/queries.ts, src/db/admin-health.ts, src/app/api/admin/health/route.ts, src/app/admin/health/page.tsx, src/db/admin-health.test.ts (if exists), src/app/api/admin/health/__tests__/route.test.ts (if exists)</files>
  <read_first>
    - src/lib/enrichment.ts (in full; only lines 1-130 are load-bearing for this edit)
    - src/db/queries.ts (offset 250 limit 280 — covers the union at 258-266 AND the normalizer at 441-455 AND `createAdminAuditEntry` at 585-595; use Read with offsets, do NOT re-read full file)
    - src/db/admin-health.ts (in full — 72 lines)
    - src/app/api/admin/health/route.ts (in full — 118 lines)
    - src/app/admin/health/page.tsx (in full — 260 lines)
    - .planning/phases/23-import-ux-price-refresh/23-PATTERNS.md (sections "src/db/admin-health.ts", "src/app/api/admin/health/route.ts", "src/app/admin/health/page.tsx", "src/lib/enrichment.ts", "Audit action enum extension")
    - src/db/schema.ts (offset 30 limit 90 — confirms cards.price is `integer` cents and admin_audit_log.action is `text`)
  </read_first>
  <action>
    Execute five surgical extensions, in this order:

    1. **src/lib/enrichment.ts line 114** — change `function getPrice(` to `export function getPrice(`. Body and signature unchanged. The single in-file caller at line 277 (`card.price = getPrice(scryfallData.prices, card.finish);`) continues to work; no other change needed in this file. Per D-09.

    2. **src/db/queries.ts** — extend the `AdminAuditAction` union (lines 258-266) by adding `| "price_refresh"` as a NEW union member after `"order.restore_inventory"`. Then extend the runtime allowed-list inside `normalizeAdminAuditAction` (lines 441-455 — the `allowed: readonly AdminAuditAction[]` array literal) by adding `"price_refresh"` as a new array entry. Both changes are required (per PATTERNS.md "Audit Action Extension" — D-16 was incomplete at the TS layer). Without the runtime list extension, audit reads silently coerce `'price_refresh'` rows to `'inventory.update'` (queries.ts:454 fallback).

    3. **src/db/admin-health.ts** — extend `AdminHealthSnapshot` interface (lines 20-25) with `lastPriceRefreshAt: string | null;`. Extend the `Promise.all` at lines 59-63 with a fourth `lastTimestamp(...)` call using the SQL: ``sql`SELECT MAX(created_at) AS last_at FROM admin_audit_log WHERE action = 'price_refresh'` ``. Destructure into a fourth variable `lastPriceRefreshAt`. Extend the return object (lines 65-71) with `lastPriceRefreshAt`. Extend the DB-error early-return branch (lines 51-57) with `lastPriceRefreshAt: null`. The existing `admin_audit_log_action_idx` (schema.ts:107) covers the WHERE filter — no new index needed.

    4. **src/app/api/admin/health/route.ts** — extend `AdminHealthCheckStatuses` (lines 22-27) with `cronSecret: "configured" | "missing";`. REPLACE the `notificationFailuresLast24h: number | null;` field on `AdminHealthRecent` (line 33) with `lastPriceRefreshAt: string | null;` (per D-06; do NOT add a 5th field). Extend `envChecks()` (lines 46-57) with `const cronSecret = isPresent(process.env.CRON_SECRET) ? "configured" : "missing";` and add `cronSecret` to the returned object. Extend the `ok` composition (lines 90-94) with `&& checks.cronSecret === "configured"` (per D-13). In the response body (lines 96-107), populate `lastPriceRefreshAt: snapshot.lastPriceRefreshAt` and REMOVE the deferred `notificationFailuresLast24h: null` line + its inline comment. NEVER log `process.env.CRON_SECRET` value (PITFALLS Security Mistakes).

    5. **src/app/admin/health/page.tsx** — the page has its OWN page-local `envChecks()` (lines 27-43, distinct from the route handler's). Extend it with `cronSecret: isPresent(process.env.CRON_SECRET) ? "configured" : "missing"` in both the return-type annotation (lines 27-31) and the returned object (lines 32-42). Append a fifth entry to the `checks[]` array (after the `email` entry around lines 133-141) mirroring the row shape:
        - key: `"cronSecret"`
        - label: `"Cron secret"`
        - status: `envState.cronSecret`
        - hint when configured: `"CRON_SECRET is set; Vercel cron can authenticate to /api/cron/refresh-prices."`
        - hint when missing: `"CRON_SECRET is not set. Generate with: openssl rand -hex 32. Daily price refresh will 401 until configured."`
    Extend `overallOk` (lines 144-148) with `&& envState.cronSecret === "configured"`. Extend the snapshot fallback (lines 92-103) with `lastPriceRefreshAt: null`. In the `<dl className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">` at lines 207-234, REPLACE the dead "Notification failures (24h)" tile (lines 226-233) with a new tile that renders `{formatTimestamp(snapshot.lastPriceRefreshAt)}` under a `<dt>` reading "Last price refresh". The grid MUST stay `lg:grid-cols-4` per D-06 (do NOT change to `lg:grid-cols-5`). Also REMOVE the explanatory paragraph at lines 235-244 (the "Notification failure counts are emitted to Vercel function logs…" block) — it's obsoleted by D-06. Leave a placeholder comment `{/* RefreshPricesButton inserted by Task 4 */}` directly below the `<dd>` inside the new tile — Task 4 wires the button into this slot.

    6. **Tests** — if `src/app/api/admin/health/__tests__/route.test.ts` exists, extend it with two new test cases:
        (a) "reports cronSecret as 'missing' and ok=false when CRON_SECRET env is unset" using `vi.stubEnv("CRON_SECRET", "")` (or `vi.unstubAllEnvs()` after stubbing the others).
        (b) "reports cronSecret as 'configured' and surfaces lastPriceRefreshAt when CRON_SECRET env is set". Mock `getAdminHealthSnapshot` to return a non-null `lastPriceRefreshAt`; assert the response body field is populated.
    If `src/db/__tests__/admin-health.test.ts` exists, extend it with a test that mocks `db.execute` and asserts `lastPriceRefreshAt` flows through from the WHERE-filtered MAX query.
    If neither test file exists, create `src/app/api/admin/health/__tests__/route.test.ts` with the two cases above. Do NOT env-gate; use `vi.stubEnv` per the D-11 pattern.

    7. Run `pnpm tsc --noEmit` to ensure the TypeScript union extension is consistent everywhere (callers that switch over `AdminAuditAction` will get an exhaustiveness warning for the new variant — fix those in this task if any surface). Per `src/db/queries.ts`, only the runtime allowed-list switches on the literal; no exhaustive switches exist over the union.

    NEVER write `cards.price` here — that's Task 2. This task is read-side scaffolding + interface extensions ONLY.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&amp;1 | grep -v "^$" | head -50 ; pnpm vitest run src/app/api/admin/health src/db/admin-health 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -nE "^export function getPrice" src/lib/enrichment.ts` returns one match
    - Source assertion: `grep -c "price_refresh" src/db/queries.ts` returns &ge; 2 (one in the union, one in the runtime allowed-list)
    - Source assertion: `grep -n "lastPriceRefreshAt" src/db/admin-health.ts` returns &ge; 4 matches (interface, two return objects, one Promise.all element); `grep -n "WHERE action = 'price_refresh'" src/db/admin-health.ts` returns 1 match
    - Source assertion: `grep -n "cronSecret" src/app/api/admin/health/route.ts` returns &ge; 4 matches
    - Source assertion: `grep -c "notificationFailuresLast24h" src/app/api/admin/health/route.ts` returns 0
    - Source assertion: `grep -n "Last price refresh" src/app/admin/health/page.tsx` returns 1 match
    - Source assertion: `grep -nE "lg:grid-cols-(4|5)" src/app/admin/health/page.tsx` returns exactly one match for `lg:grid-cols-4` (no 5-col regression)
    - Source assertion: `grep -c "Notification failures" src/app/admin/health/page.tsx` returns 0
    - Behavior: `pnpm tsc --noEmit` exits 0
    - Behavior: existing admin-health route tests still pass; new cronSecret missing/configured tests pass and are not env-gated (no `describe.skip` / `runIf` for these cases)
    - Source assertion: `grep -nE "(describe\\.skip|describe\\.runIf|it\\.skip)" src/app/api/admin/health/__tests__/route.test.ts` returns 0 matches (no gating introduced)
  </acceptance_criteria>
  <done>
    `getPrice` is exported from enrichment.ts. `AdminAuditAction` union AND runtime allowed-list both contain `'price_refresh'`. `AdminHealthSnapshot` and `AdminHealthRecent` both contain `lastPriceRefreshAt`. Both `envChecks()` (route + page) report `cronSecret` as `"configured"` / `"missing"`. `/admin/health` page renders the "Last price refresh" tile in place of the dead "Notification failures (24h)" tile; grid is `lg:grid-cols-4`. `pnpm tsc --noEmit` and the relevant test files pass. No `cards.price` write path has been touched yet.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build runPriceRefresh shared service in src/lib/price-refresh.ts (with Postgres advisory lock, per-row finish ladder, chunked UPDATE-by-composite-id, audit insert, locked-scalar metadata)</name>
  <files>src/lib/price-refresh.ts, src/lib/__tests__/price-refresh.test.ts</files>
  <read_first>
    - src/lib/scryfall.ts (offset 90 limit 200 — confirms `fetchCardsByScryfallIds` signature, returns `Map<string, ScryfallCard>`, gate behavior, `not_found` semantics at line 213-216)
    - src/lib/enrichment.ts (lines 1-130 — getPrice ladder semantics, `ScryfallCard["prices"]` shape, `Finish` type definition)
    - src/db/queries.ts (offset 580 limit 30 — exact `createAdminAuditEntry` shape just edited in Task 1)
    - src/db/queries.ts (offset 60 limit 60 — patterns for `db.execute(sql\`UPDATE ... FROM (VALUES ...)\`)` in `replaceCardsForBinders` if present; otherwise use `db.update(cards).set({ price }).where(eq(cards.id, id))` per-row for clarity then optimize)
    - src/db/admin-health.ts (lines 1-4 — copy "server-only" + import block verbatim)
    - src/db/schema.ts (offset 30 limit 90 — confirm `cards` shape, especially `id: text("id").primaryKey()` and `price: integer("price")` nullable cents)
    - .planning/phases/23-import-ux-price-refresh/23-PATTERNS.md (sections "src/lib/price-refresh.ts", "Server-only + drizzle import block", "Audit-entry insert (locked-scalars metadata)", "Audit action enum extension")
    - .planning/research/ARCHITECTURE.md (Pattern 1 — the full `runPriceRefresh` reference implementation at lines 162-234)
    - .planning/research/PITFALLS.md (Pitfalls 4, 5, 10, 13, 14 — advisory lock, partial failure, snapshot guarantee, duration, idempotency)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/02-route-segment-config (only if route-segment exports become relevant — they don't here; this is a library module)
  </read_first>
  <action>
    Create `src/lib/price-refresh.ts` as a `"server-only"` shared service with the following surface and semantics. NEVER place fenced code blocks in this action — work from the named identifiers and the embedded interface block above.

    **File header (copy verbatim from `src/db/admin-health.ts:1-4`):** `import "server-only";` followed by `import { sql, eq } from "drizzle-orm";` and `import { db } from "@/db/client";` plus the cards schema import `import { cards } from "@/db/schema";`. Then `import { fetchCardsByScryfallIds } from "@/lib/scryfall";`, `import { getPrice } from "@/lib/enrichment";` (newly exported by Task 1), `import { createAdminAuditEntry } from "@/db/queries";`, and `import { logEvent } from "@/lib/logger";` (for structured per-card failure logs — D-04 says per-card detail flows through the logger, NOT audit metadata).

    **Exported types:**
    - `export interface PriceRefreshSummary { trigger: "cron" | "manual"; updated: number; unchanged: number; failed: number; skipped: number; durationMs: number; }`
    - `export class PriceRefreshLockedError extends Error { constructor() { super("Price refresh already in progress"); this.name = "PriceRefreshLockedError"; } }` — thrown when the advisory lock cannot be acquired; route handlers map this to HTTP 409 (manual) / quiet 200 with `reason: "locked"` (cron).

    **Exported function:** `export async function runPriceRefresh(opts: { trigger: "cron" | "manual"; actorEmail?: string | null }): Promise<PriceRefreshSummary>`.

    **Implementation steps (D-08, D-09, D-10, D-14):**
    1. Record `const started = Date.now();`
    2. **Acquire Postgres advisory lock:** Run `await db.execute<{ acquired: boolean }>(sql\`SELECT pg_try_advisory_lock(hashtext('cron.refresh_prices')) AS acquired\`);`. Read `result.rows[0]?.acquired`. If `false` or undefined, throw `new PriceRefreshLockedError()` immediately. Neon-http opens a fresh connection per request so the lock auto-releases at end-of-request — no manual `pg_advisory_unlock` needed (PITFALLS Performance Traps confirms this for neon-http; do NOT add a try/finally that calls `pg_advisory_unlock` because there's no shared session to release from).
    3. **Read all rows:** `await db.select({ id: cards.id, scryfallId: cards.scryfallId, finish: cards.finish, currentPriceCents: cards.price }).from(cards);`
    4. **De-dup Scryfall IDs:** `const uniqueIds = Array.from(new Set(rows.map(r => r.scryfallId).filter((id): id is string => !!id)));`. Rows where `scryfallId` is null go straight to the `skipped` bucket — do NOT include in the Scryfall call.
    5. **Fetch from Scryfall:** `const scryfallMap = await fetchCardsByScryfallIds(uniqueIds);` (no `onBatchComplete` — cron has no NDJSON consumer).
    6. **Per-row classification loop** (D-10 — never NULL-overwrite on `not_found`):
       - `let updated = 0, unchanged = 0, failed = 0, skipped = 0;`
       - `const updates: Array<{ id: string; priceCents: number | null }> = [];`
       - For each row:
         - If `!row.scryfallId` → `skipped++`; continue (row has no scryfallId at all — preserve existing price).
         - If `!scryfallMap.has(row.scryfallId)` → `failed++`; continue (Scryfall returned `not_found` — preserve existing `cards.price`, do NOT write NULL). Emit a structured `logEvent({ level: "info", event: "price_refresh.not_found", route: "lib/price-refresh", metadata: { cardId: row.id, scryfallId: row.scryfallId } })` for per-card observability (D-04 keeps audit metadata bounded; logger handles per-card detail).
         - Otherwise: `const scryCard = scryfallMap.get(row.scryfallId)!;` then `const priceUsd = getPrice(scryCard.prices, row.finish);` (per-row, per-finish ladder — D-09; this is the v1.2 etched-bug fix bridge). Convert to cents (D-14): `const priceCents = priceUsd === null ? null : Math.round(priceUsd * 100);`. If `priceCents === row.currentPriceCents` → `unchanged++`; continue. Otherwise push `{ id: row.id, priceCents }` onto `updates` and `updated++`. (Note: `priceCents === null` IS a legitimate write — Scryfall explicitly said `prices.usd === null` per D-10. This branch only fires for cards Scryfall returned but with no usable price.)
    7. **Chunked bulk UPDATE by composite cards.id (D-09 — NEVER by scryfall_id):** Iterate `updates` in batches of 500. For each chunk, build and execute one parametrized SQL statement of the shape `UPDATE cards SET price = v.price::integer, updated_at = NOW() FROM (VALUES (id1, price1), (id2, price2), …) AS v(id, price) WHERE cards.id = v.id`. Construct with `drizzle-orm`'s `sql` tag + dynamic `sql.join` to keep parametrization safe — do NOT string-concatenate row IDs into the SQL. Reference shape: use one `sql.join(chunk.map(u => sql\`(\${u.id}, \${u.priceCents})\`), sql\`, \`)` to build the VALUES list. If you need a more explicit Drizzle-native pattern, fall back to `Promise.all(chunk.map(u => db.update(cards).set({ price: u.priceCents }).where(eq(cards.id, u.id))))` — slightly slower but still chunked at 500/wave. **Whichever pattern, the join key MUST be `cards.id` (5-segment composite), NEVER `cards.scryfall_id`.**
    8. **Compute `durationMs`:** `const durationMs = Date.now() - started;`
    9. **Insert ONE audit row** (D-04 — locked scalars only): call `await createAdminAuditEntry({ action: "price_refresh", actorEmail: opts.actorEmail ?? null, targetType: "inventory", targetId: null, targetCount: updated, metadata: { trigger: opts.trigger, updated, unchanged, failed, skipped, durationMs } });`. The metadata object has EXACTLY these six keys — no `failedSample[]`, no `errors[]`, no `notFoundIds[]`. Per-card detail already flowed through `logEvent` at step 6.
    10. Return `{ trigger: opts.trigger, updated, unchanged, failed, skipped, durationMs };`.

    **Error handling note:** Wrap steps 3-9 in a try/catch ONLY to ensure that a partial failure mid-chunk does not double-throw. Re-throw caught errors after logging through `logError` so the route handler can map to HTTP 500. Advisory lock errors are intentionally NOT caught (`PriceRefreshLockedError` propagates to the route handler).

    **Test file `src/lib/__tests__/price-refresh.test.ts`** — default-run, NOT env-gated. File-header comment must literally start with the same shape as `src/lib/__tests__/csv-parser-perf.test.ts:21-23` — include the literal string `"NOT env-gated"` in the JSDoc block. Use `vi.mock("@/lib/scryfall", …)` to mock `fetchCardsByScryfallIds`, and `vi.mock("@/db/client", …)` to mock `db`. Cover these cases:
    1. `runPriceRefresh({ trigger: "cron" })` calls `fetchCardsByScryfallIds` once with the deduped scryfallIds.
    2. Rows with `scryfallId: null` increment `skipped` and are NOT included in the Scryfall request.
    3. A row whose scryfallId is missing from the Scryfall response Map (i.e. `not_found`) increments `failed` AND its `db.update` is NEVER called — assertion via mock spy that update is not called for that id.
    4. A row whose Scryfall response has `prices.usd === null` writes `priceCents: null` (legitimate explicit-null overwrite).
    5. Two rows with the same `scryfallId` but different `finish` (`'normal'` and `'etched'`) get DIFFERENT prices written from the same Scryfall response (per-row finish ladder; mock `prices.usd_etched` distinct from `prices.usd`).
    6. UPDATE statements are built using `cards.id` as the join key (assertion via SQL string capture — `expect(capturedSql).not.toMatch(/scryfall_id/)`).
    7. Audit metadata shape: `{ trigger, updated, unchanged, failed, skipped, durationMs }` — six keys exactly. Assertion: `expect(Object.keys(metadata).sort()).toEqual(['durationMs','failed','skipped','trigger','unchanged','updated'])`.
    8. Advisory lock returns `acquired: false` → throws `PriceRefreshLockedError`; Scryfall is never called.

    **NEVER:**
    - Touch route handlers in this task (Task 3 / 4).
    - Add per-card error arrays to audit metadata (D-04).
    - Update by `scryfall_id` (D-09).
    - Write NULL to `cards.price` when `scryfallMap.has(id) === false` (D-10).
    - Env-gate this test file (D-01 / D-11).
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/__tests__/price-refresh.test.ts 2>&amp;1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "^import \"server-only\"" src/lib/price-refresh.ts` returns line 1
    - Source assertion: `grep -nE "export (function runPriceRefresh|class PriceRefreshLockedError|interface PriceRefreshSummary)" src/lib/price-refresh.ts` returns 3 matches
    - Source assertion: `grep -n "pg_try_advisory_lock" src/lib/price-refresh.ts` returns 1 match; the literal `'cron.refresh_prices'` appears as the hashtext argument
    - Source assertion: `grep -n "getPrice(" src/lib/price-refresh.ts` returns &ge; 1 match (called per row)
    - Source assertion: `grep -nE "(WHERE|where)\\s.*scryfall_id" src/lib/price-refresh.ts` returns 0 matches (NEVER updates by scryfall_id)
    - Source assertion: `grep -c "Math.round" src/lib/price-refresh.ts` returns &ge; 1 (cents conversion D-14)
    - Source assertion: `grep -nE "(failedSample|errors\\s*:)" src/lib/price-refresh.ts` returns 0 matches (audit metadata stays locked-scalar)
    - Source assertion: head -10 src/lib/__tests__/price-refresh.test.ts contains the literal "NOT env-gated"
    - Source assertion: `grep -cE "(describe\\.skip|describe\\.runIf|it\\.skip|describeIfCron)" src/lib/__tests__/price-refresh.test.ts` returns 0
    - Behavior: `pnpm vitest run src/lib/__tests__/price-refresh.test.ts` exits 0 with all 8 cases passing
    - Behavior: `pnpm tsc --noEmit` exits 0
    - Behavior assertion (covered by test 6): UPDATE SQL never mentions `scryfall_id`
    - Behavior assertion (covered by test 5): two rows with same scryfallId but different finish receive DIFFERENT priceCents (per-row finish ladder verified)
  </acceptance_criteria>
  <done>
    `src/lib/price-refresh.ts` exports `runPriceRefresh`, `PriceRefreshSummary`, and `PriceRefreshLockedError`. It is server-only, acquires a Postgres advisory lock, classifies every card row through the `getPrice(prices, finish)` ladder per row, UPDATEs by 5-segment `cards.id` in chunks of 500, never writes NULL on Scryfall `not_found`, and writes exactly one `admin_audit_log` row with metadata `{trigger, updated, unchanged, failed, skipped, durationMs}`. The default-run test file covers 8 cases including the advisory-lock path, the NULL-preservation guard, the per-finish price divergence, and the metadata shape assertion. No env-gating.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build both route handlers (GET cron + POST admin manual) + their default-run unit tests + vercel.json</name>
  <files>src/app/api/cron/refresh-prices/route.ts, src/app/api/cron/refresh-prices/__tests__/route.test.ts, src/app/api/admin/prices/refresh/route.ts, src/app/api/admin/prices/refresh/__tests__/route.test.ts, vercel.json</files>
  <read_first>
    - src/app/api/admin/cards/bulk-delete/route.ts (in full — canonical admin route wrapper; copy imports + ROUTE literal + requireAdmin pattern + enforceRateLimit pattern + logEvent/logError + try/catch shape; DROP body parsing since this POST has no body)
    - src/lib/price-refresh.ts (in full — just created by Task 2; confirm exports `runPriceRefresh`, `PriceRefreshSummary`, `PriceRefreshLockedError`)
    - src/lib/auth/admin-check.ts (lines 1-30 — confirm `requireAdmin()` returns `AdminSession | Response` and `result.user.email` is the canonical actor reference)
    - src/lib/rate-limit.ts (offset 620 limit 30 — confirm `RATE_LIMIT_BUCKETS.ADMIN_BULK` exists and `clientKeyFromRequest(request, email)` signature)
    - src/lib/logger.ts (lines 1-50 — confirm `logEvent` / `logError` signatures used elsewhere in admin routes)
    - .planning/phases/23-import-ux-price-refresh/23-PATTERNS.md (sections "src/app/api/cron/refresh-prices/route.ts", "src/app/api/admin/prices/refresh/route.ts", "vercel.json", "Bearer-token cron auth (fail-closed when env missing)", "Admin route wrapper shell", "Tier 1 / Tier 2 test split — NOT env-gated header")
    - .planning/research/PITFALLS.md (Pitfalls 1, 2, 4, 6, 9 — auth bypass, env-gating, race, missing secret, cron drift)
    - .planning/research/STACK.md ("Vercel Cron Configuration — Verified Schema (2026)" section — `vercel.json` shape, Hobby limits, GET-only, `maxDuration=300`, no retry)
    - src/lib/__tests__/csv-parser-perf.test.ts (lines 1-25 — copy the literal "NOT env-gated" header shape per D-11)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md (if uncertain about `export const runtime = "nodejs"` + `export const maxDuration = 300` syntax under this project's Next 16.2.2; the existing `src/app/api/admin/import/commit/route.ts` and `preview/route.ts` already use these exports — verify shape from them rather than guessing)
  </read_first>
  <action>
    Build three files in parallel as a single coherent slice. Test files come FIRST (TDD-leaning) so the handlers are written against contracted assertions.

    **A. `src/app/api/cron/refresh-prices/__tests__/route.test.ts`** (DEFAULT-RUN, NOT env-gated — D-11). Mirror the file-header comment shape from `src/lib/__tests__/csv-parser-perf.test.ts:21-23` — first 5 lines of JSDoc MUST contain the literal string `"NOT env-gated"`. Use `vi.stubEnv("CRON_SECRET", "test-secret-value")` per-test (or `vi.unstubAllEnvs()` to simulate missing). Mock `@/lib/price-refresh` so `runPriceRefresh` is a vi.fn() returning a canned `PriceRefreshSummary`. Test cases:
    1. Missing `Authorization` header → 401.
    2. Wrong Bearer value → 401.
    3. `CRON_SECRET` env unset (use `vi.stubEnv("CRON_SECRET", "")` or unstub) → 401 even if a header is provided (fail-closed per D-12 / Pitfall 6).
    4. Valid Bearer + successful refresh → 200 with body `{ ok: true, trigger: "cron", updated, unchanged, failed, skipped, durationMs }`; `runPriceRefresh` called exactly once with `{ trigger: "cron" }`.
    5. Valid Bearer + `runPriceRefresh` throws `PriceRefreshLockedError` → 200 with body `{ ok: false, reason: "locked" }` (cron should NOT alarm on the cron-vs-manual race — quiet 200 per PATTERNS.md cron-handler error mapping).
    6. Valid Bearer + `runPriceRefresh` throws generic Error → 500 with body `{ ok: false, error: "Refresh failed" }`.
    7. `logEvent` is called with event `"cron.refresh_prices.unauthorized"` on every 401; the Authorization header VALUE is NEVER passed to the logger (PITFALLS Security Mistakes; assert by capturing `logEvent` calls and grepping their args for the secret string).

    **B. `src/app/api/cron/refresh-prices/route.ts`** — Bearer-gated GET handler.
    - File header constants:
      - `const ROUTE = "/api/cron/refresh-prices";`
      - `export const runtime = "nodejs";` (required for neon-http advisory lock)
      - `export const maxDuration = 300;` (D-18 — Vercel Hobby 2026 default; 11x headroom over ~26s refresh)
    - Imports: `runPriceRefresh`, `PriceRefreshLockedError` from `@/lib/price-refresh`; `logEvent`, `logError` from `@/lib/logger`.
    - Handler `export async function GET(request: Request)`:
      1. Read `request.headers.get("authorization")` and `process.env.CRON_SECRET`.
      2. If `!cronSecret || authHeader !== \`Bearer \${cronSecret}\`` → call `logEvent({ level: "warn", event: "cron.refresh_prices.unauthorized", route: ROUTE })` (NEVER include the auth header value or the secret in the log metadata) → return `new Response("Unauthorized", { status: 401 })`.
      3. Otherwise, try-catch:
         - `try { const summary = await runPriceRefresh({ trigger: "cron" }); logEvent({ level: "info", event: "cron.refresh_prices.succeeded", route: ROUTE, metadata: summary }); return Response.json({ ok: true, ...summary }); }`
         - `catch (err) { if (err instanceof PriceRefreshLockedError) { logEvent({ level: "info", event: "cron.refresh_prices.locked", route: ROUTE }); return Response.json({ ok: false, reason: "locked" }, { status: 200 }); } logError({ event: "cron.refresh_prices.failed", route: ROUTE, error: err }); return Response.json({ ok: false, error: "Refresh failed" }, { status: 500 }); }`
    - NEVER use `requireAdmin()` here (the cron caller has no session — Pitfall ; uses Bearer-only). NEVER call `enforceRateLimit` here (Vercel-only caller, no IP semantics).

    **C. `src/app/api/admin/prices/refresh/__tests__/route.test.ts`** (DEFAULT-RUN, NOT env-gated). Same file-header convention (literal "NOT env-gated"). Mock `@/lib/auth/admin-check` to return either an `AdminSession` (success) or a `Response` (401); mock `@/lib/rate-limit` for the bucket call; mock `@/lib/price-refresh.runPriceRefresh`. Cases:
    1. `requireAdmin` returns a 401 Response → handler propagates that Response unchanged.
    2. `requireAdmin` returns admin session + `enforceRateLimit` returns a 429 Response → handler propagates that Response unchanged AND emits `logEvent({ event: "admin.price_refresh.rate_limited", actor })`.
    3. Auth+rate pass + `runPriceRefresh` succeeds → 200 with body `{ success: true, trigger: "manual", updated, unchanged, failed, skipped, durationMs }`; `runPriceRefresh` called once with `{ trigger: "manual", actorEmail: session.user.email }`.
    4. Auth+rate pass + `runPriceRefresh` throws `PriceRefreshLockedError` → **409** with body `{ error: "Refresh in progress" }` (per D-03 UX requirement — distinct from 5xx so the button shows the "try again in a moment" copy).
    5. Auth+rate pass + `runPriceRefresh` throws generic Error → 500 with body `{ error: "Price refresh failed" }`.
    6. `logEvent` is called with `event: "admin.price_refresh.succeeded"` on success and includes `actor` and `metadata: summary`.

    **D. `src/app/api/admin/prices/refresh/route.ts`** — admin POST handler. Copy the wrapper shape verbatim from `src/app/api/admin/cards/bulk-delete/route.ts:1-87` and adapt:
    - Drop the body parsing block (lines 55-63) and the validateIds helper — this POST has no body.
    - Set `const ROUTE = "/api/admin/prices/refresh";`, `export const runtime = "nodejs";`, `export const maxDuration = 300;`.
    - Imports: `requireAdmin`, `enforceRateLimit` + `clientKeyFromRequest` + `RATE_LIMIT_BUCKETS`, `logEvent` + `logError`, `runPriceRefresh` + `PriceRefreshLockedError`.
    - Handler `export async function POST(request: Request)`:
      1. `const result = await requireAdmin(); if (result instanceof Response) return result;` (Phase 15 invariant — auth FIRST).
      2. **Rate-limit AFTER auth** (Phase 15 invariant — verified in `15-SECURITY-REVIEW.md`):
         - `const rateLimited = await enforceRateLimit({ key: clientKeyFromRequest(request, result.user.email), config: RATE_LIMIT_BUCKETS.ADMIN_BULK });`
         - If `rateLimited` (a Response) → `logEvent({ level: "warn", event: "admin.price_refresh.rate_limited", route: ROUTE, actor: result.user.email });` and `return rateLimited;`.
      3. Try/catch around `runPriceRefresh({ trigger: "manual", actorEmail: result.user.email })`:
         - On success: `logEvent({ level: "info", event: "admin.price_refresh.succeeded", route: ROUTE, actor: result.user.email, metadata: summary });` then `return Response.json({ success: true, ...summary });`.
         - In `catch`: `if (err instanceof PriceRefreshLockedError) { logEvent({ level: "info", event: "admin.price_refresh.locked", route: ROUTE, actor: result.user.email }); return Response.json({ error: "Refresh in progress" }, { status: 409 }); } logError({ event: "admin.price_refresh.failed", route: ROUTE, actor: result.user.email, error: err }); return Response.json({ error: "Price refresh failed" }, { status: 500 });`

    **E. `vercel.json`** at repo root. Single small JSON file with these exact contents (write whole, no template — confirmed missing today):
    - `$schema`: `"https://openapi.vercel.sh/vercel.json"` (recommended by Vercel docs for editor schema validation; not load-bearing for cron)
    - `crons`: array with ONE entry: `{ "path": "/api/cron/refresh-prices", "schedule": "0 9 * * *" }` (D-07: 09:00 UTC daily; Hobby fires anywhere in 09:00–09:59 UTC per Pitfall 9)
    - No retry config, no env scoping (Vercel docs: cron has no retries; runs on production deployments only).

    NEVER:
    - Introduce `describe.skip`, `describe.runIf`, `it.skip`, or any env-gating of these test files (D-01 / D-11).
    - Log the Authorization header value or `process.env.CRON_SECRET` (Pitfall 1 / Security Mistakes).
    - Call the cron route from the manual route (no HTTP between them — both call `runPriceRefresh` directly per D-12).
    - Add a body-parsing block to the manual POST route (carries no payload).
    - Set `maxDuration` below 300 (D-18; outdated 10s/60s mental model).
    - Add cron retry config to `vercel.json` (Vercel cron does not retry — confirmed in STACK.md / PITFALLS Integration Gotchas).
  </action>
  <verify>
    <automated>pnpm vitest run src/app/api/cron/refresh-prices src/app/api/admin/prices/refresh 2>&amp;1 | tail -80 ; node -e "const v=require('./vercel.json'); if(!Array.isArray(v.crons)||v.crons.length!==1) throw new Error('vercel.json crons shape wrong'); if(v.crons[0].path!=='/api/cron/refresh-prices'||v.crons[0].schedule!=='0 9 * * *') throw new Error('vercel.json crons content wrong'); console.log('vercel.json OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `head -n 10 src/app/api/cron/refresh-prices/__tests__/route.test.ts | grep -F "NOT env-gated"` returns 1 match
    - Source assertion: `head -n 10 src/app/api/admin/prices/refresh/__tests__/route.test.ts | grep -F "NOT env-gated"` returns 1 match
    - Source assertion: `grep -cE "(describe\\.skip|describe\\.runIf|it\\.skip|describeIfCron|describeIfDb)" src/app/api/cron/refresh-prices/__tests__/route.test.ts src/app/api/admin/prices/refresh/__tests__/route.test.ts` returns 0
    - Source assertion: `grep -n "export const maxDuration = 300" src/app/api/cron/refresh-prices/route.ts` returns 1 match
    - Source assertion: `grep -n "export const runtime = \"nodejs\"" src/app/api/cron/refresh-prices/route.ts` returns 1 match
    - Source assertion: `grep -n "process.env.CRON_SECRET" src/app/api/cron/refresh-prices/route.ts` returns &ge; 1 match (fail-closed check)
    - Source assertion: cron route source does NOT contain `requireAdmin` or `enforceRateLimit` (Bearer-only): `grep -cE "(requireAdmin|enforceRateLimit)" src/app/api/cron/refresh-prices/route.ts` returns 0
    - Source assertion: admin route file MUST contain BOTH `requireAdmin` AND `enforceRateLimit` AND `ADMIN_BULK`, with `requireAdmin` appearing BEFORE `enforceRateLimit` in file order (auth-before-rate-limit invariant from Phase 15): `awk '/requireAdmin\\(\\)/{a=NR} /enforceRateLimit\\(/{b=NR} END{exit (a&&b&&a<b)?0:1}' src/app/api/admin/prices/refresh/route.ts` exits 0
    - Source assertion: admin route source does NOT parse a request body: `grep -cE "request\\.json\\(\\)|request\\.formData\\(\\)" src/app/api/admin/prices/refresh/route.ts` returns 0
    - Source assertion: `grep -n "PriceRefreshLockedError" src/app/api/admin/prices/refresh/route.ts` returns &ge; 1 match (mapped to 409)
    - Source assertion: `grep -n "status: 409" src/app/api/admin/prices/refresh/route.ts` returns 1 match
    - Source assertion: `vercel.json` exists at repo root; `node -e "process.exit(require('./vercel.json').crons[0].schedule==='0 9 * * *'?0:1)"` exits 0
    - Source assertion: `node -e "process.exit(require('./vercel.json').crons[0].path==='/api/cron/refresh-prices'?0:1)"` exits 0
    - Behavior: `pnpm vitest run src/app/api/cron/refresh-prices src/app/api/admin/prices/refresh` exits 0 with all test cases (7 cron + 6 admin) passing
    - Behavior: missing-secret 401, wrong-bearer 401, valid-bearer 200, locked 200/{reason:'locked'} (cron) and 409 (admin), 500 generic — all verified via tests
    - Behavior: `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Two thin route handlers ship: cron GET fails closed on missing/wrong secret, returns 200 on success, quiet 200/{reason:'locked'} on advisory-lock contention, 500 on generic error. Admin POST follows the Phase 15 auth-then-rate-limit invariant, returns 401/429/200/409/500 in the expected matrix. Both test files default-run with the literal "NOT env-gated" header. `vercel.json` ships at repo root with the canonical Hobby-compatible cron entry. All assertions pass under `pnpm vitest run` and `pnpm tsc --noEmit`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Build RefreshPricesButton client component, mount it on /admin/health under the Last price refresh tile, validate the full slice end-to-end</name>
  <files>src/app/admin/health/_components/refresh-prices-button.tsx, src/app/admin/health/_components/__tests__/refresh-prices-button.test.tsx, src/app/admin/health/page.tsx</files>
  <read_first>
    - src/app/admin/orders/_components/order-detail.tsx (offset 350 limit 50 — `handleAdvance` is the closest existing fetch-POST-then-router.refresh() pattern)
    - src/app/admin/_components/inventory-danger-zone.tsx (in full — the quietest existing client mutation button; copy disabled-button class lexicon and useState shape, NOT the destructive red styling)
    - src/app/admin/health/page.tsx (current state after Task 1 — the "Last price refresh" tile placeholder comment marks the insertion point)
    - .planning/phases/23-import-ux-price-refresh/23-PATTERNS.md (section "src/app/admin/health/_components/refresh-prices-button.tsx" — full state machine; section "Client-mutation button with router.refresh()" — pattern reference)
    - .planning/phases/23-import-ux-price-refresh/23-CONTEXT.md (D-03 — exact state-machine semantics)
    - .planning/research/PITFALLS.md (UX Pitfalls table — "Refresh now" button gives no feedback case)
  </read_first>
  <action>
    Three deliverables: the component, its default-run test, and wiring it into the health page.

    **A. `src/app/admin/health/_components/refresh-prices-button.tsx`** — create the `_components/` directory if it does not exist (mirrors `src/app/admin/import/_components/`, confirmed missing per PATTERNS.md "Verification" table).
    - First line: `"use client";`
    - Imports: `useState` from `"react"`, `useRouter` from `"next/navigation"`.
    - State type (D-03): `type Status = { kind: "idle" } | { kind: "refreshing" } | { kind: "error"; message: string };`
    - Export named function `RefreshPricesButton` (no props):
      - `const router = useRouter();`
      - `const [status, setStatus] = useState<Status>({ kind: "idle" });`
      - Async `handleClick`:
        - Guard re-entry: `if (status.kind === "refreshing") return;`
        - `setStatus({ kind: "refreshing" });`
        - Try: `const res = await fetch("/api/admin/prices/refresh", { method: "POST" });`
        - On `res.status === 409`: `setStatus({ kind: "error", message: "Refresh in progress — try again in a moment" }); setTimeout(() => setStatus({ kind: "idle" }), 5000); return;` (D-03 distinguishes 409 from 5xx in inline copy).
        - On `!res.ok` (any other non-2xx): `setStatus({ kind: "error", message: "Refresh failed — check logs" }); setTimeout(() => setStatus({ kind: "idle" }), 5000); return;`
        - On success: `setStatus({ kind: "idle" }); router.refresh();` — the page is already `dynamic = "force-dynamic"` (page.tsx:19) so `router.refresh()` re-executes `getAdminHealthSnapshot()` and the new `lastPriceRefreshAt` tile updates without a full reload.
        - On `catch` (network error): same as the generic non-ok branch — `setStatus({ kind: "error", message: "Refresh failed — check logs" }); setTimeout(() => setStatus({ kind: "idle" }), 5000);`
      - Render:
        - A `<button type="button" onClick={handleClick} disabled={status.kind === "refreshing"} aria-busy={status.kind === "refreshing"} className="mt-2 rounded-lg px-3 py-1.5 text-xs font-semibold border border-zinc-300 dark:border-zinc-600 hover:bg-zinc-100 dark:hover:bg-zinc-800 transition-colors disabled:opacity-50 disabled:cursor-not-allowed whitespace-nowrap">` — text is `{status.kind === "refreshing" ? "Refreshing…" : "Refresh now"}`. Outlined / quiet weight (NOT the destructive red filled style from inventory-danger-zone — refresh is non-destructive per PATTERNS.md / Anti-Pattern 3).
        - Below the button, conditional inline error: `{status.kind === "error" && (<p role="alert" className="mt-2 text-xs text-amber-700 dark:text-amber-300">{status.message}</p>)}`
    - NEVER:
      - Wrap in `useTransition` — the page is `force-dynamic`; a transition-pending state would obscure D-03's button-local UX (PATTERNS.md "Client-mutation button" anti-note).
      - Import a toast library / global notification system (D-03 explicit).
      - Persist anything to localStorage or sessionStorage (D-03: button-local state, no persistence).
      - Send a request body (POST carries no payload per Task 3).
      - Use `<a>` or `<div role="button">` — must be a native `<button type="button">` (PITFALLS Pitfall 15 keyboard nav).

    **B. `src/app/admin/health/_components/__tests__/refresh-prices-button.test.tsx`** — default-run, NOT env-gated. Use `@testing-library/react` (already in the test deps; mirror existing client-component tests under `src/app/admin/*/_components/__tests__/`). Mock `next/navigation` so `useRouter().refresh` is a vi.fn(). Mock `global.fetch` via vi.spyOn. Use vi.useFakeTimers() to test the 5-second reset. Cases:
    1. Initial render → button text is "Refresh now"; disabled is false; no `<p role="alert">` present.
    2. Click → button text becomes "Refreshing…"; button is disabled; `fetch` was called exactly once with `("/api/admin/prices/refresh", { method: "POST" })`.
    3. fetch resolves with status 200 → button returns to "Refresh now"; `router.refresh()` was called once; no error message visible.
    4. fetch resolves with status 409 + body `{ error: "Refresh in progress" }` → inline `<p role="alert">` contains "Refresh in progress — try again in a moment"; advance timers 5s → message disappears; `router.refresh()` was NOT called.
    5. fetch resolves with status 500 → inline `<p role="alert">` contains "Refresh failed — check logs"; advance timers 5s → message disappears; `router.refresh()` was NOT called.
    6. fetch rejects (network error) → inline `<p role="alert">` contains "Refresh failed — check logs"; `router.refresh()` was NOT called.
    7. Re-click while `kind === "refreshing"` → `fetch` is still only called once (re-entry guard).

    **C. Wire into `src/app/admin/health/page.tsx`** — replace the `{/* RefreshPricesButton inserted by Task 4 */}` placeholder comment left by Task 1 with `<RefreshPricesButton />`. Add the import at the top of the file: `import { RefreshPricesButton } from "./_components/refresh-prices-button";`. The button renders INSIDE the "Last price refresh" tile's outer `<div>`, BELOW the `<dd>` that shows the timestamp — operator sees the timestamp and the action button together (PATTERNS.md / ARCHITECTURE.md Anti-Pattern 3: causal locality).

    **D. Smoke-test the assembled slice** — after writing the three above, run:
    1. `pnpm tsc --noEmit` (full type check)
    2. `pnpm vitest run src/lib/__tests__/price-refresh.test.ts src/app/api/cron/refresh-prices src/app/api/admin/prices/refresh src/app/admin/health` (all new test surfaces pass)
    3. `pnpm vitest run` (full suite — confirm no regression in the 464 existing tests; per STATE.md, baseline is 464 passing / 2 skipped)
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&amp;1 | tail -30 ; pnpm vitest run src/lib/__tests__/price-refresh.test.ts src/app/api/cron/refresh-prices src/app/api/admin/prices/refresh src/app/admin/health 2>&amp;1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `head -n 1 src/app/admin/health/_components/refresh-prices-button.tsx` equals `"use client";`
    - Source assertion: `grep -nE "export (function|const) RefreshPricesButton" src/app/admin/health/_components/refresh-prices-button.tsx` returns 1 match
    - Source assertion: `grep -n "router.refresh" src/app/admin/health/_components/refresh-prices-button.tsx` returns 1 match
    - Source assertion: `grep -cE "(useTransition|toast|Toast|localStorage|sessionStorage)" src/app/admin/health/_components/refresh-prices-button.tsx` returns 0
    - Source assertion: `grep -n "fetch(\"/api/admin/prices/refresh\"" src/app/admin/health/_components/refresh-prices-button.tsx` returns 1 match
    - Source assertion: `grep -n "method: \"POST\"" src/app/admin/health/_components/refresh-prices-button.tsx` returns 1 match
    - Source assertion: `grep -n "RefreshPricesButton" src/app/admin/health/page.tsx` returns &ge; 2 matches (import + JSX usage)
    - Source assertion: `grep -c "RefreshPricesButton inserted by Task 4" src/app/admin/health/page.tsx` returns 0 (placeholder consumed)
    - Source assertion: `grep -cE "(describe\\.skip|describe\\.runIf|it\\.skip)" src/app/admin/health/_components/__tests__/refresh-prices-button.test.tsx` returns 0
    - Behavior: `pnpm tsc --noEmit` exits 0
    - Behavior: `pnpm vitest run src/lib/__tests__/price-refresh.test.ts src/app/api/cron/refresh-prices src/app/api/admin/prices/refresh src/app/admin/health` exits 0 with all cases (8 service + 7 cron + 6 admin + 7 button + admin-health route additions) passing
    - Behavior: full `pnpm vitest run` exits 0 (no existing-test regression)
    - Behavior assertion (covered by button test 7): re-click while refreshing does NOT issue a second fetch
    - Behavior assertion (covered by button tests 4-6): 409 → "try again in a moment"; 5xx and network error → "check logs"; both auto-clear after 5s
  </acceptance_criteria>
  <done>
    `<RefreshPricesButton />` renders next to the "Last price refresh" tile on `/admin/health`. Clicking it POSTs to `/api/admin/prices/refresh`, shows "Refreshing…" while in flight, calls `router.refresh()` on 200 (which re-runs `getAdminHealthSnapshot()` and updates the tile timestamp), shows "Refresh in progress — try again in a moment" inline for 5s on 409, shows "Refresh failed — check logs" inline for 5s on 5xx/network. Re-click guard prevents double-firing. Full test suite (existing 464 + new) passes under `pnpm vitest run` and `pnpm tsc --noEmit` clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel scheduler → `/api/cron/refresh-prices` | Untrusted public GET; only auth is `Authorization: Bearer ${CRON_SECRET}`; any leaked secret enables Scryfall-budget DoS + audit-log pollution |
| Browser (admin operator) → `/api/admin/prices/refresh` | Authenticated POST; gated by `requireAdmin()` (Google OAuth + email allowlist) + `enforceRateLimit(ADMIN_BULK)` AFTER auth |
| `runPriceRefresh` → Neon Postgres | `db.execute(sql\`SELECT pg_try_advisory_lock(...)\`)` + chunked UPDATEs + audit INSERT; advisory lock is the single-flight primitive |
| `runPriceRefresh` → Scryfall `/cards/collection` | Existing rate-gated batched fetcher (`src/lib/scryfall.ts`); reused as-is, no new outbound surface |
| `/admin/health` server render → env vars (`process.env.CRON_SECRET`) | Read for `isPresent()` check ONLY; literal "configured"/"missing" returned; secret value NEVER serialized to response or rendered to HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Spoofing | `GET /api/cron/refresh-prices` | mitigate | Bearer-token equality check via constant-string comparison; `if (!cronSecret \|\| authHeader !== \`Bearer ${cronSecret}\`) return 401`; fail-closed when env unset (D-12, PITFALLS Pitfall 1). Tier-1 test asserts 401 in three states (no header / wrong / env-unset). |
| T-23-02 | Information Disclosure | Cron route 401 path | mitigate | NEVER log `authHeader` value or `process.env.CRON_SECRET` (PITFALLS Security Mistakes — `src/lib/logger.ts` redaction list is keyed on `password/secret/token/api_key/cookie`, "authorization" may not match). Log only `{ event: "cron.refresh_prices.unauthorized", route }`. Tier-1 test asserts log args don't contain the secret string. |
| T-23-03 | Spoofing | `POST /api/admin/prices/refresh` | mitigate | `requireAdmin()` (Google OAuth + email allowlist) gates the route; tested by Phase 15 baseline + Task 3 test case 1 (`requireAdmin` 401 propagates unchanged). |
| T-23-04 | Denial of Service | `POST /api/admin/prices/refresh` | mitigate | `enforceRateLimit({ config: RATE_LIMIT_BUCKETS.ADMIN_BULK })` AFTER `requireAdmin()` (Phase 15 invariant). 20 req/min cap prevents stuck-clicking operator from DDoSing Scryfall. Task 3 test case 2 asserts the rate-limit Response propagates with `logEvent`. |
| T-23-05 | Denial of Service | `runPriceRefresh` concurrent invocation | mitigate | Postgres advisory lock `pg_try_advisory_lock(hashtext('cron.refresh_prices'))` non-blocking; second caller throws `PriceRefreshLockedError` → 409 (manual) / quiet 200 (cron). Auto-released at end of neon-http connection (PITFALLS Performance Traps). D-08. |
| T-23-06 | Tampering | `cards.price` write path | mitigate | UPDATE by 5-segment `cards.id` composite PK (NEVER by `scryfall_id`); per-row `getPrice(prices, finish)` ladder; chunk size 500 via parametrized `sql.join` (no string-concatenation of IDs). Prevents both the v1.2 etched-mispricing regression (D-09) and SQL injection at the SQL-layer (drizzle parametrization). Task 2 test case 6 asserts SQL never references `scryfall_id`. |
| T-23-07 | Tampering | NULL-overwrite on `not_found` | mitigate | `if (!scryfallMap.has(row.scryfallId)) { failed++; continue; }` — skip the write entirely; preserve existing `cards.price`. Only write NULL when Scryfall explicitly returned `prices.usd === null`. D-10; PITFALLS Pitfall 5. Task 2 test case 3. |
| T-23-08 | Information Disclosure | `/admin/health` env reporting | mitigate | `envChecks()` returns literal `"configured"` / `"missing"` (Phase 15 STATUS_LABELS-only invariant); NEVER echoes `process.env.CRON_SECRET` value. Task 1 asserts no value leaks via response body grep. |
| T-23-09 | Repudiation | Price refresh audit trail | mitigate | One `admin_audit_log` row per run with `action='price_refresh'`, `actorEmail` populated for manual triggers (null for cron), `metadata: { trigger, updated, unchanged, failed, skipped, durationMs }`. Phase 14 bounded-metadata invariant preserved. D-04. Per-card failure detail flows through Phase 15 structured logger (`logEvent` with `cardId`/`scryfallId`) — out of audit metadata to stay under the 4KB cap. |
| T-23-10 | Information Disclosure | Per-card failure log details | accept | Structured logs include `cardId` (5-segment composite) and `scryfallId` (Scryfall UUID). Neither is PII; both are public-equivalent identifiers (set codes + collector numbers are on Scryfall). Risk acceptable to preserve diagnostic value. |
| T-23-11 | Elevation of Privilege | Cron handler trying to mutate beyond its scope | mitigate | Cron handler does ONLY `runPriceRefresh({ trigger: "cron" })`. No other DB writes, no admin-action gates bypassed. The shared service is `"server-only"` and auth-agnostic per D-12. |
| T-23-12 | Information Disclosure | `vercel.json` in repo | accept | Schedule + path are non-sensitive; the SECRET (`CRON_SECRET`) lives in Vercel env, not in `vercel.json`. Standard practice per Vercel docs. |
| T-23-13 | Denial of Service | Cron event delivered &gt; 1× (Vercel docs disclose this) | mitigate | Same advisory lock as T-23-05 — second delivery acquires nothing, returns quiet 200 `{reason:"locked"}`. PITFALLS Pitfall 14; D-08. |
| T-23-14 | Spoofing | User-Agent `vercel-cron/1.0` check | accept | Belt-and-suspenders only; UA is spoofable. Bearer-token (T-23-01) is the load-bearing guard. Documented in PATTERNS.md / PITFALLS Integration Gotchas — explicitly NOT used as auth. |
</threat_model>

<verification>
## Phase-level checks (after all four tasks complete)

1. **TypeScript:** `pnpm tsc --noEmit` exits 0.
2. **Test suite:** `pnpm vitest run` exits 0 with the new test files passing AND existing ~464 tests still passing (no regression). Look for the line "X passed | Y skipped" in vitest output; `Y` must equal the prior baseline (currently 2 per STATE.md).
3. **Default-run guarantee (D-01 / D-11):** `grep -lRE "(describe\\.skip|describe\\.runIf|describeIfCron|describeIfDb|it\\.skip|test\\.skip)" src/lib/__tests__/price-refresh.test.ts src/app/api/cron/refresh-prices src/app/api/admin/prices/refresh src/app/admin/health/_components/__tests__ | wc -l` returns 0.
4. **"NOT env-gated" header literal:** `for f in src/lib/__tests__/price-refresh.test.ts src/app/api/cron/refresh-prices/__tests__/route.test.ts src/app/api/admin/prices/refresh/__tests__/route.test.ts; do head -n 10 "$f" | grep -q "NOT env-gated" || echo "MISSING: $f"; done` produces no output.
5. **Audit metadata shape (D-04):** `grep -nE "(failedSample|errors\\s*:|notFoundIds)" src/lib/price-refresh.ts` returns 0 matches.
6. **UPDATE by composite ID, never by scryfall_id (D-09):** `grep -nE "(WHERE|where|on)\\s.*scryfall_id" src/lib/price-refresh.ts` returns 0 matches.
7. **AdminAuditAction extension (PATTERNS.md correction):** `grep -c "price_refresh" src/db/queries.ts` returns &ge; 2 (TS union + runtime allowed-list).
8. **Health page grid stays 4-col (D-06):** `grep -cE "lg:grid-cols-5" src/app/admin/health/page.tsx` returns 0; `grep -cE "lg:grid-cols-4" src/app/admin/health/page.tsx` returns 1.
9. **Dead-tile retirement (D-06):** `grep -cE "(notificationFailuresLast24h|Notification failures)" src/app/admin/health/page.tsx src/app/api/admin/health/route.ts` returns 0.
10. **`vercel.json` shape:** `node -e "const v=require('./vercel.json'); if (!Array.isArray(v.crons)||v.crons.length!==1||v.crons[0].path!=='/api/cron/refresh-prices'||v.crons[0].schedule!=='0 9 * * *') process.exit(1);"` exits 0.
11. **`maxDuration = 300` on both routes (D-18):** `grep -c "export const maxDuration = 300" src/app/api/cron/refresh-prices/route.ts src/app/api/admin/prices/refresh/route.ts` returns 2 (one per file).
12. **No HTTP between routes (D-12):** the admin route source does NOT `fetch("/api/cron/refresh-prices"...)`: `grep -c "/api/cron" src/app/api/admin/prices/refresh/route.ts` returns 0.

## SUMMARY content gates

Plan 23-01 SUMMARY.md (created during execute-plan) MUST contain:
1. A `## Operator Setup` section with the literal sequence:
   - `openssl rand -hex 32` (secret generation)
   - "Paste into Vercel Project Settings → Environment Variables → Production (and Preview)"
   - "Redeploy"
   - "Verify `cronSecret: \"configured\"` on `/admin/health`"
2. A `## Why no Tier 2 live-DB integration test` section explaining:
   - Per D-01, Plan 23-01 stays Tier 1 only
   - The v1.3.5 retrospective lesson — env-gated tests silently skip in CI
   - The Phase 18 concurrent-proof harness is still pending operator's `TEST_DATABASE_URL` provisioning (`STATE.md → Operator Next Steps`)
   - Advisory-lock contention is verified by operator UAT against the deployed cron, NOT by an env-gated test in CI
3. Cross-reference to PITFALLS.md Pitfall 9 (Vercel Hobby cron drifts ±59min within the hour) so operator doesn't report drift as a bug.
</verification>

<success_criteria>
- All 11 PRICE-REFRESH requirements (PRICE-REFRESH-01..11) are covered by code and tests in this plan.
- All 17 must_haves.truths verifiable by automated grep/test assertions.
- `pnpm vitest run` and `pnpm tsc --noEmit` both exit 0.
- No env-gated test files introduced (D-01 / D-11).
- `AdminAuditAction` extension touches BOTH the TS union AND the runtime allowed-list (PATTERNS.md correction).
- `vercel.json` ships at repo root with the canonical Hobby-compatible cron entry.
- Operator runbook for `CRON_SECRET` ships inline in `23-01-SUMMARY.md` (D-02); no separate ops doc.
- No `failedSample[]` / `errors[]` arrays in audit metadata (D-04).
- UPDATE goes by composite `cards.id` only; SQL never references `scryfall_id` in the WHERE/JOIN (D-09).
- NULL-overwrite guard explicit in code AND covered by test 3 in `price-refresh.test.ts` (D-10).
- Advisory-lock + 409 second-caller behavior covered by Task 2 test 8 AND Task 3 admin-test case 4 (D-08).
- CRON_SECRET-missing 401 behavior covered by Task 3 cron-test case 3 (PRICE-REFRESH-02 + success criterion #5).
- Health grid stays `lg:grid-cols-4` (D-06).
</success_criteria>

<output>
After completion, create `.planning/phases/23-import-ux-price-refresh/23-01-SUMMARY.md` containing:
1. Standard summary template content (work completed, files changed, tests added, decisions implemented with D-XX references).
2. **`## Operator Setup`** section (D-02 inline runbook — see verification block above).
3. **`## Why no Tier 2 live-DB integration test`** section (D-01 / D-11 deliberate-absence explanation — see verification block above).
4. Cross-reference to PITFALLS.md Pitfalls 1, 2, 4, 5, 6, 9 with how this plan mitigates each.
5. Open items for operator UAT (live cron firing window verification post-deploy; advisory-lock contention verification via cron-manual race attempt).
</output>
