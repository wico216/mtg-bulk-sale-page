---
phase: 13-admin-order-workflow
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/db/schema.ts
  - src/db/orders.ts
  - src/db/__tests__/orders-admin.test.ts
  - src/app/api/admin/orders/[id]/cancel/route.ts
  - src/app/api/admin/orders/__tests__/route.test.ts
  - src/app/admin/orders/[id]/page.tsx
  - src/app/admin/orders/_components/order-detail.tsx
autonomous: true
requirements:
  - ORD-06
must_haves:
  truths:
    - "Admin can cancel a pending or confirmed order without deleting the order record"
    - "Admin can choose whether cancellation restores inventory quantities"
    - "Inventory restore increments only existing card rows by order item cardId"
    - "Cancellation reports restored quantity and skipped missing inventory rows"
    - "Completed orders are not cancelled through the normal UI"
    - "Cancellation and optional restore happen in one server-side operation"
  artifacts:
    - path: "src/db/orders.ts"
      provides: "Cancel order with optional inventory restore helper"
      exports: ["cancelOrder"]
    - path: "src/app/api/admin/orders/[id]/cancel/route.ts"
      provides: "Authenticated cancel-order endpoint"
      contains: "restoreInventory"
    - path: "src/app/admin/orders/_components/order-detail.tsx"
      provides: "Cancel order confirmation flow"
      contains: "Restore inventory"
---

<objective>
Add safe cancellation to the order workflow. Cancellation preserves the order record and lets the admin explicitly decide whether to restore inventory quantities for matching existing card rows.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-admin-order-workflow/13-CONTEXT.md
@.planning/phases/13-admin-order-workflow/13-01-SUMMARY.md
@.planning/phases/12-bulk-operations-dashboard/12-02-SUMMARY.md
</execution_context>

<context>
@src/db/schema.ts
@src/db/orders.ts
@src/app/admin/orders/[id]/page.tsx
@src/app/admin/orders/_components/order-detail.tsx
@src/app/admin/_components/delete-confirmation.tsx
@src/app/admin/_components/toast.tsx
@src/app/api/admin/orders/[id]/route.ts
@src/lib/auth/admin-check.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cancellation and restore tests</name>
  <files>
    src/db/__tests__/orders-admin.test.ts,
    src/app/api/admin/orders/__tests__/route.test.ts
  </files>
  <read_first>
    src/db/orders.ts,
    src/db/schema.ts,
    src/db/__tests__/orders-admin.test.ts,
    src/app/api/admin/orders/__tests__/route.test.ts
  </read_first>
  <behavior>
    - `cancelOrder({ orderId, restoreInventory })` changes status to `cancelled` and preserves order/items.
    - If `restoreInventory` is false, inventory is unchanged.
    - If `restoreInventory` is true, existing card rows are incremented by order item quantity.
    - Missing card rows are skipped and reported; they are not recreated.
    - Completed orders are rejected by the normal cancellation helper.
    - Cancelling an already-cancelled order is idempotent and does not double-restore inventory.
    - `POST /api/admin/orders/[id]/cancel` validates body/auth and returns the cancellation result.
  </behavior>
  <action>
    Write failing tests first. Include a double-submit/idempotency test so inventory restore cannot run twice for the same cancelled order.
  </action>
  <verify>
    <automated>npx vitest run src/db/__tests__/orders-admin.test.ts src/app/api/admin/orders/__tests__/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because cancellation contracts are missing.
    - Tests cover no-restore, restore, missing card skip, completed-order rejection, already-cancelled idempotency, and auth.
  </acceptance_criteria>
  <done>Red tests define cancellation and restore contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cancellation helper and endpoint</name>
  <files>
    src/db/schema.ts,
    src/db/orders.ts,
    src/app/api/admin/orders/[id]/cancel/route.ts
  </files>
  <read_first>
    src/db/schema.ts,
    src/db/orders.ts,
    src/db/__tests__/orders-admin.test.ts,
    src/app/api/admin/orders/[id]/route.ts,
    src/lib/auth/admin-check.ts
  </read_first>
  <behavior>
    - `cancelled` is an explicit order status value.
    - Cancellation and optional restore are server-side and atomic enough to avoid double restoration.
    - Existing order item snapshots remain unchanged after cancellation.
    - The helper returns a result object suitable for UI copy.
  </behavior>
  <action>
    Add `cancelled` to the order status enum/schema. Verify the Neon-compatible schema update path; for Postgres this may require `ALTER TYPE order_status ADD VALUE IF NOT EXISTS 'cancelled'` before using the new status.

    Implement `cancelOrder` with a guard that prevents restoring inventory more than once. If the current schema cannot represent whether restore already happened, use the order's previous status and a single update condition (`WHERE id = ? AND status != 'cancelled'`) so only the first cancellation attempt can restore.

    Add `POST /api/admin/orders/[id]/cancel` with request body:
    ```typescript
    { restoreInventory: boolean }
    ```
  </action>
  <verify>
    <automated>npx vitest run src/db/__tests__/orders-admin.test.ts src/app/api/admin/orders/__tests__/route.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cancelOrder` export exists.
    - Cancel endpoint imports `requireAdmin`.
    - Focused tests and TypeScript pass.
    - Schema update notes are recorded for summary/ops handoff.
  </acceptance_criteria>
  <done>Cancel-order API is implemented and covered.</done>
</task>

<task type="auto">
  <name>Task 3: Add cancel order UI</name>
  <files>
    src/app/admin/orders/[id]/page.tsx,
    src/app/admin/orders/_components/order-detail.tsx
  </files>
  <read_first>
    src/app/admin/orders/[id]/page.tsx,
    src/app/admin/orders/_components/order-detail.tsx,
    src/app/admin/_components/delete-confirmation.tsx,
    src/app/admin/_components/toast.tsx
  </read_first>
  <behavior>
    - Order detail shows a cancel action for cancellable statuses.
    - Cancel confirmation clearly states whether inventory will be restored.
    - Restore inventory is an explicit checkbox, defaulting to off unless execution confirms a better default.
    - Success UI reports cancellation and restore/skipped counts.
    - Completed/cancelled orders do not show the normal cancel action.
  </behavior>
  <action>
    Add an inline destructive confirmation pattern matching existing admin destructive actions. Keep copy specific: "Cancel order" is not "delete order".

    After successful cancellation, refresh the order detail so the status and available actions reflect the persisted state.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Admin can cancel a pending/confirmed order.
    - Admin can choose inventory restore.
    - Cancellable vs non-cancellable states render correctly.
    - Full tests and build pass.
  </acceptance_criteria>
  <done>Admin can cancel orders from order detail safely.</done>
</task>

<task type="checkpoint:verify" gate="blocking">
  <name>Task 4: Browser/DB verify cancellation restore</name>
  <files>src/app/admin/orders/_components/order-detail.tsx</files>
  <action>
    Browser verification must include:
    1. Insert disposable inventory and place a checkout order so stock decrements.
    2. Open the order detail as admin.
    3. Cancel once without restore and confirm order remains visible with `cancelled` status and inventory does not change.
    4. Create a second disposable order, cancel with restore enabled, and confirm matching card quantity increments.
    5. Attempt refresh/double-submit behavior if practical and confirm inventory does not restore twice.
    6. Clean up disposable orders/items/cards.
    7. Confirm browser console and network logs have no unexpected errors.
  </action>
  <done>Cancellation and optional inventory restore are verified end-to-end.</done>
</task>

</tasks>

<verification>
- `git diff --check`
- `npx vitest run src/db/__tests__/orders-admin.test.ts src/app/api/admin/orders/__tests__/route.test.ts`
- `npx tsc --noEmit`
- `npm test`
- `npm run build`
- Browser/DB verification of cancel without restore and cancel with restore
</verification>

<success_criteria>
- [ ] ORD-06: Admin can cancel orders while preserving order history
- [ ] Optional inventory restore is explicit and safe against double restore
- [ ] Missing inventory rows are reported, not recreated from partial snapshots
- [ ] Completed orders are protected from normal cancellation
</success_criteria>

<output>
After completion, create `.planning/phases/13-admin-order-workflow/13-02-SUMMARY.md` with:
- final cancellation helper/API contract
- schema migration notes for `cancelled`
- browser/DB verification evidence
- known limitations around missing inventory-row restoration
</output>
