---
phase: 13-admin-order-workflow
plan: 01
type: execute
wave: 1
depends_on: ["12-02"]
files_modified:
  - src/db/schema.ts
  - src/db/orders.ts
  - src/db/__tests__/orders-admin.test.ts
  - src/app/api/admin/orders/route.ts
  - src/app/api/admin/orders/[id]/route.ts
  - src/app/api/admin/orders/__tests__/route.test.ts
  - src/app/admin/orders/page.tsx
  - src/app/admin/orders/_components/orders-table.tsx
  - src/app/admin/orders/_components/order-detail.tsx
autonomous: true
requirements:
  - ORD-04
  - ORD-05
must_haves:
  truths:
    - "Admin can filter order history by status"
    - "Admin can search order history by order ref, buyer name, or buyer email"
    - "Admin can update order status among pending, confirmed, and completed"
    - "Admin can save a private internal note on an order"
    - "Order status and internal note mutations are auth-gated"
    - "Buyer-facing confirmation and storefront pages never expose internal admin notes"
  artifacts:
    - path: "src/db/orders.ts"
      provides: "Order search/filter and workflow update helpers"
      exports: ["getAdminOrders", "updateOrderWorkflow"]
    - path: "src/app/api/admin/orders/[id]/route.ts"
      provides: "Authenticated order workflow update endpoint"
      contains: "PATCH"
    - path: "src/app/admin/orders/page.tsx"
      provides: "Order list with search and status filters"
      contains: "status"
    - path: "src/app/admin/orders/_components/order-detail.tsx"
      provides: "Status controls and internal note UI"
      contains: "Internal note"
---

<objective>
Add the first operational layer to admin orders: search/filter the order list, update order status, and store one private internal note per order.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-admin-order-workflow/13-CONTEXT.md
@.planning/phases/11-checkout-upgrade-order-history/11-02-SUMMARY.md
@.planning/phases/12-bulk-operations-dashboard/12-02-SUMMARY.md
</execution_context>

<context>
@src/db/schema.ts
@src/db/orders.ts
@src/app/admin/orders/page.tsx
@src/app/admin/orders/[id]/page.tsx
@src/app/admin/orders/_components/orders-table.tsx
@src/app/admin/orders/_components/order-detail.tsx
@src/app/api/admin/orders/route.ts
@src/app/api/admin/orders/[id]/route.ts
@src/lib/auth/admin-check.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add order workflow tests</name>
  <files>
    src/db/__tests__/orders-admin.test.ts,
    src/app/api/admin/orders/__tests__/route.test.ts
  </files>
  <read_first>
    src/db/orders.ts,
    src/db/schema.ts,
    src/app/api/admin/orders/__tests__/route.test.ts,
    src/app/api/admin/cards/__tests__/route.test.ts
  </read_first>
  <behavior>
    - `getAdminOrders({ q, status, page, limit })` filters by order ref, buyer name, buyer email, and status.
    - `updateOrderWorkflow({ orderId, status, adminNote })` updates only provided fields and returns the updated order summary.
    - `PATCH /api/admin/orders/[id]` rejects unauthenticated/non-admin requests consistently with existing admin routes.
    - `PATCH /api/admin/orders/[id]` validates status values and admin note type/length.
    - Internal notes are not included in public checkout/confirmation data.
  </behavior>
  <action>
    Write failing tests first. Mock the DB/query layer at the same level as existing order helper tests. Route tests should mock `requireAdmin` and helper functions; do not hit a real database.
  </action>
  <verify>
    <automated>npx vitest run src/db/__tests__/orders-admin.test.ts src/app/api/admin/orders/__tests__/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because filtering/update contracts are missing.
    - Tests cover search, status filter, status validation, note validation, auth, and successful mutation.
  </acceptance_criteria>
  <done>Red tests define order workflow contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend schema and order helpers</name>
  <files>
    src/db/schema.ts,
    src/db/orders.ts,
    src/db/__tests__/orders-admin.test.ts
  </files>
  <read_first>
    src/db/schema.ts,
    src/db/orders.ts,
    src/db/__tests__/orders-admin.test.ts,
    drizzle.config.ts
  </read_first>
  <behavior>
    - `orders` can store `adminNote` as private text.
    - Order helpers support `pending`, `confirmed`, and `completed` workflow changes.
    - Query helpers keep prices in dollars for UI/API consumers while DB cents remain internal.
    - Search/filter happens in the query/helper layer, not by fetching every order into the browser.
  </behavior>
  <action>
    Add an internal admin note column to the order schema.

    Keep `cancelled` schema work for 13-02 unless execution proves adding the enum value now is simpler. This plan only needs the existing non-cancelled lifecycle states.

    Extend `getAdminOrders` params with:
    ```typescript
    interface AdminOrdersParams {
      page?: number;
      limit?: number;
      q?: string;
      status?: OrderStatus | "all";
    }
    ```

    Add:
    ```typescript
    export async function updateOrderWorkflow(input: UpdateOrderWorkflowInput): Promise<AdminOrderDetail | null>;
    ```
  </action>
  <verify>
    <automated>npx vitest run src/db/__tests__/orders-admin.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Order helper tests pass.
    - TypeScript passes.
    - Schema change path is documented in the task summary so execution knows whether a DB push/manual DDL was needed.
  </acceptance_criteria>
  <done>Order query/update helpers support workflow state and notes.</done>
</task>

<task type="auto">
  <name>Task 3: Add order workflow API and UI</name>
  <files>
    src/app/api/admin/orders/route.ts,
    src/app/api/admin/orders/[id]/route.ts,
    src/app/admin/orders/page.tsx,
    src/app/admin/orders/_components/orders-table.tsx,
    src/app/admin/orders/_components/order-detail.tsx
  </files>
  <read_first>
    src/app/api/admin/orders/route.ts,
    src/app/api/admin/orders/[id]/route.ts,
    src/app/admin/orders/page.tsx,
    src/app/admin/orders/_components/orders-table.tsx,
    src/app/admin/orders/_components/order-detail.tsx,
    src/app/admin/_components/toast.tsx,
    src/app/admin/_components/pagination.tsx
  </read_first>
  <behavior>
    - `/admin/orders` exposes search and status filter controls backed by URL search params.
    - Order list keeps pagination and shows filtered result count or empty state.
    - Order detail exposes status controls and internal note editing.
    - Successful mutations refresh visible order data.
    - Mutation failures preserve local input and show clear admin-facing errors.
  </behavior>
  <action>
    Add `PATCH /api/admin/orders/[id]` for status/note updates.

    Prefer server-rendered list filtering via `searchParams` on `/admin/orders` and small client components only for the interactive detail controls. Keep direct server-side admin auth checks on pages.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Admin can search and filter orders.
    - Admin can change status between pending/confirmed/completed.
    - Admin can save and reload an internal note.
    - Internal note is absent from buyer-facing confirmation data.
    - Full tests and build pass.
  </acceptance_criteria>
  <done>Admin can manage non-cancelled order workflow state and notes.</done>
</task>

<task type="checkpoint:verify" gate="blocking">
  <name>Task 4: Browser verify order workflow basics</name>
  <files>src/app/admin/orders/page.tsx</files>
  <action>
    Browser verification must include:
    1. Create a disposable checkout order in local/dev.
    2. Sign into admin locally.
    3. Visit `/admin/orders` and search by buyer email and order ref.
    4. Filter by `pending` and confirm the order appears; filter by a different status and confirm it does not.
    5. Open the order detail, change status to `confirmed`, save an internal note, reload, and confirm both persisted.
    6. Confirm browser console and network logs have no unexpected errors.
    7. Clean up disposable order/item/card rows.
  </action>
  <done>Order workflow basics are verified end-to-end in browser and DB.</done>
</task>

</tasks>

<verification>
- `git diff --check`
- `npx vitest run src/db/__tests__/orders-admin.test.ts src/app/api/admin/orders/__tests__/route.test.ts`
- `npx tsc --noEmit`
- `npm test`
- `npm run build`
- Browser verification of order search/filter/status/note persistence
</verification>

<success_criteria>
- [ ] ORD-04: Admin can update order status through the order detail page
- [ ] ORD-05: Admin can search and filter orders from `/admin/orders`
- [ ] Internal notes persist and remain admin-only
- [ ] Admin order workflow APIs are auth-gated and tested
</success_criteria>

<output>
After completion, create `.planning/phases/13-admin-order-workflow/13-01-SUMMARY.md` with:
- final helper/API contracts
- schema migration notes
- browser/DB verification evidence
- deferred cancellation/restore notes for 13-02
</output>
