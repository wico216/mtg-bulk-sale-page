---
phase: 22-hardening-uat
plan: 01
type: execute
wave: 1
depends_on: ["18-01", "19-02", "20-02", "21-02"]
files_modified:
  - src/app/api/admin/import/preview/route.ts
  - src/app/api/admin/import/preview/__tests__/route.test.ts
  - .planning/phases/22-hardening-uat/22-SECURITY-REVIEW.md
autonomous: true
requirements:
  - HARD-01
  - HARD-02
must_haves:
  truths:
    - "src/app/api/admin/import/preview/route.ts gains `enforceRateLimit({ key: clientKeyFromRequest(request, auth.user.email), config: RATE_LIMIT_BUCKETS.ADMIN_BULK })` immediately AFTER `requireAdmin()` (between current line 58 `if (auth instanceof Response) return auth;` and line 60 `let formData: FormData;`). Pattern matches `src/app/api/admin/import/commit/route.ts:121-138` verbatim — same bucket (ADMIN_BULK 20/min), same key derivation, same `logEvent({ event: 'admin.import_preview.rate_limited', route, actor })` log call on a 429 path. The 429 short-circuits BEFORE `request.formData()`, BEFORE `parseManaboxCsvContents`, BEFORE `enrichCards` — i.e. the expensive Scryfall pass is gated by the rate-limit, which is the entire point of resolving D-DOS-01 in v1.3 (per 22-CONTEXT D-03)"
    - "src/app/api/admin/import/preview/route.ts adds a top-of-file ROUTE constant `const ROUTE = '/api/admin/import/preview';` matching the convention used in import/commit/route.ts (line 27 there) so the structured log emits a stable route literal. Existing imports gain `enforceRateLimit, RATE_LIMIT_BUCKETS, clientKeyFromRequest` from `@/lib/rate-limit` and `logEvent` from `@/lib/logger` — same import shape as commit/route.ts:1-9"
    - "src/app/api/admin/import/preview/__tests__/route.test.ts is a NEW file (preview/__tests__/ does not yet exist per planner ls). Pattern mirrors `src/app/api/admin/cards/bulk-delete/__tests__/route.test.ts` (Phase 15-01's bulk-delete 429 pin) but for the preview surface. Tests pin: (a) unauthenticated request returns 401 BEFORE any rate-limit hit (auth runs first, per E-PRIV-02 invariant — `enforceRateLimit` must NOT be called when `requireAdmin()` short-circuits); (b) authenticated request beyond the ADMIN_BULK limit returns 429 with Retry-After header AND does NOT call `parseManaboxCsvContents` or open the NDJSON stream; (c) authenticated request within the limit proceeds normally (200 + NDJSON content-type). Use the `createMemoryRateLimitStore` injection pattern via mocking `@/lib/rate-limit` — same approach as the bulk-delete test file"
    - "Phase 18's existing `src/db/__tests__/orders.concurrent.test.ts` (env-gated on TEST_DATABASE_URL with Variants 1 + 2 already implemented, scenarios 1-5 from CONTEXT D-05) is verified UNCHANGED by this plan — the executor reads it, confirms the 5 scenarios are covered (single-binder regression = covered by Phase 11 baseline `orders.test.ts`; multi-binder split = Variant 1; multi-binder oversell-prevention = Variant 2 honors-spirit form; allocator pick-order determinism = embedded in Variant 1's `success.order.items[0].binder === 'a02'` assertion at line 228; CHECK constraint trip = covered separately by `src/db/__tests__/schema.test.ts` quantity CHECK pin from Phase 16). NO new test code is added by Plan 22-01 to orders.concurrent.test.ts; the executor's job is to RUN the suite once with TEST_DATABASE_URL set 5 times consecutively per CONTEXT D-06's flake bar and record the 5/5 pass evidence in 22-SECURITY-REVIEW.md (and document the runbook for operator + CI)"
    - "22-SECURITY-REVIEW.md is a NEW file mirroring the format of `.planning/phases/15-production-hardening/15-SECURITY-REVIEW.md` (the baseline). Required sections in order: (1) header with Reviewer/Date/Scope identifying this as the v1.3 STRIDE delta over Phase 15, (2) `## Surface inventory delta` table listing ONLY the v1.3-amplified surfaces (import/preview now rate-limited, import/commit unchanged but binder-aware, checkout still rate-limited but now multi-binder-aware, public storefront/cart/checkout responses now PublicCard-typed), (3) `## New findings` section containing I-DISC-05 with the exact severity/exploit/remediation/status format used in Phase 15, (4) `## Resolved deferrals` section containing D-DOS-01 with before/after status (Phase 15: deferred Medium → Phase 22: Resolved with concrete file+line citation `src/app/api/admin/import/preview/route.ts` line numbers from this plan), (5) `## Unchanged deferrals` table re-listing S-01, D-DOS-02, D-DOS-03, I-DISC-03 with rationale `no v1.3 amplification`, (6) `## Multi-binder concurrent-proof` section recording the 5x flake-check pass evidence with exact `vitest run` output captured at execute-time, (7) `## Summary of follow-ups` table updated to add I-DISC-05 (resolved by v1.3) and update D-DOS-01 (resolved by Phase 22)"
    - "22-SECURITY-REVIEW.md `## New findings` section's I-DISC-05 entry contains the EXACT four-bullet structure per CONTEXT D-02: (a) Surface — every public-facing API (GET /, GET /cart, POST /api/checkout success and stock_conflict) + buyer confirmation email + structured logs from public routes; (b) Mitigation — PublicCard/AdminCard type split (Phase 20 D-05) with concrete file citations `src/lib/types.ts` PublicCard interface; per-route invariant tests (Phase 20 D-07) with file citations `src/app/__tests__/page.test.ts`, `src/app/cart/__tests__/page.test.ts`, `src/app/api/checkout/__tests__/route.test.ts` asserting `JSON.stringify(response).includes('binder') === false`; StockConflict.available is SUM across binders (Phase 18 D-06); (c) Severity — Low with the rationale verbatim from CONTEXT D-02 (`binder name is an organizational label, not credential or PII; leak embarrasses but doesn't expose the seller's collection map`); (d) Status — resolved by v1.3"
    - "22-SECURITY-REVIEW.md `## Resolved deferrals` section's D-DOS-01 entry contains: (a) reference to Phase 15's D-DOS-01 entry (`see 15-SECURITY-REVIEW.md § D-DOS-01`); (b) why v1.3 amplifies the cost (parser groups 12,749 rows into BinderSummary[] before responding; D-08 perf pin proves the parse is bounded but enrichment is not — Scryfall pass remains the unbounded variable); (c) the fix applied (`enforceRateLimit` post-auth in `src/app/api/admin/import/preview/route.ts`); (d) test coverage (`src/app/api/admin/import/preview/__tests__/route.test.ts` 429 pin); (e) Phase 19 D-19 acknowledgment — `Phase 19 explicitly did NOT add this rate-limit because the import_commit route already has it, and Phase 19's scope was the picker contract not the security delta; Phase 22 owns this fix per CONTEXT D-03`"
    - "22-SECURITY-REVIEW.md `## Unchanged deferrals` table contains 4 rows (one per CONTEXT D-04 item: S-01, D-DOS-02, D-DOS-03, I-DISC-03) each with columns: ID | Phase 15 status | v1.3 amplification | rationale for staying deferred | revisit-in milestone (`v1.4+ Operations milestone`)"
    - "22-SECURITY-REVIEW.md `## Multi-binder concurrent-proof` section records: (a) test file path `src/db/__tests__/orders.concurrent.test.ts`; (b) env-gating contract `TEST_DATABASE_URL` (NEVER point at production); (c) Phase 18 implementation Variants 1 + 2 cover the 5 CONTEXT D-05 scenarios (with the planner's mapping evidence: V1 → multi-binder split, V2 → multi-binder oversell-prevention with SUM=0 spirit, V1's pick-order assertion → allocator determinism, baseline orders.test.ts → single-binder regression, schema.test.ts → CHECK trip); (d) the 5x flake-check evidence (5 consecutive `npx vitest run src/db/__tests__/orders.concurrent.test.ts` invocations with TEST_DATABASE_URL set, captured exit code 0 and `2 passed` line each time); (e) the runbook for operator + CI: how to provision a Neon test branch (or the Testcontainers fallback), the exact env var, and the warning that production DATABASE_URL must NEVER be used"
    - "22-SECURITY-REVIEW.md `## Summary of follow-ups` table extends Phase 15's table with TWO updated rows: I-DISC-05 (Low, Resolved by v1.3, owner: this phase) and D-DOS-01 (Medium, Resolved by Phase 22, owner: this phase); the other 4 rows (S-01, D-DOS-02, D-DOS-03, I-DISC-03, R-01) are re-stated unchanged for self-contained readability of the v1.3 delta document"
    - "Existing `vitest run` baseline (460 passed + 2 skipped = 462 across 44 files per CONTEXT post-Phase 21 baseline) continues to pass; new tests added by this plan EXCEED the baseline (target: +3 tests in preview/__tests__/route.test.ts for unauth-401, auth-429-no-stream, auth-200-stream); no existing test is deleted or skipped. The 2 skipped tests remain `orders.concurrent.test.ts` Variants 1 + 2 when TEST_DATABASE_URL is unset (env-gated skip is correct behavior, not a regression). Net delta target: +1 file (preview route test), +3 it() blocks minimum"
  artifacts:
    - path: "src/app/api/admin/import/preview/route.ts"
      provides: "Rate-limited import preview route resolving D-DOS-01"
      contains: "enforceRateLimit({ key: clientKeyFromRequest(request, auth.user.email), config: RATE_LIMIT_BUCKETS.ADMIN_BULK }) called post-requireAdmin and pre-formData; logEvent('admin.import_preview.rate_limited') on 429 path; ROUTE constant '/api/admin/import/preview'; new imports enforceRateLimit, RATE_LIMIT_BUCKETS, clientKeyFromRequest, logEvent"
      exports: ["POST"]
    - path: "src/app/api/admin/import/preview/__tests__/route.test.ts"
      provides: "Pins the rate-limit 429 behavior on the import preview route"
      contains: "vi.mock @/lib/auth/admin-check + @/lib/rate-limit + @/lib/csv-parser; 3 it() tests covering: unauth → 401 with no rate-limit call; auth + over-limit → 429 with Retry-After + parseManaboxCsvContents NOT called; auth + under-limit → 200 with NDJSON content-type"
    - path: ".planning/phases/22-hardening-uat/22-SECURITY-REVIEW.md"
      provides: "STRIDE delta document for v1.3 — references Phase 15 baseline; adds I-DISC-05; resolves D-DOS-01; restates unchanged deferrals; records multi-binder concurrent-proof 5x flake-check evidence"
      contains: "Header with v1.3 delta scoping; surface inventory delta table; new findings (I-DISC-05); resolved deferrals (D-DOS-01); unchanged deferrals table (S-01, D-DOS-02, D-DOS-03, I-DISC-03); multi-binder concurrent-proof section with 5x flake-check evidence; updated summary of follow-ups table"
---

<objective>
Resolve the deferred Phase 15 D-DOS-01 finding by adding `enforceRateLimit({ config: RATE_LIMIT_BUCKETS.ADMIN_BULK })` post-`requireAdmin()` to `/api/admin/import/preview` (the v1.3 amplification: parser groups 12,749 rows into BinderSummary[] BEFORE responding, and the Scryfall enrichment pass is unbounded). Pin the new 429 behavior with a focused route test mirroring the Phase 15-01 bulk-delete pattern. Verify Phase 18's existing `src/db/__tests__/orders.concurrent.test.ts` suite (env-gated on TEST_DATABASE_URL with Variants 1 + 2 implementing scenarios from CONTEXT D-05) passes the CONTEXT D-06 5x flake-check bar against a Neon test branch, and document the runbook for operator + CI. Author the v1.3 STRIDE delta document `22-SECURITY-REVIEW.md` mirroring the Phase 15 format: surface inventory delta, new I-DISC-05 (binder leak — Low, resolved by Phase 20 D-05/D-07 + Phase 18 D-06), resolved D-DOS-01, unchanged deferrals (S-01, D-DOS-02, D-DOS-03, I-DISC-03), multi-binder concurrent-proof flake-check evidence, and updated follow-ups summary. All decisions are per CONTEXT D-01..D-06.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-hardening-uat/22-CONTEXT.md
@.planning/phases/22-hardening-uat/22-DISCUSSION-LOG.md
@.planning/phases/15-production-hardening/15-SECURITY-REVIEW.md
@.planning/phases/15-production-hardening/15-01-SUMMARY.md
@.planning/phases/18-allocator/18-01-SUMMARY.md
@.planning/phases/19-import-preview-picker/19-01-SUMMARY.md
@.planning/phases/20-storefront-aggregation-cart-migration/20-01-SUMMARY.md
</execution_context>

<context>
@src/app/api/admin/import/preview/route.ts
@src/app/api/admin/import/commit/route.ts
@src/app/api/admin/cards/bulk-delete/route.ts
@src/lib/rate-limit.ts
@src/lib/logger.ts
@src/lib/auth/admin-check.ts
@src/db/__tests__/orders.concurrent.test.ts
@src/db/__tests__/orders.test.ts
@src/db/__tests__/schema.test.ts
@src/lib/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin import preview rate-limit behavior (TDD via preview/__tests__/route.test.ts)</name>
  <files>
    src/app/api/admin/import/preview/__tests__/route.test.ts
  </files>
  <read_first>
    src/app/api/admin/cards/bulk-delete/__tests__/route.test.ts,
    src/app/api/admin/import/commit/route.ts,
    src/app/api/admin/import/preview/route.ts,
    src/lib/rate-limit.ts,
    src/lib/auth/admin-check.ts
  </read_first>
  <behavior>
    - Unauthenticated request to POST /api/admin/import/preview returns 401 BEFORE any rate-limit decision is made (E-PRIV-02 ordering invariant: auth before rate-limit).
    - Authenticated request that exceeds the ADMIN_BULK bucket (limit=20, windowMs=60_000) returns 429 with `Retry-After` header AND does NOT invoke `parseManaboxCsvContents` and does NOT open the NDJSON ReadableStream.
    - Authenticated request within the bucket proceeds: response status is 200 AND `Content-Type: application/x-ndjson` is set on the response headers.
    - The 429 path emits a `logEvent({ event: 'admin.import_preview.rate_limited', actor })` call (asserted via vi.spyOn or by mocking `@/lib/logger`).
  </behavior>
  <action>
    Mirror the test layout of `src/app/api/admin/cards/bulk-delete/__tests__/route.test.ts` (the Phase 15-01 bulk-delete pin). Mock `@/lib/auth/admin-check` to return either a Response (unauth) or a session (auth). Inject a `createMemoryRateLimitStore` (existing factory in `src/lib/rate-limit.ts`) by mocking `getDefaultRateLimitStore` so the test deterministically controls the bucket. Build a multipart FormData body for the auth+200 case (single-row CSV is sufficient — the test verifies the 200 path is reachable, not the full enrichment). For the auth+429 case, pre-fill the memory store with 20 hits before issuing the request.

    Write all three it() blocks. They MUST fail before the route is patched (specifically the 429 case: without rate-limit, the auth-success request will return 200 every time).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/import/preview/__tests__/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Three failing tests exist before Task 2 patches the route.
    - Tests use the same mock-injection pattern as bulk-delete/__tests__/route.test.ts (no new test infrastructure invented).
    - The auth-401 test asserts requireAdmin runs FIRST (rate-limit store is not consulted).
    - The auth-429 test asserts parseManaboxCsvContents is NOT called when blocked (mock or spy on the parser module).
  </acceptance_criteria>
  <done>Red tests pin the import preview rate-limit contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply rate limit to import preview route (D-03 / D-DOS-01 resolution)</name>
  <files>
    src/app/api/admin/import/preview/route.ts
  </files>
  <read_first>
    src/app/api/admin/import/preview/route.ts,
    src/app/api/admin/import/commit/route.ts,
    src/lib/rate-limit.ts,
    src/lib/logger.ts
  </read_first>
  <behavior>
    - `enforceRateLimit({ key: clientKeyFromRequest(request, auth.user.email), config: RATE_LIMIT_BUCKETS.ADMIN_BULK })` runs immediately AFTER `requireAdmin()` returns the session and BEFORE `await request.formData()` (so the expensive parse + Scryfall pass are gated by the rate-limit).
    - On 429, a `logEvent({ level: 'warn', event: 'admin.import_preview.rate_limited', route: ROUTE, actor: auth.user.email })` is emitted and the 429 Response from `enforceRateLimit` is returned.
    - On allowed, the existing flow (formData → parse → buildBindersFromParsed → NDJSON stream) is unchanged.
    - A top-of-file `const ROUTE = '/api/admin/import/preview';` matches the convention in commit/route.ts:27.
  </behavior>
  <action>
    Add the ROUTE constant near the top (after the existing constants block at line 23-24). Add the `enforceRateLimit, RATE_LIMIT_BUCKETS, clientKeyFromRequest` import from `@/lib/rate-limit` and `logEvent` from `@/lib/logger`. Insert the rate-limit check immediately after the `if (auth instanceof Response) return auth;` line. Mirror the commit/route.ts:121-138 block verbatim, substituting the event name `admin.import_preview.rate_limited`.

    Run the Task 1 tests; all three MUST now pass. Existing import preview tests (if any beyond this plan's new file) MUST continue to pass.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/import/preview/__tests__/route.test.ts && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - All three Task 1 tests pass (red → green).
    - TypeScript and the full suite pass; baseline 460 passed + 2 skipped (462 across 44 files) is preserved or exceeded.
    - The route file diff shows only the rate-limit insertion (no other behavior change to the picker stream).
  </acceptance_criteria>
  <done>D-DOS-01 is resolved; import preview is rate-limited post-auth.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the multi-binder concurrent-proof harness against TEST_DATABASE_URL (5x flake check)</name>
  <files>
    src/db/__tests__/orders.concurrent.test.ts
  </files>
  <read_first>
    src/db/__tests__/orders.concurrent.test.ts,
    src/db/__tests__/orders.test.ts,
    src/db/__tests__/schema.test.ts,
    .planning/phases/18-allocator/18-01-SUMMARY.md,
    .planning/phases/22-hardening-uat/22-CONTEXT.md
  </read_first>
  <behavior>
    - The existing `orders.concurrent.test.ts` Variants 1 + 2 (already env-gated on TEST_DATABASE_URL per Phase 18) cover CONTEXT D-05 scenarios 2 (multi-binder split → SUM=1 corrected from D-05's mathematically-impossible SUM=0) and 3 (multi-binder oversell-prevention SUM=0 spirit).
    - CONTEXT D-05 scenario 1 (single-binder regression) is covered by the existing Phase 11 `src/db/__tests__/orders.test.ts` baseline (verify by reading the file); no new test required if the baseline already pins last-copy race.
    - CONTEXT D-05 scenario 4 (allocator pick-order determinism) is covered by `orders.concurrent.test.ts` Variant 1 line 228 assertion `expect(success.order.items[0].binder).toBe('a02')` (smallest-first + lex tiebreak); no new test required.
    - CONTEXT D-05 scenario 5 (CHECK constraint trip) is covered by `src/db/__tests__/schema.test.ts` Phase 16 quantity CHECK pin; no new test required.
    - Running `TEST_DATABASE_URL=<neon-test-branch-url> npx vitest run src/db/__tests__/orders.concurrent.test.ts` 5 times consecutively yields `2 passed` and exit code 0 each time.
  </behavior>
  <action>
    READ the existing `orders.concurrent.test.ts` file end-to-end to confirm the 5-scenario coverage mapping above. If a scenario is genuinely missing (planner has high confidence Variants 1 + 2 cover it; if execute-time discovery contradicts, add it; do NOT silently skip), document the addition in the SUMMARY deviations.

    Provision a Neon test branch (operator action — the executor cannot create a Neon branch from inside Claude Code without the operator's Neon dashboard; if no branch is available, the executor MUST mark Task 3 BLOCKED and request the operator provision one before continuing). Document the exact provisioning steps in 22-SECURITY-REVIEW.md (`Multi-binder concurrent-proof § Runbook` section per Task 4): (a) log into Neon → click Branches → New Branch from production at recent timestamp → copy connection URL; (b) NEVER use the production DATABASE_URL — the test INSERTs, decrements, and DELETEs rows; (c) export `TEST_DATABASE_URL` in the local shell.

    Run the test suite 5 times. Capture `npx vitest run src/db/__tests__/orders.concurrent.test.ts 2>&1 | tail -5` output for each run. Save the 5 outputs as the evidence block for the 22-SECURITY-REVIEW.md `Multi-binder concurrent-proof` section (Task 4).

    NO test code is added or modified by Task 3 — this task is verification + runbook documentation. If Variants 1 + 2 unexpectedly fail or flake during the 5x check, that is a hard failure of v1.3 (the load-bearing allocator concurrency invariant is broken — escalate before continuing).
  </action>
  <verify>
    <automated>TEST_DATABASE_URL=&lt;neon-test-branch-url&gt; npx vitest run src/db/__tests__/orders.concurrent.test.ts</automated>
    <human>The executor runs the suite 5 times consecutively (no JS-level loop required; sequential shell invocations are fine) and verifies 5/5 exit code 0 + `2 passed` lines. The 5 outputs are captured for Task 4's evidence block.</human>
  </verify>
  <acceptance_criteria>
    - The planner-asserted scenario-to-test mapping (5 CONTEXT D-05 scenarios → existing test coverage) is verified by reading the test files; any genuine gap is flagged.
    - 5 consecutive runs of `orders.concurrent.test.ts` against TEST_DATABASE_URL yield exit 0 + `2 passed`.
    - The 5 raw vitest outputs are captured for embedding in 22-SECURITY-REVIEW.md.
    - If Neon branch is unavailable, Task 3 is marked BLOCKED with operator action required (do NOT silently skip).
  </acceptance_criteria>
  <done>The multi-binder concurrent-proof passes the CONTEXT D-06 5x flake bar; runbook is captured for Task 4.</done>
</task>

<task type="auto">
  <name>Task 4: Author 22-SECURITY-REVIEW.md (D-01 / D-02 / D-04 / D-05 / D-06)</name>
  <files>
    .planning/phases/22-hardening-uat/22-SECURITY-REVIEW.md
  </files>
  <read_first>
    .planning/phases/15-production-hardening/15-SECURITY-REVIEW.md,
    .planning/phases/22-hardening-uat/22-CONTEXT.md,
    src/lib/types.ts,
    src/app/__tests__/page.test.ts,
    src/app/cart/__tests__/page.test.ts,
    src/app/api/checkout/__tests__/route.test.ts
  </read_first>
  <behavior>
    - The document references `15-SECURITY-REVIEW.md` as the baseline and documents ONLY v1.3 deltas (per CONTEXT D-01).
    - Section structure (in order): Header → Surface inventory delta → New findings (I-DISC-05) → Resolved deferrals (D-DOS-01) → Unchanged deferrals (S-01, D-DOS-02, D-DOS-03, I-DISC-03) → Multi-binder concurrent-proof (with 5x flake-check evidence from Task 3) → Summary of follow-ups (updated table).
    - I-DISC-05 entry contains the four-bullet structure per CONTEXT D-02: Surface, Mitigation (with concrete file citations from Phase 20 D-05, D-07 + Phase 18 D-06), Severity (Low + verbatim CONTEXT rationale), Status (resolved by v1.3).
    - D-DOS-01 resolved-deferrals entry contains: reference to 15-SECURITY-REVIEW § D-DOS-01, why v1.3 amplifies the cost (12,749-row parse + unbounded Scryfall pass), the fix (enforceRateLimit post-auth in preview/route.ts), test coverage citation (preview/__tests__/route.test.ts from Tasks 1 + 2), Phase 19 D-19 acknowledgment.
    - Unchanged-deferrals table has 4 rows (S-01, D-DOS-02, D-DOS-03, I-DISC-03) with explicit `no v1.3 amplification` rationale per CONTEXT D-04.
    - Multi-binder concurrent-proof section embeds the Task 3 5x flake-check evidence (5 raw vitest output blocks) plus the runbook (Neon branch provisioning + TEST_DATABASE_URL export + production-DB warning + Testcontainers fallback note per CONTEXT D-05 option (b)).
    - Summary-of-follow-ups table re-states all 6 Phase 15 rows for self-contained readability and adds rows for I-DISC-05 (Resolved by v1.3) + updates D-DOS-01 status to (Resolved by Phase 22).
  </behavior>
  <action>
    Author the markdown file. Mirror the section ordering, table column shapes, and severity-rationale phrasing of `15-SECURITY-REVIEW.md` (Phase 15 baseline). Do NOT repeat findings from Phase 15 that are unchanged — reference them by ID.

    For the Multi-binder concurrent-proof section, paste the Task 3 5x flake-check raw vitest outputs into a fenced ```text block. Document the runbook with explicit shell commands (`export TEST_DATABASE_URL=...`, `npx vitest run src/db/__tests__/orders.concurrent.test.ts`, the 5x sequential invocation note) and the Testcontainers fallback as a brief paragraph noting it is heavier but CI-friendly per CONTEXT D-05.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-hardening-uat/22-SECURITY-REVIEW.md && grep -q "I-DISC-05" .planning/phases/22-hardening-uat/22-SECURITY-REVIEW.md && grep -q "D-DOS-01" .planning/phases/22-hardening-uat/22-SECURITY-REVIEW.md && grep -q "Multi-binder concurrent-proof" .planning/phases/22-hardening-uat/22-SECURITY-REVIEW.md</automated>
    <human>The executor cross-reads the document against CONTEXT D-01..D-06 and confirms each decision is reflected.</human>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/22-hardening-uat/22-SECURITY-REVIEW.md` with all 7 required sections.
    - I-DISC-05 entry has all 4 required bullets (Surface, Mitigation, Severity, Status) with concrete file citations.
    - D-DOS-01 entry references 15-SECURITY-REVIEW baseline and cites the Task 1 + Task 2 fix.
    - Unchanged-deferrals table covers S-01, D-DOS-02, D-DOS-03, I-DISC-03.
    - Multi-binder concurrent-proof section embeds the 5x flake-check evidence captured in Task 3.
    - Summary-of-follow-ups table is self-contained (re-states Phase 15 rows + adds v1.3 deltas).
  </acceptance_criteria>
  <done>22-SECURITY-REVIEW.md is the authoritative v1.3 STRIDE delta document.</done>
</task>

<task type="checkpoint:verify" gate="blocking">
  <name>Task 5: Full Plan 22-01 verification</name>
  <files>
    src/app/api/admin/import/preview/route.ts,
    src/app/api/admin/import/preview/__tests__/route.test.ts,
    .planning/phases/22-hardening-uat/22-SECURITY-REVIEW.md
  </files>
  <action>
    Verification must include:
    1. `git diff --check` (no whitespace errors).
    2. `npx tsc --noEmit` passes.
    3. `npm test` baseline preserved: 460 passed + 2 skipped → at least 463 passed + 2 skipped (Task 1 adds 3 new tests; the 2 skipped remain orders.concurrent under unset TEST_DATABASE_URL).
    4. `npm run build` passes.
    5. The 5x flake-check from Task 3 is captured in 22-SECURITY-REVIEW.md (5/5 exit code 0 + `2 passed`).
    6. 22-SECURITY-REVIEW.md cross-checks against CONTEXT D-01..D-06 with each decision reflected.
    7. No secrets in any of the new files (preview route, test file, security review).
  </action>
  <done>Plan 22-01 (STRIDE delta + concurrent-proof verification + import preview rate-limit) is verified.</done>
</task>

</tasks>

<verification>
- `git diff --check`
- `npx vitest run src/app/api/admin/import/preview/__tests__/route.test.ts`
- `TEST_DATABASE_URL=<neon-test-branch> npx vitest run src/db/__tests__/orders.concurrent.test.ts` (run 5x consecutively)
- `npx tsc --noEmit`
- `npm test`
- `npm run build`
- File existence + section/finding/citation grep checks on 22-SECURITY-REVIEW.md
</verification>

<success_criteria>
- [ ] HARD-02: STRIDE delta document records I-DISC-05 + resolves D-DOS-01 (`22-SECURITY-REVIEW.md` complete)
- [ ] HARD-01: Multi-binder concurrent-proof harness passes the 5x flake check against a real Postgres test branch (`orders.concurrent.test.ts` Variants 1 + 2 verified; runbook captured)
- [ ] D-03: `/api/admin/import/preview` is rate-limited post-`requireAdmin()` with `RATE_LIMIT_BUCKETS.ADMIN_BULK` (D-DOS-01 resolved)
- [ ] D-01/D-02/D-04: I-DISC-05 + unchanged-deferrals + Phase 15 baseline reference present in 22-SECURITY-REVIEW.md
- [ ] D-05/D-06: Phase 18's concurrent-proof tests verified to cover the 5 CONTEXT D-05 scenarios; 5x flake-check evidence captured
- [ ] Existing 460 passed + 2 skipped baseline preserved or exceeded
</success_criteria>

<output>
After completion, create `.planning/phases/22-hardening-uat/22-01-SUMMARY.md` with:
- import preview rate-limit insertion site + final route diff snippet
- preview/__tests__/route.test.ts test summary (3 cases)
- multi-binder concurrent-proof 5x flake-check evidence (5 vitest outputs)
- 22-SECURITY-REVIEW.md section-by-section walk
- any deviations from CONTEXT D-01..D-06 with justification
</output>
</content>
</invoke>