---
phase: 22-hardening-uat
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - scripts/generate-large-fixture.ts
  - test-fixtures/large-export.csv
  - src/lib/__tests__/csv-parser-perf.test.ts
  - .planning/phases/22-hardening-uat/22-HUMAN-UAT.md
autonomous: true
requirements:
  - HARD-03
  - HARD-04
must_haves:
  truths:
    - "scripts/generate-large-fixture.ts is a NEW Node script (tsx-runnable per the existing scripts/smoke-production.ts and scripts/migrate-v1.3-binder.ts pattern). It synthesizes a 12,749-row Manabox CSV with the canonical Phase 17 header (`Name,Set code,Set name,Collector number,Condition,Quantity,Foil,Rarity,Binder Name,Binder Type` per src/lib/__tests__/csv-parser-content.test.ts:15-37 plus the binder columns) and writes to `test-fixtures/large-export.csv`. The synthetic data uses fictional set codes (`tst`, `fix`, `syn`) and fictional card names (`Test Card 1`..`Test Card 12749`) so it is unmistakably synthetic per Phase 17 D-11 privacy (NEVER ship the operator's real export). Binder distribution: spread rows across 14 distinct binder names (`a01`..`a14` + `unsorted`) to mirror the operator's real-world distribution shape; quantities random 1-4; finishes mostly `normal` with ~5% `foil` and ~1% `etched`; conditions distributed across the 5 Phase 16 enum values; rarities distributed across `common/uncommon/rare/mythic`. Set codes vary across ~10 fake sets to keep aggregated keys distinct. Script is idempotent: deterministic seed via `Math.seedrandom`-like pattern OR a fixed-iteration loop with simple modular arithmetic so consecutive runs produce byte-identical output (tests can pin file size). Final file size target: ~2MB ± 200KB"
    - "test-fixtures/ directory is NEW (planner ls confirmed it does not exist). The generated CSV is committed to the repo (~2MB is acceptable for a once-generated fixture; per CONTEXT D-08 it is checked in unless too noisy. 2MB plain-text CSV gzip-compresses well in git's pack files; not noisy enough to gitignore). Script is also committed so a future maintainer can regenerate. NO real customer data, NO real card names from operator's actual collection, NO real binder names from operator's binder set"
    - "src/lib/__tests__/csv-parser-perf.test.ts is a NEW file (planner ls confirms only csv-parser-content.test.ts exists in src/lib/__tests__/). Co-located with the existing parser tests per CONTEXT D-07. Single it() block matching the CONTEXT D-07 spec: reads `test-fixtures/large-export.csv` via `readFileSync` + `path.resolve(__dirname, '../../../test-fixtures/large-export.csv')` (path is relative to the test file's __dirname → up 3 levels to project root → into test-fixtures); calls `parseManaboxCsvContent(csv)` (NOTE: the singular `parseManaboxCsvContent`, not the plural `parseManaboxCsvContents` which takes a UploadedFile[] structure — the singular form takes a string and returns ParseResult per src/lib/__tests__/csv-parser-content.test.ts:48); measures `performance.now()` deltas; asserts `expect(elapsed).toBeLessThan(2000)` AND `expect(result.cards.length).toBeGreaterThan(12000)`. Test file imports: `vitest` (describe/it/expect), `node:fs` (readFileSync), `node:path` (resolve, dirname), the parser. Uses `import.meta.dirname` (Node 20+, project uses @types/node@^20) for the test-fixtures path resolution"
    - "The perf test runs in the default `npm test` execution (NOT skipped, NOT env-gated) per CONTEXT D-07. The test asserts the bound is under 2 seconds; on the runner machine this is the load-bearing pin. The 12,749-row fixture parsed via Papa.parse + the Phase 17 row mapper completes well under 2s on commodity hardware (Phase 17 D-08 SUMMARY confirms parser is O(n) with low constant). If the perf bound flakes on slow CI runners, the executor records a SUMMARY note recommending an env-gated relaxation (`HARD03_PERF_BUDGET_MS` env override) — but the default ships at 2000ms per HARD-03"
    - "Playwright is NOT installed in package.json (planner verified: package.json deps + devDeps contain `next, react, vitest, @testing-library/*, happy-dom, tsx` — no `@playwright/*` or `playwright`). Per CONTEXT D-09, the picker latency target (HARD-03 second clause: 3-second picker render) is documented as a MANUAL UAT step in 22-HUMAN-UAT.md (UAT scenario 1 — operator-on-autopilot binder picker — explicitly times the picker render via the operator's stopwatch / browser DevTools Network tab). Plan 22-02 does NOT add Playwright as a dependency (heavy, requires Chromium download in CI, deferred per CONTEXT D-09 fallback path). The 22-HUMAN-UAT.md scenario 1 acceptance criteria explicitly state `picker renders within 3 seconds of upload click` with the operator-records-pass/fail evidence pattern matching Phase 15 HUMAN-UAT format"
    - "22-HUMAN-UAT.md is a NEW file mirroring the format of `.planning/phases/15-production-hardening/15-HUMAN-UAT.md` (the format precedent per CONTEXT D-11). Required frontmatter (per 15-HUMAN-UAT.md:1-8): status (initially `complete` per the Phase 15 pattern is wrong for a fresh UAT doc — set to `pending` initially; operator updates to `complete` after running scenarios), phase: 22-hardening-uat, source: [22-VERIFICATION.md] (file may not exist yet — that is fine, it is a forward reference), started/updated timestamps, deployment_url: https://wikos-spellbinder.vercel.app (the v1.2 production URL; v1.3 deploys to the same URL after merge per STATE.md). Body sections: `## Current Test` (initially `[awaiting human testing]` per 15 pattern), `## Tests` with 5 numbered scenarios per CONTEXT D-12, `## Summary` with the 5 metrics (total / passed / issues / pending / skipped / blocked) per 15-HUMAN-UAT.md:94-100, `## Gaps` (initially empty)"
    - "22-HUMAN-UAT.md scenario 1 (Operator-on-autopilot binder picker per CONTEXT D-12 #1) format mirrors 15-HUMAN-UAT.md's per-test structure (heading, `expected:`, `how to run:` fenced block, `result:` placeholder, `evidence:` placeholder). Acceptance: (a) operator imports the synthetic 12,749-row CSV (using test-fixtures/large-export.csv as a stand-in if the operator does not want to dirty their inventory) OR a real export; (b) Stage 1 binders message arrives within 3 seconds (HARD-03 picker latency clause — operator measures via browser DevTools Network tab `Time` column on the NDJSON request's first chunk arrival); (c) operator commits the import; (d) operator re-imports the same CSV; (e) picker remembers last selection (Phase 19 D-04 IMP-02 acceptance — selectedBinders pre-checked); (f) WILL DELETE panel surfaces if any binder is missing from the second upload (Phase 19 D-05 IMP-04); (g) commit confirmation requires typed REPLACE before mutating (Phase 19 D-06 acceptance)"
    - "22-HUMAN-UAT.md scenario 2 (v1.2 → v1.3 cart hydration per CONTEXT D-12 #2). Acceptance: (a) operator opens the v1.2 storefront in DevTools, manually injects a v1.2 localStorage cart shape into `localStorage` (the v1.2 key shape is `{cart: [{id: 'sld-1-foil-near_mint', qty: 2}, ...]}` — the 4-segment v1.2 composite id; planner cites Phase 10-03 D-13 + Phase 20 D-09 cart migration spec); (b) operator navigates to v1.3 storefront; (c) one-time toast appears (Phase 20 D-10); (d) cart items reconcile under aggregated keys (Phase 20 D-08 — quantities transferred to matching aggregated card if present, clamped to current stock, silently dropped if no match); (e) no console errors during hydration; (f) cart UI displays the correct (possibly clamped) quantities. how to run block uses concrete DevTools console snippet for the localStorage injection"
    - "22-HUMAN-UAT.md scenario 3 (CHECK constraint trip detection per CONTEXT D-12 #3). Acceptance: (a) operator uses Neon SQL editor against a SANDBOX database (NEVER production — explicit warning in the how-to-run block) to run `UPDATE cards SET quantity = -1 WHERE id = '<test-card-aggregated-key>' LIMIT 1`; (b) UPDATE fails with the Phase 16 BIND-04 CHECK constraint trip (`new row for relation \"cards\" violates check constraint \"cards_quantity_check\"`); (c) operator then attempts a checkout against that card via curl POST /api/checkout; (d) the route returns HTTP 503 (Phase 18 D-08 — CHECK trip caught and surfaced as 503, NOT a silent oversell); (e) the structured log emits `checkout.constraint_violation` (Phase 15-01 logger surface; operator verifies via Vercel function logs OR by reading the response body's error code field); (f) recoverable — a follow-up successful UPDATE restoring quantity to a non-negative value allows the next checkout to succeed normally"
    - "22-HUMAN-UAT.md scenario 4 (Public-page binder leak grep per CONTEXT D-12 #4). Acceptance: (a) operator runs `curl -sS https://wikos-spellbinder.vercel.app/ | tee /tmp/store.html; curl -sS https://wikos-spellbinder.vercel.app/cart | tee /tmp/cart.html; curl -sS -X POST https://wikos-spellbinder.vercel.app/api/checkout -H 'Content-Type: application/json' -d '{\"items\":[{\"id\":\"sld-1-foil-near_mint\",\"qty\":1}],\"buyer\":{\"name\":\"t\",\"email\":\"t@t.com\",\"message\":\"\"}}' | tee /tmp/checkout-success.json` (the success body) AND a stock-conflict-shaped POST (with a deliberately-too-large quantity to force the conflict path) into /tmp/checkout-conflict.json; (b) operator runs `grep -i -E '(a01|a02|a03|a04|a05|a06|a07|a08|a09|a10|a11|a12|a13|a14|bulk drawers|unsorted)' /tmp/store.html /tmp/cart.html /tmp/checkout-success.json /tmp/checkout-conflict.json` against ALL the binder names from the operator's actual binder set (operator substitutes their own binder names — the planner provides a14/bulk drawers as the documented operator binder set per STATE.md cross-cutting constraints); (c) zero hits — the grep returns no lines. The acceptance pin matches Phase 20 D-07 invariant tests (those test the unit-level shape; this UAT verifies the live deployment shape)"
    - "22-HUMAN-UAT.md scenario 5 (Multi-binder concurrent checkout per CONTEXT D-12 #5). Acceptance: (a) operator seeds a sandbox card with stock split across 2 binders summing to e.g. 3 (`a02:2 + a05:1`) via SQL editor; (b) operator fires 5 simultaneous POSTs to /api/checkout requesting that card with quantity=1 each via `for i in $(seq 1 5); do curl ... & done; wait`; (c) exactly 3 succeed (200 OK) and exactly 2 fail with stock_conflict (409); (d) the SUM(quantity) across both binder rows is 0 after the burst; (e) no row has `quantity < 0` (CHECK constraint trip would surface as 503 — different code from 409, so distinguishable). Cleanup: operator deletes the test card. The how-to-run block includes the SQL seed, the burst command, and the verification SUM query"
    - "22-HUMAN-UAT.md `## Summary` block (per 15-HUMAN-UAT.md:93-100) initialized with: total: 5, passed: 0, issues: 0, pending: 5, skipped: 0, blocked: 0. The operator updates these counts after running each scenario. `## Gaps` is initially empty; operator adds entries if any scenario uncovers a defect"
    - "Existing `vitest run` baseline post-Plan 22-01 (target: 463 passed + 2 skipped per Plan 22-01 success criteria) continues to pass; Plan 22-02 adds +1 it() block (csv-parser-perf.test.ts → +1 test). Net target: 464 passed + 2 skipped across 45 files. The synthetic CSV fixture file does NOT count toward test counts; the perf test file's single it() does"
  artifacts:
    - path: "scripts/generate-large-fixture.ts"
      provides: "Synthetic 12,749-row Manabox CSV generator for HARD-03 perf pin fixture"
      contains: "tsx-runnable Node script; uses node:fs writeFileSync; canonical Phase 17 header with Binder Name + Binder Type columns; deterministic synthetic data (fictional sets, names, binder names a01-a14+unsorted, quantity 1-4 random, condition/finish/rarity distribution); idempotent (byte-identical output across runs)"
    - path: "test-fixtures/large-export.csv"
      provides: "Checked-in 12,749-row synthetic Manabox CSV (~2MB) for the perf pin"
      contains: "Header line + 12,749 data lines; fictional cards across ~10 fake sets and 15 binders; explicitly synthetic (no real card data per Phase 17 D-11 privacy)"
    - path: "src/lib/__tests__/csv-parser-perf.test.ts"
      provides: "HARD-03 perf pin: parseManaboxCsvContent of 12,749 rows completes in under 2 seconds"
      contains: "single it() block reading test-fixtures/large-export.csv via readFileSync + path.resolve from import.meta.dirname; performance.now() delta measurement; expect(elapsed).toBeLessThan(2000); expect(result.cards.length).toBeGreaterThan(12000)"
    - path: ".planning/phases/22-hardening-uat/22-HUMAN-UAT.md"
      provides: "5 live-deployment UAT scenarios mirroring Phase 15 HUMAN-UAT format; operator records pass/fail per scenario"
      contains: "frontmatter (status, phase, source, started, updated, deployment_url); ## Current Test placeholder; ## Tests with 5 numbered scenarios (operator-on-autopilot binder picker, v1.2→v1.3 cart hydration, CHECK constraint trip detection, public-page binder leak grep, multi-binder concurrent checkout); each scenario has expected/how-to-run/result/evidence per Phase 15 pattern; ## Summary block initialized to total:5 passed:0 pending:5; ## Gaps empty"
---

<objective>
Land the v1.3 perf pin and live-deployment UAT runbook. Generate a synthetic 12,749-row Manabox CSV fixture (`scripts/generate-large-fixture.ts` → `test-fixtures/large-export.csv`) per CONTEXT D-08; pin the parser performance bound with `src/lib/__tests__/csv-parser-perf.test.ts` asserting `parseManaboxCsvContent(12_749) < 2000ms` per CONTEXT D-07. Verify Playwright is absent from package.json (planner confirmed; it is) and document the picker latency target as a manual UAT step per CONTEXT D-09's fallback path. Author `22-HUMAN-UAT.md` mirroring the Phase 15 HUMAN-UAT format with 5 scenarios per CONTEXT D-11/D-12: operator-on-autopilot binder picker, v1.2→v1.3 cart hydration, CHECK constraint trip detection, public-page binder leak grep, multi-binder concurrent checkout. The operator runs the scenarios on the live deployment (https://wikos-spellbinder.vercel.app) after Plan 22-01 + Plan 22-02 are merged + deployed; this plan ships the runbook, not the operator's pass/fail evidence.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-hardening-uat/22-CONTEXT.md
@.planning/phases/22-hardening-uat/22-DISCUSSION-LOG.md
@.planning/phases/22-hardening-uat/22-01-PLAN.md
@.planning/phases/15-production-hardening/15-HUMAN-UAT.md
@.planning/phases/17-parser-etched/17-01-SUMMARY.md
@.planning/phases/19-import-preview-picker/19-01-SUMMARY.md
@.planning/phases/19-import-preview-picker/19-02-SUMMARY.md
@.planning/phases/20-storefront-aggregation-cart-migration/20-01-SUMMARY.md
@.planning/phases/20-storefront-aggregation-cart-migration/20-02-SUMMARY.md
</execution_context>

<context>
@src/lib/csv-parser.ts
@src/lib/__tests__/csv-parser-content.test.ts
@src/lib/binder-name.ts
@src/lib/types.ts
@scripts/smoke-production.ts
@scripts/migrate-v1.3-binder.ts
@package.json
@.planning/phases/15-production-hardening/15-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the synthetic 12,749-row Manabox CSV fixture (D-08)</name>
  <files>
    scripts/generate-large-fixture.ts,
    test-fixtures/large-export.csv
  </files>
  <read_first>
    src/lib/csv-parser.ts,
    src/lib/__tests__/csv-parser-content.test.ts,
    src/lib/binder-name.ts,
    src/lib/types.ts,
    scripts/migrate-v1.3-binder.ts
  </read_first>
  <behavior>
    - The script writes a 12,749-row Manabox CSV to `test-fixtures/large-export.csv`.
    - Header line uses the canonical Phase 17 column order: `Name,Set code,Set name,Collector number,Condition,Quantity,Foil,Rarity,Binder Name,Binder Type`.
    - All cards are unmistakably synthetic: fictional set codes (`tst`, `fix`, `syn`, plus ~7 more two-or-three-letter fake set codes); fictional names (`Test Card 1`..`Test Card 12749` is acceptable, OR a more realistic-shape name like `Synthetic <Adjective> <Noun> <N>` for variety; planner choice — go with simple `Test Card N` for unmistakable fakeness per Phase 17 D-11 privacy).
    - Binder Name distribution spans 15 distinct names: `a01`..`a14` plus `unsorted`. Distribution is roughly proportional to a real binder set: most rows in `a01`..`a09`, fewer in `a10`..`a14`, ~5% in `unsorted`.
    - Binder Type for every row is the literal `binder` (so the parser does NOT skip them per Phase 17 D-04).
    - Quantity per row: 1-4 (uniform random over the 4-value space).
    - Condition: distributed across 5 Phase 16 enum values (`near_mint, lightly_played, moderately_played, heavily_played, damaged`) with `near_mint` weighted ~50%, `lightly_played` ~25%, others smaller.
    - Foil: `normal` (~94%), `foil` (~5%), `etched` (~1%) — Phase 17 D-01 etched literal coverage.
    - Rarity: `common` (~60%), `uncommon` (~25%), `rare` (~12%), `mythic` (~3%).
    - Set code + Collector number combinations are deterministic (seeded) and DISTINCT enough that the parser produces ~12,749 cards (some overlap is acceptable — the perf test asserts `cards.length > 12000`, not exactly 12749).
    - Output is byte-identical across consecutive runs (deterministic seed via simple modular arithmetic; no random library dependency).
  </behavior>
  <action>
    Create `test-fixtures/` directory. Write the script as `scripts/generate-large-fixture.ts` runnable via `tsx scripts/generate-large-fixture.ts`. The script body builds the rows in a tight for-loop using modular arithmetic to drive the field distributions deterministically (no `Math.random` — that would defeat byte-identical reproduction).

    Run the script once. Verify the output: file size ~2MB, line count 12,750 (header + 12,749 data rows), header is the canonical column set, distribution roughly matches the spec.

    Commit both `scripts/generate-large-fixture.ts` and `test-fixtures/large-export.csv`. Per CONTEXT D-08 the file is checked in unless too noisy; ~2MB is acceptable for a once-generated reproducible fixture.
  </action>
  <verify>
    <automated>tsx scripts/generate-large-fixture.ts && wc -l test-fixtures/large-export.csv && head -1 test-fixtures/large-export.csv && stat -c '%s' test-fixtures/large-export.csv</automated>
    <human>The executor opens test-fixtures/large-export.csv and verifies the header is correct, a sample row matches Manabox shape, and binder names span a01..a14+unsorted.</human>
  </verify>
  <acceptance_criteria>
    - `tsx scripts/generate-large-fixture.ts` runs to completion without error.
    - `wc -l test-fixtures/large-export.csv` reports 12750 (12749 data rows + 1 header).
    - File size is between 1.8MB and 2.2MB.
    - Re-running the script produces a byte-identical file (verifiable via sha256sum before + after).
    - The fixture contains zero real-world card data and zero real-world binder names from the operator's collection.
  </acceptance_criteria>
  <done>The 12,749-row synthetic Manabox CSV fixture exists at test-fixtures/large-export.csv.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the parser perf pin (D-07)</name>
  <files>
    src/lib/__tests__/csv-parser-perf.test.ts
  </files>
  <read_first>
    src/lib/csv-parser.ts,
    src/lib/__tests__/csv-parser-content.test.ts,
    test-fixtures/large-export.csv
  </read_first>
  <behavior>
    - Test file co-located with existing parser tests in `src/lib/__tests__/`.
    - Single it() block: `parses 12,749-row Manabox CSV in under 2 seconds`.
    - Reads `test-fixtures/large-export.csv` via `readFileSync` + path resolved from `import.meta.dirname` (Node 20+ via @types/node@^20).
    - Calls `parseManaboxCsvContent(csv)` (the SINGULAR string-input form, NOT `parseManaboxCsvContents` which expects UploadedFile[]).
    - Measures `performance.now()` delta around the parse call.
    - Asserts `expect(elapsed).toBeLessThan(2000)` AND `expect(result.cards.length).toBeGreaterThan(12000)`.
  </behavior>
  <action>
    Write the test as a single it() block. Use `import.meta.dirname` for `__dirname` equivalent (Node 20+). The fixture path is `path.resolve(import.meta.dirname, '../../../test-fixtures/large-export.csv')` (3 levels up: __tests__/ → lib/ → src/ → project root → test-fixtures/).

    The test MUST run as part of `npm test` (no env-gating, no skip).
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/csv-parser-perf.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test passes (parser parses 12,749 rows in under 2 seconds on the runner machine).
    - Test runs in the default `npm test` execution (not skipped, not env-gated).
    - The test reads the fixture file from disk; if the fixture is missing, the test fails clearly (not silently passes).
  </acceptance_criteria>
  <done>HARD-03 first clause (parser perf pin) is enforced.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Playwright absence (D-10) and document picker latency as manual UAT (D-09)</name>
  <files></files>
  <read_first>
    package.json
  </read_first>
  <behavior>
    - The executor verifies `package.json` does NOT contain `@playwright/test`, `playwright`, or any Playwright-related dependency.
    - Per CONTEXT D-09 fallback path: the picker latency target (3-second render after upload) is documented as a manual UAT step in 22-HUMAN-UAT.md scenario 1 (Task 4).
    - NO files are modified by Task 3 — it is a verification + decision-confirmation task feeding Task 4's UAT authoring.
  </behavior>
  <action>
    Inspect `package.json` dependencies and devDependencies. Confirm Playwright is absent. Record the confirmation in the SUMMARY (Task 5 output) so the deferral is auditable.

    Per CONTEXT D-09 + D-10 + the planner-verified package.json state, the decision is: ship the picker latency as a manual UAT step in scenario 1 of 22-HUMAN-UAT.md. Do NOT add Playwright as a dependency in this plan — that is a heavy commitment requiring CI Chromium downloads, browser binary caching, and a separate test pipeline. The friend-store deployment cadence and milestone scope (final phase, hardening only) do not justify the addition.
  </action>
  <verify>
    <automated>grep -E '"@?playwright' package.json || echo "Playwright absent (confirmed)"</automated>
  </verify>
  <acceptance_criteria>
    - Confirmation that Playwright is not in package.json is recorded for Task 4 + Task 5.
    - No new dependency is added.
  </acceptance_criteria>
  <done>D-09/D-10 fallback path (manual UAT for picker latency) is confirmed.</done>
</task>

<task type="auto">
  <name>Task 4: Author 22-HUMAN-UAT.md with 5 scenarios (D-11 / D-12)</name>
  <files>
    .planning/phases/22-hardening-uat/22-HUMAN-UAT.md
  </files>
  <read_first>
    .planning/phases/15-production-hardening/15-HUMAN-UAT.md,
    .planning/phases/22-hardening-uat/22-CONTEXT.md,
    src/app/api/checkout/route.ts,
    src/app/api/admin/import/preview/route.ts,
    .planning/phases/19-import-preview-picker/19-02-SUMMARY.md,
    .planning/phases/20-storefront-aggregation-cart-migration/20-02-SUMMARY.md
  </read_first>
  <behavior>
    - File frontmatter (per 15-HUMAN-UAT.md:1-8): status: pending, phase: 22-hardening-uat, source: [22-VERIFICATION.md] (forward reference; verification doc may not yet exist), started + updated timestamps (executor sets to current ISO time), deployment_url: https://wikos-spellbinder.vercel.app.
    - `## Current Test` placeholder: `[awaiting human testing]` per Phase 15 pattern.
    - `## Tests` with 5 numbered scenarios per CONTEXT D-12.
    - Each scenario follows the 15-HUMAN-UAT.md per-test structure: heading (`### N. <Scenario name>`), `expected:` paragraph, `how to run:` fenced block (bash or DevTools console), `result:` placeholder (executor leaves blank for operator), `evidence:` placeholder.
    - `## Summary` block: total: 5, passed: 0, issues: 0, pending: 5, skipped: 0, blocked: 0.
    - `## Gaps` empty.
  </behavior>
  <action>
    Author the file. For each scenario, write the `expected:` paragraph as a single sentence summarizing the pass criterion, then the `how to run:` block with concrete commands the operator can copy-paste.

    Scenario 1 (Operator-on-autopilot binder picker — CONTEXT D-12 #1): how-to-run is a sequence of (a) operator opens admin/import in browser, (b) uploads a CSV (could be test-fixtures/large-export.csv or a real export), (c) measures Stage 1 binder picker render time via browser DevTools Network tab `Time` column on the NDJSON request's first chunk arrival — must be ≤ 3000ms (HARD-03 picker latency clause), (d) selects a subset of binders and commits, (e) re-uploads the same CSV, (f) verifies the picker pre-checks the previously-selected binders (Phase 19 IMP-02), (g) if the second upload omits a binder from the prior selection, the WILL DELETE panel surfaces (Phase 19 IMP-04), (h) commit confirmation requires typing REPLACE before mutating (Phase 19 D-06).

    Scenario 2 (v1.2 → v1.3 cart hydration — CONTEXT D-12 #2): how-to-run includes a DevTools console snippet `localStorage.setItem('cart-storage', JSON.stringify({state:{items:[{id:'sld-1-foil-near_mint',qty:2}]},version:0}))` (planner uses the v1.2 4-segment composite id format per Phase 10 + Phase 20 D-09 cart migration spec; executor confirms the actual v1.2 storage key by reading the existing src/lib/store/cart.ts file at execute time and substitutes the correct key/version). Operator navigates to v1.3 storefront, observes the one-time toast (Phase 20 D-10), confirms cart items reconcile under aggregated keys (Phase 20 D-08), confirms quantities clamp to current stock, confirms zero console errors.

    Scenario 3 (CHECK constraint trip detection — CONTEXT D-12 #3): how-to-run includes (a) an explicit warning `NEVER run this UPDATE against production`, (b) the SQL `UPDATE cards SET quantity = -1 WHERE id = '<sandbox-test-card-id>'` against a Neon test branch (NOT production), (c) verification the UPDATE fails with `cards_quantity_check` constraint violation (Phase 16 BIND-04), (d) attempt a checkout via curl POST /api/checkout and verify HTTP 503 (Phase 18 D-08 surfaces the CHECK trip as 503 — distinct from 409 stock_conflict), (e) verify the structured log emits `checkout.constraint_violation` (Phase 15-01 logger event name).

    Scenario 4 (Public-page binder leak grep — CONTEXT D-12 #4): how-to-run includes the curl commands for `GET /`, `GET /cart`, `POST /api/checkout` (success shape) AND `POST /api/checkout` (stock_conflict shape with deliberately oversized quantity), tee'd to /tmp/*.html and /tmp/*.json. Then the grep against ALL the operator's binder names (planner provides a01..a14 + 'bulk drawers' + 'unsorted' as the documented operator binder set; operator substitutes their actual binder names if different). Acceptance: zero hits across all 4 captured response bodies.

    Scenario 5 (Multi-binder concurrent checkout — CONTEXT D-12 #5): how-to-run includes (a) SQL seed against a sandbox DB inserting two rows with the same aggregated key but different binders (e.g., `tst-9999-normal-near_mint-a02` qty=2 + `tst-9999-normal-near_mint-a05` qty=1), (b) burst command `for i in $(seq 1 5); do curl -sS -X POST .../api/checkout -d '{"items":[{"id":"tst-9999-normal-near_mint","qty":1}],...}' & done; wait` against the production deployment (NOT the sandbox seed location — the operator decides whether to run against production with a synthetic test card or against a v1.3 staging deployment), (c) count successes (200) and stock_conflicts (409) — assert exactly 3 successes + 2 conflicts, (d) verify SUM(quantity) = 0 across both binder rows via a follow-up SELECT, (e) cleanup: delete the test rows.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-hardening-uat/22-HUMAN-UAT.md && grep -q "Operator-on-autopilot" .planning/phases/22-hardening-uat/22-HUMAN-UAT.md && grep -q "cart hydration" .planning/phases/22-hardening-uat/22-HUMAN-UAT.md && grep -q "CHECK constraint" .planning/phases/22-hardening-uat/22-HUMAN-UAT.md && grep -q "binder leak" .planning/phases/22-hardening-uat/22-HUMAN-UAT.md && grep -q "concurrent checkout" .planning/phases/22-hardening-uat/22-HUMAN-UAT.md</automated>
    <human>The executor cross-reads the document against CONTEXT D-11/D-12 and confirms each of the 5 scenarios is present with expected/how-to-run/result/evidence sections.</human>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/22-hardening-uat/22-HUMAN-UAT.md` with the required frontmatter.
    - All 5 CONTEXT D-12 scenarios are present, each with expected/how-to-run/result/evidence sections matching the Phase 15 HUMAN-UAT format.
    - Summary block initialized to total:5 passed:0 pending:5.
    - Each how-to-run block contains concrete copy-pasteable commands.
    - Scenario 1 explicitly times the picker render via DevTools Network tab (HARD-03 picker latency clause via the D-09 manual UAT fallback path).
  </acceptance_criteria>
  <done>22-HUMAN-UAT.md is the operator runbook for v1.3 live-deployment UAT.</done>
</task>

<task type="checkpoint:verify" gate="blocking">
  <name>Task 5: Full Plan 22-02 verification</name>
  <files>
    scripts/generate-large-fixture.ts,
    test-fixtures/large-export.csv,
    src/lib/__tests__/csv-parser-perf.test.ts,
    .planning/phases/22-hardening-uat/22-HUMAN-UAT.md
  </files>
  <action>
    Verification must include:
    1. `git diff --check` (no whitespace errors).
    2. `tsx scripts/generate-large-fixture.ts` runs to completion; output file is 12750 lines + ~2MB; rerun produces byte-identical file (sha256sum check).
    3. `npx vitest run src/lib/__tests__/csv-parser-perf.test.ts` passes (parse < 2000ms).
    4. `npx tsc --noEmit` passes.
    5. `npm test` baseline preserved: post-22-01 baseline (target 463 passed + 2 skipped) → at least 464 passed + 2 skipped.
    6. `npm run build` passes.
    7. 22-HUMAN-UAT.md cross-checks against CONTEXT D-11/D-12 with all 5 scenarios present.
    8. No real customer data + no real card data + no real binder names from operator's collection in test-fixtures/large-export.csv.
  </action>
  <done>Plan 22-02 (perf pin + UAT runbook) is verified.</done>
</task>

</tasks>

<verification>
- `git diff --check`
- `tsx scripts/generate-large-fixture.ts && wc -l test-fixtures/large-export.csv`
- Re-run script and `sha256sum test-fixtures/large-export.csv` (deterministic check)
- `npx vitest run src/lib/__tests__/csv-parser-perf.test.ts`
- `npx tsc --noEmit`
- `npm test`
- `npm run build`
- File existence + 5-scenario grep on 22-HUMAN-UAT.md
</verification>

<success_criteria>
- [ ] HARD-03 first clause: `parseManaboxCsvContent(12_749 rows) < 2000ms` perf pin in csv-parser-perf.test.ts (D-07/D-08)
- [ ] HARD-03 second clause: picker render < 3 seconds documented as manual UAT scenario 1 step (D-09/D-10 fallback path)
- [ ] HARD-04: Live-deployment UAT scenarios documented in 22-HUMAN-UAT.md per CONTEXT D-11/D-12 (5 scenarios, Phase 15 format)
- [ ] D-08: Synthetic 12,749-row CSV fixture generated and committed at test-fixtures/large-export.csv
- [ ] D-12: 5 UAT scenarios present (operator-on-autopilot binder picker, v1.2→v1.3 cart hydration, CHECK constraint trip, binder leak grep, multi-binder concurrent checkout)
- [ ] Existing post-22-01 baseline preserved or exceeded
</success_criteria>

<output>
After completion, create `.planning/phases/22-hardening-uat/22-02-SUMMARY.md` with:
- generator script + fixture generation details (deterministic seed approach, distribution stats, file size, sha256)
- perf pin test result (elapsed time on the runner machine)
- Playwright-absent confirmation + manual UAT fallback rationale
- 22-HUMAN-UAT.md scenario-by-scenario walk
- any deviations from CONTEXT D-07..D-12 with justification
- next step: operator runs the 5 scenarios on the live deployment after merge + records pass/fail in 22-HUMAN-UAT.md to close v1.3
</output>
</content>
</invoke>