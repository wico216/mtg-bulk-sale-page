---
phase: 06-database-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle.config.ts
  - src/db/schema.ts
  - src/db/client.ts
  - .env.local.example
  - package.json
autonomous: false
requirements:
  - DB-01
user_setup:
  - service: neon
    why: "Postgres database hosting"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console -> Project -> Connection Details -> Connection string (direct, not pooled)"
    dashboard_config:
      - task: "Create a Neon project"
        location: "https://console.neon.tech -> New Project"

must_haves:
  truths:
    - "Cards table exists in Neon Postgres with all card fields as columns"
    - "Orders table exists with buyer info, totals, and status enum"
    - "Order_items table exists with denormalized card snapshots, FK to orders, and index on order_id"
    - "Indexes exist on cards.name, cards.set_code, orders.created_at, and order_items.order_id"
    - "Drizzle ORM provides typed database client for all tables"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Drizzle table definitions for cards, orders, order_items, orderStatusEnum"
      contains: "pgTable"
    - path: "src/db/client.ts"
      provides: "Neon HTTP database client export"
      exports: ["db"]
    - path: "drizzle.config.ts"
      provides: "Drizzle Kit CLI configuration"
      contains: "defineConfig"
    - path: ".env.local.example"
      provides: "DATABASE_URL placeholder for developers"
      contains: "DATABASE_URL"
  key_links:
    - from: "src/db/client.ts"
      to: "src/db/schema.ts"
      via: "import * as schema"
      pattern: 'import \* as schema from "./schema"'
    - from: "drizzle.config.ts"
      to: "src/db/schema.ts"
      via: "schema config property"
      pattern: 'schema: "./src/db/schema.ts"'
---

<objective>
Create the Drizzle ORM schema for cards, orders, and order_items tables, configure the Neon HTTP database client, provision the Neon database, and push the schema.

Purpose: Establishes the database foundation that all subsequent v1.1 phases depend on. Without this, there is no database to read from (Phase 7), authenticate against (Phase 8), or manage inventory in (Phase 9).

Output: `src/db/schema.ts`, `src/db/client.ts`, `drizzle.config.ts`, updated `.env.local.example`, schema pushed to Neon Postgres.
</objective>

<execution_context>
@/home/wiko/Projects/active/mtg-bulk-sale-page/.claude/get-shit-done/workflows/execute-plan.md
@/home/wiko/Projects/active/mtg-bulk-sale-page/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-database-foundation/06-CONTEXT.md
@.planning/phases/06-database-foundation/06-RESEARCH.md

<interfaces>
<!-- Key types the executor needs from existing codebase -->

From src/lib/types.ts:
```typescript
export interface Card {
  id: string;              // composite: ${setCode}-${collectorNumber}-${foil}-${condition}
  name: string;
  setCode: string;
  setName: string;
  collectorNumber: string;
  price: number | null;    // USD dollars -- DB stores as integer cents (D-02)
  condition: string;
  quantity: number;
  colorIdentity: string[]; // e.g. ["G"], ["W","U"]
  imageUrl: string | null;
  oracleText: string | null;
  rarity: string;
  foil: boolean;
}

export interface OrderItem {
  cardId: string;
  name: string;
  setName: string;
  setCode: string;
  collectorNumber: string;
  condition: string;
  price: number | null;
  quantity: number;
  lineTotal: number | null;
}

export interface OrderData {
  orderRef: string;
  buyerName: string;
  buyerEmail: string;
  message?: string;
  items: OrderItem[];
  totalItems: number;
  totalPrice: number;
  createdAt: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Drizzle dependencies and create schema, client, and config files</name>
  <files>package.json, src/db/schema.ts, src/db/client.ts, drizzle.config.ts, .env.local.example</files>
  <read_first>
    - src/lib/types.ts (Card, OrderItem, OrderData interfaces -- schema must match these fields)
    - src/lib/order.ts (OrderData construction -- informs orders/order_items column design)
    - package.json (current dependencies before adding new ones)
    - .env.local.example (current env vars before adding DATABASE_URL)
    - tsconfig.json (path aliases -- @/* maps to ./src/*)
  </read_first>
  <action>
**Step 1: Install dependencies**

Run:
```bash
npm install drizzle-orm @neondatabase/serverless dotenv
npm install -D drizzle-kit
```

Expected versions: drizzle-orm@0.45.x, @neondatabase/serverless@1.0.x, dotenv@17.x, drizzle-kit@0.31.x

**Step 2: Create `src/db/schema.ts`**

Create the file with these exact table definitions (per D-01 through D-08):

1. **orderStatusEnum** (D-05): `pgEnum("order_status", ["pending", "confirmed", "completed"])`

2. **cards table** (pgTable "cards"):
   - `id`: `text("id").primaryKey()` -- D-01 composite string PK
   - `name`: `text("name").notNull()`
   - `setCode`: `text("set_code").notNull()`
   - `setName`: `text("set_name").notNull()`
   - `collectorNumber`: `text("collector_number").notNull()`
   - `price`: `integer("price")` -- D-02 nullable integer cents
   - `condition`: `text("condition").notNull()`
   - `quantity`: `integer("quantity").notNull().default(0)`
   - `colorIdentity`: `text("color_identity").array().notNull().default(sql\`'{}'::text[]\`)` -- D-03 TEXT[]
   - `imageUrl`: `text("image_url")`
   - `oracleText`: `text("oracle_text")`
   - `rarity`: `text("rarity").notNull()`
   - `foil`: `boolean("foil").notNull().default(false)`
   - `scryfallId`: `text("scryfall_id")` -- D-07 (null until Phase 10 CSV import populates it)
   - `createdAt`: `timestamp("created_at", { withTimezone: true }).notNull().defaultNow()` -- D-04
   - `updatedAt`: `timestamp("updated_at", { withTimezone: true }).notNull().defaultNow().$onUpdate(() => new Date())` -- D-04
   - Indexes (D-08): `index("cards_name_idx").on(table.name)`, `index("cards_set_code_idx").on(table.setCode)`

3. **orders table** (pgTable "orders"):
   - `id`: `text("id").primaryKey()` -- orderRef string like "ORD-20260411-1430"
   - `buyerName`: `text("buyer_name").notNull()`
   - `buyerEmail`: `text("buyer_email").notNull()`
   - `message`: `text("message")`
   - `totalItems`: `integer("total_items").notNull()`
   - `totalPrice`: `integer("total_price").notNull()` -- integer cents
   - `status`: `orderStatusEnum("status").notNull().default("pending")` -- D-05
   - `createdAt`: `timestamp("created_at", { withTimezone: true }).notNull().defaultNow()`
   - Index (D-08): `index("orders_created_at_idx").on(table.createdAt)`

4. **orderItems table** (pgTable "order_items"):
   - `id`: `integer("id").primaryKey().generatedAlwaysAsIdentity()`
   - `orderId`: `text("order_id").notNull().references(() => orders.id, { onDelete: "cascade" })`
   - `cardId`: `text("card_id").notNull()` -- NO FK to cards (denormalized, survives re-imports)
   - `name`: `text("name").notNull()`
   - `setName`: `text("set_name").notNull()`
   - `setCode`: `text("set_code").notNull()`
   - `collectorNumber`: `text("collector_number").notNull()`
   - `condition`: `text("condition").notNull()`
   - `price`: `integer("price")` -- integer cents at time of order
   - `quantity`: `integer("quantity").notNull()`
   - `lineTotal`: `integer("line_total")` -- integer cents
   - `imageUrl`: `text("image_url")` -- snapshot for order history display
   - **Index (addresses Codex review concern: missing index on order_items.order_id)**: `index("order_items_order_id_idx").on(table.orderId)`

Import from `drizzle-orm`: `sql`
Import from `drizzle-orm/pg-core`: `pgTable`, `pgEnum`, `text`, `integer`, `boolean`, `timestamp`, `index`

Export all: `orderStatusEnum`, `cards`, `orders`, `orderItems`

**Step 3: Create `src/db/client.ts`**

```typescript
import { drizzle } from "drizzle-orm/neon-http";
import * as schema from "./schema";

export const db = drizzle(process.env.DATABASE_URL!, { schema });
```

This uses the neon-http driver (HTTP chosen over WebSocket per RESEARCH.md Pattern 1). The `drizzle()` shorthand with a connection string is the current recommended pattern for drizzle-orm 0.45.x.

**Step 4: Create `drizzle.config.ts` at project root**

```typescript
import { config } from "dotenv";
import { defineConfig } from "drizzle-kit";

config({ path: ".env.local" });

export default defineConfig({
  schema: "./src/db/schema.ts",
  out: "./drizzle",
  dialect: "postgresql",
  dbCredentials: {
    url: process.env.DATABASE_URL!,
  },
});
```

The explicit `config({ path: ".env.local" })` is REQUIRED because drizzle-kit reads `.env` by default, not `.env.local` (per RESEARCH.md Pitfall 2).

**Step 5: Update `.env.local.example`**

Add DATABASE_URL to the existing file (keep existing RESEND_API_KEY and SELLER_EMAIL entries):

```
# Neon Postgres database connection string (direct, not pooled)
# Get from: https://console.neon.tech -> Project -> Connection Details
DATABASE_URL=postgresql://user:password@ep-xxxx.us-east-2.aws.neon.tech/neondb?sslmode=require
```

**Step 6: Add convenience scripts to package.json**

Add these scripts to the `"scripts"` section of package.json:

```json
"db:push": "drizzle-kit push",
"db:studio": "drizzle-kit studio",
"db:seed": "tsx src/db/seed.ts"
```

Keep all existing scripts (dev, generate, build, start, lint) unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "drizzle-orm" in dependencies
    - package.json contains "@neondatabase/serverless" in dependencies
    - package.json contains "dotenv" in dependencies
    - package.json contains "drizzle-kit" in devDependencies
    - package.json contains `"db:push": "drizzle-kit push"`
    - package.json contains `"db:studio": "drizzle-kit studio"`
    - package.json contains `"db:seed": "tsx src/db/seed.ts"`
    - src/db/schema.ts contains `pgTable("cards"`
    - src/db/schema.ts contains `pgTable("orders"`
    - src/db/schema.ts contains `pgTable("order_items"`
    - src/db/schema.ts contains `pgEnum("order_status"`
    - src/db/schema.ts contains `text("id").primaryKey()` (D-01 composite string PK)
    - src/db/schema.ts contains `integer("price")` (D-02 cents)
    - src/db/schema.ts contains `.array()` (D-03 TEXT[])
    - src/db/schema.ts contains `timestamp("created_at"` (D-04)
    - src/db/schema.ts contains `timestamp("updated_at"` (D-04)
    - src/db/schema.ts contains `text("scryfall_id")` (D-07)
    - src/db/schema.ts contains `index("cards_name_idx")` (D-08)
    - src/db/schema.ts contains `index("cards_set_code_idx")` (D-08)
    - src/db/schema.ts contains `index("orders_created_at_idx")` (D-08)
    - src/db/schema.ts contains `index("order_items_order_id_idx")` (addresses Codex review: missing index)
    - src/db/schema.ts does NOT contain `deletedAt` or `deleted_at` (D-06 hard delete)
    - src/db/client.ts contains `drizzle(process.env.DATABASE_URL!`
    - src/db/client.ts contains `import * as schema from "./schema"`
    - src/db/client.ts contains `export const db`
    - drizzle.config.ts contains `config({ path: ".env.local" })`
    - drizzle.config.ts contains `schema: "./src/db/schema.ts"`
    - drizzle.config.ts contains `dialect: "postgresql"`
    - .env.local.example contains `DATABASE_URL=`
    - .env.local.example contains `RESEND_API_KEY=` (existing entry preserved)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>All Drizzle ORM dependencies installed. Schema file defines cards (with composite PK, integer cents price, TEXT[] colorIdentity, scryfall_id, timestamps, indexes), orders (with status enum, created_at index), and order_items (denormalized with imageUrl snapshot, no FK to cards, index on order_id). Client exports typed db instance via neon-http driver. Config file at project root with .env.local dotenv loading. Convenience scripts added. TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Provision Neon database and push schema</name>
  <files>.env.local</files>
  <read_first>
    - .env.local.example (reference for required env vars)
    - drizzle.config.ts (confirm it reads from .env.local)
  </read_first>
  <action>
This is a human-action checkpoint. The user must provision a Neon Postgres database and configure the DATABASE_URL.

**What was built:** Drizzle schema, client, and config files are ready. The schema needs a live Neon Postgres database to push to.

**Steps for the user:**
1. Go to https://console.neon.tech and sign in (or create account)
2. Create a new project (any name, e.g., "mtg-bulk-store")
3. Choose a region close to your Vercel deployment (e.g., us-east-2)
4. Copy the **direct** connection string (NOT the pooled one) from Connection Details
   - It looks like: `postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require`
   - Make sure it does NOT contain `-pooler` in the hostname
5. Create `.env.local` in the project root (if it does not exist) and add:
   ```
   DATABASE_URL=postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require
   ```
6. Push the schema by running: `npx drizzle-kit push`
   - Expected output: tables created (cards, orders, order_items) with no errors
   - If push asks about data loss, this is expected on first run against empty DB -- accept it

**Resume signal:** Type "done" after DATABASE_URL is configured and `npx drizzle-kit push` succeeds, or describe any issues.
  </action>
  <verify>
    <automated>npx drizzle-kit push 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - .env.local exists and contains DATABASE_URL=postgresql://
    - npx drizzle-kit push completes without error
    - Neon database has cards, orders, and order_items tables
  </acceptance_criteria>
  <done>Neon Postgres database provisioned. DATABASE_URL configured in .env.local. Schema pushed successfully via drizzle-kit push -- cards, orders, and order_items tables exist in the database with all indexes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine -> Neon DB | Connection string in .env.local crosses network boundary |
| drizzle-kit push -> Neon DB | DDL statements applied to remote database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | .env.local | mitigate | .env.local is gitignored (confirmed in .gitignore: `.env*`). DATABASE_URL never prefixed with NEXT_PUBLIC_. .env.local.example uses placeholder values only. |
| T-06-02 | Tampering | src/db/schema.ts | accept | Schema push is developer-initiated (`drizzle-kit push`). No public-facing schema modification. Solo dev project. |
| T-06-03 | Elevation of Privilege | Neon connection | mitigate | Connection string requires SSL (`sslmode=require` in URL). Neon enforces TLS. No public DB access without credentials. |
</threat_model>

<verification>
1. `npx tsc --noEmit` -- TypeScript compiles with no errors
2. `npx drizzle-kit push` -- schema applies to Neon (or reports no changes if already applied)
3. `grep -c "pgTable" src/db/schema.ts` returns 3 (cards, orders, order_items)
4. `grep -c "index(" src/db/schema.ts` returns 4 (cards_name_idx, cards_set_code_idx, orders_created_at_idx, order_items_order_id_idx)
5. `.env.local.example` contains DATABASE_URL placeholder
</verification>

<success_criteria>
- Neon Postgres database is provisioned and accessible
- Cards table schema has all 16 columns (id, name, setCode, setName, collectorNumber, price, condition, quantity, colorIdentity, imageUrl, oracleText, rarity, foil, scryfallId, createdAt, updatedAt)
- Orders table has status enum column with pending/confirmed/completed values
- Order_items table has denormalized card fields plus imageUrl snapshot, FK to orders with cascade delete, and index on order_id
- Four indexes exist (cards.name, cards.set_code, orders.created_at, order_items.order_id)
- Typed db client exported from src/db/client.ts using neon-http driver
- All existing code continues to compile (npx tsc --noEmit passes)
</success_criteria>

<output>
After completion, create `.planning/phases/06-database-foundation/06-01-SUMMARY.md`
</output>
