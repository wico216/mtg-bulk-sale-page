---
phase: 12-bulk-operations-dashboard
plan: 01
type: execute
wave: 1
depends_on: ["11-02"]
files_modified:
  - src/db/queries.ts
  - src/db/__tests__/dashboard-stats.test.ts
  - src/app/admin/page.tsx
  - src/app/admin/_components/dashboard-summary.tsx
  - src/app/admin/_components/inventory-table.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
must_haves:
  truths:
    - "Admin sees total unique cards, total quantity, total inventory value, low-stock count, and missing-price count above the inventory table"
    - "Admin sees inventory breakdowns by set, color identity, and rarity"
    - "Dashboard stats are computed server-side from database rows, not by refetching all cards in the browser"
    - "Null card prices count as missing prices and contribute $0 to total value"
    - "Colorless cards are grouped under C/Colorless, and multi-color cards use a stable sorted label"
    - "Dashboard gracefully handles empty inventory with zeros and empty breakdowns"
    - "Dashboard stats refresh after existing successful inventory mutations so server-rendered cards do not remain stale"
  artifacts:
    - path: "src/db/queries.ts"
      provides: "Admin dashboard stats query helper"
      exports: ["getAdminDashboardStats"]
    - path: "src/db/__tests__/dashboard-stats.test.ts"
      provides: "Dashboard stats helper tests"
      contains: "getAdminDashboardStats"
    - path: "src/app/admin/_components/dashboard-summary.tsx"
      provides: "Server-rendered dashboard stat cards and breakdown sections"
      contains: "Total inventory value"
    - path: "src/app/admin/page.tsx"
      provides: "Inventory page with dashboard summary above InventoryTable"
      contains: "DashboardSummary"
---

<objective>
Add the at-a-glance admin dashboard to the existing `/admin` Inventory page. This completes DASH-01 and DASH-02 without adding a new route: the seller lands on inventory, sees the store snapshot, then acts.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-bulk-operations-dashboard/12-CONTEXT.md
@.planning/phases/11-checkout-upgrade-order-history/11-02-SUMMARY.md
@.planning/phases/09-admin-inventory-management/09-03-PLAN.md
</execution_context>

<context>
@src/db/queries.ts
@src/db/schema.ts
@src/db/orders.ts
@src/app/admin/page.tsx
@src/app/admin/layout.tsx
@src/app/admin/_components/inventory-table.tsx
@src/lib/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dashboard stats helper tests</name>
  <files>
    src/db/__tests__/dashboard-stats.test.ts
  </files>
  <read_first>
    src/db/queries.ts,
    src/db/schema.ts,
    src/db/__tests__/replace-all-cards.test.ts,
    src/db/__tests__/orders-admin.test.ts
  </read_first>
  <behavior>
    - `getAdminDashboardStats()` returns inventory totals: uniqueCards, totalQuantity, totalValue, lowStockCount, missingPriceCount.
    - Breakdown arrays exist for bySet, byColor, and byRarity.
    - Prices are returned in dollars while DB cents remain internal.
    - Null prices count as missing prices and contribute zero value.
    - Empty color identity arrays normalize to color `C`; multi-color identities sort into stable labels such as `WU`.
    - Empty inventory returns zeros and empty breakdown arrays.
  </behavior>
  <action>
    Write failing tests first. Mock the DB layer at the same level used by existing query-helper tests; do not hit the real database.
  </action>
  <verify>
    <automated>npx vitest run src/db/__tests__/dashboard-stats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because `getAdminDashboardStats` does not exist.
    - Tests cover empty inventory and mixed cards with null prices/colorless/multi-color/rarities.
  </acceptance_criteria>
  <done>Red tests define dashboard stats and breakdown contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dashboard stats helper</name>
  <files>
    src/db/queries.ts,
    src/db/__tests__/dashboard-stats.test.ts
  </files>
  <read_first>
    src/db/queries.ts,
    src/db/schema.ts,
    src/db/__tests__/dashboard-stats.test.ts
  </read_first>
  <behavior>
    - Helper computes inventory stats from DB rows server-side.
    - Helper returns dollars for UI-facing values.
    - Helper avoids exposing raw DB cents outside the DB layer.
    - Helper is deterministic for color, set, and rarity ordering.
  </behavior>
  <action>
    Add `AdminDashboardStats` types and `getAdminDashboardStats()` to `src/db/queries.ts`.

    Prefer the simplest correct implementation for this small dataset:
    - Fetch the needed card fields once from the DB.
    - Aggregate in TypeScript for color identity labels and compact breakdowns.
    - Sort breakdowns by quantity descending, then label ascending.
    - Treat `price === null` as missing and `$0` for value calculations.
  </action>
  <verify>
    <automated>npx vitest run src/db/__tests__/dashboard-stats.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `getAdminDashboardStats` export exists.
    - Dashboard stats tests pass.
    - TypeScript passes.
  </acceptance_criteria>
  <done>Dashboard stats helper is implemented and covered.</done>
</task>

<task type="auto">
  <name>Task 3: Render dashboard summary on admin inventory page</name>
  <files>
    src/app/admin/page.tsx,
    src/app/admin/_components/dashboard-summary.tsx,
    src/app/admin/_components/inventory-table.tsx
  </files>
  <read_first>
    src/app/admin/page.tsx,
    src/app/admin/layout.tsx,
    src/db/queries.ts,
    src/app/admin/orders/_components/orders-table.tsx
  </read_first>
  <behavior>
    - `/admin` renders dashboard stat cards above `InventoryTable`.
    - Stat cards include total unique cards, total quantity, total inventory value, low-stock count, and missing-price count.
    - Breakdown sections show set, color, and rarity summaries.
    - Empty inventory renders zeros without throwing.
    - `/admin` keeps its existing direct server-side `auth()` + `isAdminEmail()` checks.
    - Successful existing table mutations that change dashboard totals trigger an App Router refresh or equivalent server-data refresh.
  </behavior>
  <action>
    Add a server-safe presentational `DashboardSummary` component. Keep it non-interactive and avoid `use client` unless execution proves it necessary.

    Update `/admin/page.tsx` to call `getAdminDashboardStats()` after auth succeeds and render `<DashboardSummary stats={stats} />` above the existing Inventory heading/table.

    Because the dashboard is server-rendered while `InventoryTable` mutates inventory client-side, update the existing successful mutation paths (`handleSave`, `handleDelete`, and `handleDeleteAll`) to refresh server data after they reconcile local table state. Prefer `router.refresh()` from `next/navigation`; preserve existing local UX and toasts.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/admin/page.tsx` imports and renders `DashboardSummary`.
    - `DashboardSummary` displays all DASH-01 and DASH-02 values.
    - Existing successful inline edit, single delete, and delete-all paths refresh server dashboard data without regressing their current table UX.
    - Full tests and build pass.
  </acceptance_criteria>
  <done>Admin dashboard summary is visible on `/admin`.</done>
</task>

<task type="checkpoint:verify" gate="blocking">
  <name>Task 4: Browser verify dashboard summary</name>
  <files>src/app/admin/page.tsx</files>
  <action>
    Browser verification must include:
    1. Sign into admin locally using the existing local browser auth approach.
    2. Visit `/admin`.
    3. Confirm total unique cards, total quantity, total value, low-stock count, and missing-price count render.
    4. Confirm set, color, and rarity breakdown sections render.
    5. Confirm the existing inventory table still renders below the dashboard.
    6. Confirm browser console and network logs have no unexpected errors.
  </action>
  <done>Dashboard summary is verified in the browser.</done>
</task>

</tasks>

<verification>
- `git diff --check`
- `npx vitest run src/db/__tests__/dashboard-stats.test.ts`
- `npx tsc --noEmit`
- `npm test`
- `npm run build`
- Browser verification on `/admin`
</verification>

<success_criteria>
- [ ] DASH-01: Admin sees total cards, quantity, value, low-stock count, and missing-price count at a glance
- [ ] DASH-02: Admin sees inventory breakdown by set, color identity, and rarity
- [ ] Dashboard is server-rendered and keeps existing admin auth checks
- [ ] Inventory table still works below dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/12-bulk-operations-dashboard/12-01-SUMMARY.md` with:
- final dashboard helper contract
- UI placement notes
- browser verification evidence
- known limitations/deferred dashboard ideas
</output>
