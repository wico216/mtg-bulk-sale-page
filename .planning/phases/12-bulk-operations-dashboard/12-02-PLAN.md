---
phase: 12-bulk-operations-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/db/queries.ts
  - src/db/__tests__/bulk-delete-cards.test.ts
  - src/app/api/admin/cards/bulk-delete/route.ts
  - src/app/api/admin/cards/__tests__/bulk-delete-route.test.ts
  - src/app/admin/_components/inventory-table.tsx
  - src/app/admin/_components/action-bar.tsx
autonomous: true
requirements:
  - INV-04
must_haves:
  truths:
    - "Admin can select individual inventory rows with checkboxes"
    - "Admin can select all rows on the current page"
    - "Admin sees the selected count before bulk deletion"
    - "Bulk delete requires explicit inline confirmation before mutation"
    - "Bulk delete removes selected cards only, not the full inventory"
    - "Bulk delete is auth-gated and returns the deleted count"
    - "After bulk delete succeeds, selected rows disappear from the current table and selection clears"
    - "Bulk delete refreshes server-rendered dashboard stats after mutation"
  artifacts:
    - path: "src/db/queries.ts"
      provides: "Bulk delete helper for selected card IDs"
      exports: ["deleteCardsByIds"]
    - path: "src/app/api/admin/cards/bulk-delete/route.ts"
      provides: "Authenticated bulk-delete endpoint"
      contains: "requireAdmin"
    - path: "src/app/admin/_components/inventory-table.tsx"
      provides: "Selectable rows and bulk delete UI state"
      contains: "selectedCardIds"
    - path: "src/app/admin/_components/action-bar.tsx"
      provides: "Bulk action affordance or selected-count display hook"
      contains: "selected"
---

<objective>
Add safe bulk deletion to the admin inventory table. This completes INV-04 while preserving the existing delete-one and delete-all semantics.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-bulk-operations-dashboard/12-CONTEXT.md
@.planning/phases/12-bulk-operations-dashboard/12-01-SUMMARY.md
@.planning/phases/10.1-multi-csv-delete-inventory/10.1-01-SUMMARY.md
</execution_context>

<context>
@src/db/queries.ts
@src/db/schema.ts
@src/app/api/admin/cards/route.ts
@src/app/api/admin/cards/[id]/route.ts
@src/app/api/admin/cards/__tests__/route.test.ts
@src/app/admin/_components/inventory-table.tsx
@src/app/admin/_components/action-bar.tsx
@src/app/admin/_components/delete-confirmation.tsx
@src/app/admin/_components/toast.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bulk-delete helper and route tests</name>
  <files>
    src/db/__tests__/bulk-delete-cards.test.ts,
    src/app/api/admin/cards/__tests__/bulk-delete-route.test.ts
  </files>
  <read_first>
    src/db/queries.ts,
    src/db/__tests__/replace-all-cards.test.ts,
    src/app/api/admin/cards/__tests__/route.test.ts,
    src/lib/auth/admin-check.ts
  </read_first>
  <behavior>
    - `deleteCardsByIds(ids)` deletes only matching card IDs with one atomic delete statement and returns actual deleted IDs/count.
    - Duplicate IDs are de-duped before hitting the DB.
    - Empty ID arrays are rejected at helper or route boundary.
    - `POST /api/admin/cards/bulk-delete` returns 400 for missing/empty/invalid IDs.
    - Bulk delete route returns 401/403 consistently with existing admin routes.
    - Bulk delete failure returns 500 with "Bulk delete failed — inventory unchanged".
  </behavior>
  <action>
    Write failing tests first. Mock `requireAdmin` and DB/query helpers in route tests. Do not hit a real database.
  </action>
  <verify>
    <automated>npx vitest run src/db/__tests__/bulk-delete-cards.test.ts src/app/api/admin/cards/__tests__/bulk-delete-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because helper/route do not exist.
    - Tests prove the route cannot call the delete-all helper.
    - Tests cover validation, auth, success, and failure.
  </acceptance_criteria>
  <done>Red tests define bulk delete contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bulk-delete helper and route</name>
  <files>
    src/db/queries.ts,
    src/app/api/admin/cards/bulk-delete/route.ts
  </files>
  <read_first>
    src/db/queries.ts,
    src/app/api/admin/cards/route.ts,
    src/app/api/admin/cards/[id]/route.ts,
    src/db/__tests__/bulk-delete-cards.test.ts,
    src/app/api/admin/cards/__tests__/bulk-delete-route.test.ts
  </read_first>
  <behavior>
    - Helper deletes only selected card IDs.
    - Route uses `requireAdmin()` and existing JSON auth conventions.
    - Route validates body before mutation.
    - Route name/copy clearly distinguishes bulk delete from delete inventory.
  </behavior>
  <action>
    Add `deleteCardsByIds(ids: string[]): Promise<{ deleted: number; ids: string[] }>` to `src/db/queries.ts`.

    Implement the helper as one database delete statement with `RETURNING`, not a per-ID loop, so the operation cannot partially delete selected rows on statement failure. The returned `ids` must be actual deleted row IDs, not merely the requested IDs.

    Add `POST /api/admin/cards/bulk-delete`:
    - Parse JSON body.
    - Validate `ids` is a non-empty array of strings.
    - Cap request size defensively (for example 500 IDs) even though the current dataset is small.
    - Call `deleteCardsByIds`.
    - Return `{ success: true, deleted, ids }`.
  </action>
  <verify>
    <automated>npx vitest run src/db/__tests__/bulk-delete-cards.test.ts src/app/api/admin/cards/__tests__/bulk-delete-route.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `deleteCardsByIds` export exists.
    - `bulk-delete/route.ts` imports `requireAdmin`.
    - Focused tests and TypeScript pass.
  </acceptance_criteria>
  <done>Bulk-delete API is implemented and covered.</done>
</task>

<task type="auto">
  <name>Task 3: Add row selection and bulk delete UI</name>
  <files>
    src/app/admin/_components/inventory-table.tsx,
    src/app/admin/_components/action-bar.tsx
  </files>
  <read_first>
    src/app/admin/_components/inventory-table.tsx,
    src/app/admin/_components/action-bar.tsx,
    src/app/admin/_components/delete-confirmation.tsx,
    src/app/admin/_components/toast.tsx
  </read_first>
  <behavior>
    - Each inventory row has an accessible checkbox.
    - Table header has a select-all-current-page checkbox with indeterminate state when partially selected.
    - Selected count is visible.
    - Bulk delete button is disabled when nothing is selected.
    - Bulk delete confirmation is inline and says exactly how many selected cards will be deleted.
    - Successful bulk delete removes selected rows, decrements totals, clears selection, refreshes dashboard stats, and shows success toast.
    - Failed bulk delete keeps selection and shows error toast.
  </behavior>
  <action>
    Extend `InventoryTable` with `selectedCardIds` state and handlers:
    - toggle one row
    - select/clear current page rows
    - clear selection when filters/search/page change
    - confirm/cancel bulk delete
    - call `/api/admin/cards/bulk-delete`
    - use the server-data refresh pattern introduced in 12-01 after successful mutation so dashboard stats stay current

    Keep delete-all inventory confirmation separate and visually distinct from bulk delete.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Bulk selection does not break existing single delete, delete-all, inline edit, pagination, search, filters, import, or export controls.
    - Full tests and build pass.
  </acceptance_criteria>
  <done>Admin can select and bulk-delete visible inventory rows safely.</done>
</task>

<task type="checkpoint:verify" gate="blocking">
  <name>Task 4: Browser verify bulk delete</name>
  <files>src/app/admin/_components/inventory-table.tsx</files>
  <action>
    Browser verification must include a disposable DB proof:
    1. Insert at least two selected sentinel cards into the configured DB, plus one unselected sentinel card if no unrelated inventory row already exists.
    2. Sign into admin locally.
    3. Visit `/admin`, search/filter to the sentinel cards, select both rows, and confirm selected count.
    4. Click bulk delete and confirm the inline destructive prompt.
    5. Verify the selected sentinel cards disappear and success toast reports the deleted count.
    6. Verify unrelated inventory remains.
    7. Query DB cleanup state for the sentinel IDs and confirm zero remaining rows.
    8. Confirm browser console and network logs have no unexpected errors.
  </action>
  <done>Bulk delete is verified end-to-end in the browser and DB.</done>
</task>

</tasks>

<verification>
- `git diff --check`
- `npx vitest run src/db/__tests__/bulk-delete-cards.test.ts src/app/api/admin/cards/__tests__/bulk-delete-route.test.ts`
- `npx tsc --noEmit`
- `npm test`
- `npm run build`
- Browser verification of select rows -> bulk delete -> DB cleanup
</verification>

<success_criteria>
- [ ] INV-04: Admin can select multiple cards and delete them in bulk
- [ ] Bulk delete cannot be confused with delete-all inventory
- [ ] Bulk delete is auth-gated and tested
- [ ] Browser/DB verification proves selected rows only are removed
</success_criteria>

<output>
After completion, create `.planning/phases/12-bulk-operations-dashboard/12-02-SUMMARY.md` with:
- final bulk delete API/helper contract
- UI behavior notes
- browser/DB verification evidence
- known limitations around cross-page selection
</output>
