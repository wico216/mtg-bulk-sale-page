---
quick_id: 260514-95g
status: complete
completed: 2026-05-14
commit: 8038d84
---

# Quick Task 260514-95g — SUMMARY

## Outcome

Added a searchable first row to the storefront Set filter list. Selecting or deselecting a set now clears the local set search field so the customer can immediately search for another set or browse the full set list.

## Files Changed

| Path | Change |
|------|--------|
| `src/components/filter-rail.tsx` | Moved the set search control into the scrollable set list as a sticky first row and clears the search after `toggleSet`. |
| `src/components/__tests__/filter-rail.test.tsx` | Added regression coverage for first-row ordering and search clearing after selecting a set. |

## Verification

- `npx vitest run src/components/__tests__/filter-rail.test.tsx` — passed
- `npx tsc --noEmit` — passed
- `npx eslint src/components/filter-rail.tsx src/components/__tests__/filter-rail.test.tsx` — passed

## Notes

- The global filter store was left unchanged; the search query is local UI state for the Set filter.
- Existing clear-search button behavior remains available while typing.
