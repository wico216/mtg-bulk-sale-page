---
quick_id: 260514-95g
slug: add-a-searchable-first-row-to-the-storef
status: planned
created: 2026-05-14
---

# Quick Task 260514-95g — Set List Search Row

## Goal

Add a searchable first row to the storefront Set filter list. When a customer selects a set from the filtered list, clear the set search field so they can immediately search for another set or browse the full list again.

## Plan

### 1. Update Storefront Set Filter Interaction

files:
- `src/components/filter-rail.tsx`

action:
- Move the set search control into the list container as the first row.
- Keep the existing search icon and clear affordance.
- Wrap set toggles so selecting or deselecting a set clears the local set search query.

verify:
- TypeScript accepts the component.
- Manual behavior: type a set query, select a result, search input clears, full set list returns.

done:
- Search is the first row of the set list.
- Selecting a set clears the set search input.

### 2. Add Focused Regression Test

files:
- `src/components/__tests__/filter-rail.test.tsx`

action:
- Render the storefront filter rail with sample cards.
- Open the Set section, search for one set, select it, and assert the search field clears and another set is visible again.

verify:
- `npx vitest run src/components/__tests__/filter-rail.test.tsx`

done:
- Test fails before the behavior change and passes after.
